# PAUL Handoff

**Date:** 2026-03-10
**Status:** Paused

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** helixtones.com — AI-powered preset builder for Line 6 products (Helix, Stomp, Pod Go, Stadium)
**Core value:** Guitarists and bassists can get professional-quality, device-specific Line 6 presets tailored to their exact rig and tonal preferences — without deep technical knowledge of their hardware.

---

## Current State

**Milestone:** v7.0 — Intelligent Dual-DSP Architecture
**Phase:** 21 of 23 — Research & Pattern Extraction
**Plan:** Not started

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ○        ○        ○     [Ready for first PLAN]
```

---

## What Was Done This Session

- Cloned repo and installed dependencies
- Completed v6.0 milestone (was already done — closed it out, archived, tagged v6.0.0)
- Fixed DSP1 block limit crash (v6.1): replaced throwing validation with graceful budget enforcement that drops lowest-priority user effects when a DSP exceeds its 8-block limit
- Committed, tagged v6.1.0, pushed to GitHub
- Discussed the real problem: DSP allocation is dumb (hardcoded pre-cab=DSP0, post-cab=DSP1) — needs intelligent dual-DSP design
- Created v7.0 milestone with 3 phases: Research, Engine, Validation

---

## What's In Progress

- Nothing in progress — clean pause point between milestone creation and first plan

---

## What's Next

**Immediate:** `/paul:plan` for Phase 21 (Research & Pattern Extraction)
- Analyze reference presets for DSP allocation patterns
- Study Line 6 DSP architecture (routing, stereo, resource weight)
- Document allocation rules that professional preset designers follow
- Produce formal ruleset for Phase 22 implementation

**After that:** Phase 22 — Build the intelligent DSP allocation engine

---

## Key Context

- v6.1 added a safety net (drop effects on overflow) but the ROOT CAUSE is unintelligent DSP allocation
- `getDspForSlot` in `src/lib/helix/chain-rules.ts` is a static switch — all post-cab effects go to DSP1 regardless of budget
- DSP0 typically has 5-6 empty slots (only boost + amp use it) while DSP1 overflows
- Reference presets available: 7 Helix, 2 Native, 7 Pod Go, 5 Stomp, 5 Stomp XL, 7 Stadium
- Only Helix family has dual-DSP — Stomp/PodGo/Stadium are single-DSP and unaffected by this work

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview with v7.0 milestone |
| `src/lib/helix/chain-rules.ts` | Signal chain assembly — contains `getDspForSlot` to replace |
| `src/lib/helix/device-family.ts` | Device capabilities (maxBlocksPerDsp, dspCount) |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress`
3. Next action: `/paul:plan` for Phase 21

---

*Handoff created: 2026-03-10*
