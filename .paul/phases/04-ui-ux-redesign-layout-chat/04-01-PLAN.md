---
phase: 04-ui-ux-redesign-layout-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/chat/ChatMessage.tsx
  - src/components/chat/ChatInput.tsx
  - src/components/chat/SuggestionChips.tsx
  - src/components/DevicePicker.tsx
  - src/components/PresetCard.tsx
  - src/components/WelcomeScreen.tsx
autonomous: false
---

<objective>
## Goal
Extract the 1829-line monolithic `page.tsx` into focused components and restructure the page layout as a clean composition of named sections.

## Purpose
The current page.tsx contains everything: device picker, welcome screen, chat messages, input form, suggestion chips, preset card, signal chain viz, rig analysis UI, and generation flow. This makes visual redesign in subsequent plans impossible to do cleanly. Component extraction creates the seams needed for targeted styling and behavior changes.

## Output
- 6 new component files extracted from page.tsx
- page.tsx reduced to ~400-500 lines of composition + state management
- Zero visual regressions — same rendered output before and after
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/app/page.tsx
@src/app/layout.tsx
@src/app/globals.css
@src/components/Footer.tsx
@src/components/DonationCard.tsx
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /ui-ux-pro-max | required | Before UI component work | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.
Run each skill command or confirm already loaded.

## Skill Invocation Checklist
- [ ] /ui-ux-pro-max loaded (run command or confirm)

</skills>

<acceptance_criteria>

## AC-1: Component Extraction Complete
```gherkin
Given the monolithic page.tsx with ~1829 lines
When all extractable UI sections are moved to dedicated component files
Then page.tsx is reduced to <600 lines of composition and state management
And each extracted component is a single-responsibility module
```

## AC-2: Zero Visual Regression
```gherkin
Given the app renders identically before and after extraction
When the dev server is started and all user flows are exercised
Then the welcome screen, chat messages, device picker, input area, preset card, and signal chain all render identically
And no CSS classes, animations, or interactive behaviors are broken
```

## AC-3: Build Passes Clean
```gherkin
Given the refactored component tree
When `npm run build` is executed
Then the build succeeds with zero TypeScript errors
And no new warnings are introduced
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract presentational components from page.tsx</name>
  <files>
    src/app/page.tsx,
    src/components/chat/ChatMessage.tsx,
    src/components/chat/SuggestionChips.tsx,
    src/components/DevicePicker.tsx,
    src/components/PresetCard.tsx,
    src/components/WelcomeScreen.tsx
  </files>
  <action>
    Extract these sections from page.tsx into standalone components:

    1. **ChatMessage** (`src/components/chat/ChatMessage.tsx`):
       - Extract the message rendering logic (user vs assistant bubbles)
       - Props: `message: Message`, `isLatest: boolean`, `isStreaming: boolean`
       - Move the `hlx-msg-ai` / `hlx-msg-user` rendering, ReactMarkdown usage, typing cursor
       - Keep Message interface definition in page.tsx or a shared types file

    2. **SuggestionChips** (`src/components/chat/SuggestionChips.tsx`):
       - Extract the 6-card suggestion grid shown on welcome/idle
       - Props: `onSelect: (text: string) => void`, `disabled: boolean`
       - Move the suggestion text content and chip styling

    3. **DevicePicker** (`src/components/DevicePicker.tsx`):
       - Extract DEVICE_OPTIONS, DEVICE_LABELS constants and the picker grid UI
       - Props: `selected: string | null`, `onSelect: (id: string) => void`, `disabled: boolean`, `compact?: boolean`
       - Used in both welcome screen and generation resume flow — single component, two modes
       - Export DEVICE_OPTIONS and DEVICE_LABELS for use elsewhere

    4. **PresetCard** (`src/components/PresetCard.tsx`):
       - Extract the entire preset result card (`.hlx-preset-card` section)
       - Move SignalChainViz inline component, LED_CSS, BLOCK_LABEL, getBlockLabel into this file
       - Props: preset data (blocks, snapshots, tone description, amp/cab info, download URL, device)
       - Includes signal chain visualization, tone description, snapshot pills, download button

    5. **WelcomeScreen** (`src/components/WelcomeScreen.tsx`):
       - Extract the hero section shown before chat starts (logo, title, description, device picker)
       - Props: `onDeviceSelect: (id: string) => void`, `selectedDevice: string | null`
       - Composes DevicePicker internally

    In page.tsx, replace extracted sections with component imports. Pass state/callbacks as props.
    Do NOT change any CSS classes, styling, or behavior — pure extraction.
    Do NOT rename any CSS custom properties or Tailwind classes.
  </action>
  <verify>
    npm run build passes with zero errors.
    Visual inspection: dev server shows identical rendering.
  </verify>
  <done>AC-1 partially satisfied: 5 components extracted. AC-2 satisfied: no visual changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ChatInput and slim down page.tsx</name>
  <files>
    src/app/page.tsx,
    src/components/chat/ChatInput.tsx
  </files>
  <action>
    1. **ChatInput** (`src/components/chat/ChatInput.tsx`):
       - Extract the sticky-bottom input form (textarea + camera button + send button)
       - Props: `onSubmit: (text: string) => void`, `onCameraClick: () => void`, `disabled: boolean`, `imageCount: number`, `imageAnalyzed: boolean`, `placeholder?: string`
       - Move textarea auto-resize logic, keyboard handling (Enter to send), and input ref management
       - Move `.hlx-input`, `.hlx-send` button rendering

    2. **Slim page.tsx**:
       - After all extractions, page.tsx should contain:
         - State management (useState, useEffect, useCallback hooks)
         - API call logic (chat, generate, vision)
         - Auth/conversation management
         - Composition of extracted components with prop passing
       - Remove any now-unused imports
       - Ensure all extracted component imports are clean

    Do NOT restructure the page layout or change the flex/scroll structure.
    Do NOT modify API call logic or state management patterns.
    Preserve all existing ref management (messagesEndRef, inputRef) — pass refs via props where needed.
  </action>
  <verify>
    npm run build passes with zero errors.
    page.tsx is under 600 lines.
    All interactive flows work: chat, device select, generate, download.
  </verify>
  <done>AC-1 fully satisfied: page.tsx under 600 lines. AC-3 satisfied: clean build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Extracted 6 components from the monolithic page.tsx without changing any visual output or behavior.</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000
    3. Test these flows:
       - Welcome screen renders with logo, title, device picker grid
       - Select a device → chat starts, device badge shows
       - Send a message → user bubble (warm gradient pill) appears
       - AI response streams → assistant message with amber left border, typing cursor
       - Suggestion chips display and are clickable
       - Generate a preset → PresetCard renders with signal chain, snapshots, download
       - Camera button opens rig upload flow
    4. Confirm: All rendering is pixel-identical to before the extraction
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/app/globals.css (no CSS changes in this plan)
- src/app/layout.tsx (layout restructure is a later plan)
- src/components/auth/AuthButton.tsx
- src/components/sidebar/* (sidebar changes are a later plan)
- src/components/visualizer/* (visualizer untouched)
- src/components/Footer.tsx
- src/components/DonationCard.tsx
- Any API routes (src/app/api/*)
- Any lib/ files

## SCOPE LIMITS
- Pure extraction refactor only — no visual changes
- No new CSS classes or styling
- No new dependencies
- No layout restructure (that's Plan 02+)
- No responsive behavior changes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes with zero errors
- [ ] page.tsx is under 600 lines
- [ ] All 6 extracted components exist and are imported
- [ ] Dev server renders identically to pre-extraction state
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- page.tsx reduced from ~1829 to <600 lines
- 6 new component files created with clear single-responsibility boundaries
</success_criteria>

<output>
After completion, create `.paul/phases/04-ui-ux-redesign-layout-chat/04-01-SUMMARY.md`
</output>
