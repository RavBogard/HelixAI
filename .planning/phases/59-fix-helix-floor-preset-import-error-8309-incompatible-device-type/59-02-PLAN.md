---
phase: 59-fix-helix-floor-preset-import-error-8309-incompatible-device-type
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - src/lib/helix/types.ts
  - src/lib/helix/orchestration.test.ts
autonomous: true
requirements:
  - FLOOR-01

must_haves:
  truths:
    - "DEVICE_IDS.helix_floor equals the confirmed value from Plan 01 (not 2162691)"
    - "DEVICE_IDS.helix_floor !== DEVICE_IDS.helix_lt (Floor and LT remain distinct)"
    - "The test literal in orchestration.test.ts matches the new constant (dual-assertion pattern)"
    - "All 170+ tests pass with the updated constant"
    - "The types.ts comment cites the source file, user name, and date for provenance"
  artifacts:
    - path: "src/lib/helix/types.ts"
      provides: "Updated DEVICE_IDS.helix_floor constant with provenance comment"
      contains: "helix_floor"
    - path: "src/lib/helix/orchestration.test.ts"
      provides: "Updated test description and literal assertion matching new device ID"
      contains: "helix_floor"
  key_links:
    - from: "src/lib/helix/types.ts"
      to: "src/lib/helix/preset-builder.ts"
      via: "DEVICE_IDS[device] lookup in buildHlxFile()"
      pattern: "DEVICE_IDS\\[device\\]"
    - from: "src/lib/helix/types.ts"
      to: "src/lib/helix/orchestration.test.ts"
      via: "import DEVICE_IDS and literal assertion"
      pattern: "DEVICE_IDS\\.helix_floor"
---

<objective>
Update the Helix Floor device ID constant and test assertion to the value confirmed in Plan 01.

Purpose: The current `helix_floor = 2162691` causes HX Edit error -8309 for Paul Morgan and Tal Solomon Vardy. Plan 01 obtained the correct device ID from a real Helix Floor hardware export. This plan applies that value to the codebase with full provenance documentation and regression protection.

Output: Updated `types.ts` constant and `orchestration.test.ts` assertion, all tests passing.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-RESEARCH.md
@.planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/helix/types.ts (lines 173-180):
```typescript
export const DEVICE_IDS: Record<DeviceTarget, number> = {
  helix_lt: 2162692,        // 0x210004
  helix_floor: 2162691,     // 0x210003 — THIS IS THE LINE TO CHANGE
  pod_go: 2162695,          // 0x210007
  helix_stadium: 2490368,
  helix_stomp: 2162694,
  helix_stomp_xl: 2162699,
} as const;
```

From src/lib/helix/orchestration.test.ts (lines 90-97):
```typescript
it("buildHlxFile with device='helix_floor' produces .hlx with device=2162691", () => {
  const spec = buildPresetSpec(cleanIntent());
  validatePresetSpec(spec);
  const hlx = buildHlxFile(spec, "helix_floor");

  expect(hlx.data.device).toBe(DEVICE_IDS.helix_floor);  // line 95 — references constant (DO NOT CHANGE)
  expect(hlx.data.device).toBe(2162691);                  // line 96 — literal (MUST CHANGE to match new ID)
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Helix Floor device ID constant and test assertion</name>
  <files>src/lib/helix/types.ts, src/lib/helix/orchestration.test.ts</files>
  <action>
**Read Plan 01 SUMMARY first** to get the confirmed device ID value. The SUMMARY will contain the integer confirmed by Paul Morgan (or another Helix Floor user).

**In `src/lib/helix/types.ts`:**

1. Update `helix_floor` value in `DEVICE_IDS` from `2162691` to the confirmed value from Plan 01 SUMMARY
2. Replace the entire comment on the `helix_floor` line with provenance citing the Phase 59 verification:
   - Format: `helix_floor: XXXX,  // 0xYYYYYY — confirmed from [filename].hlx, Helix Floor, [user name], [date] (Phase 59)`
   - Example: `helix_floor: 2162689,  // 0x210001 — confirmed from [filename].hlx, Helix Floor, Paul Morgan, 2026-03-XX (Phase 59)`
3. Remove the old regression history comments on lines 171-172 (the "Source: Phase 23..." and "Regression: commit 68ad895..." comments). Replace with a single clean comment:
   - `// Device IDs verified from real hardware .hlx/.pgp/.hsp exports`

**In `src/lib/helix/orchestration.test.ts`:**

1. Update the test description on line 90: change `device=2162691` to `device=CONFIRMED_VALUE`
2. Update the literal assertion on line 96: change `.toBe(2162691)` to `.toBe(CONFIRMED_VALUE)`
3. DO NOT change line 95 — it references `DEVICE_IDS.helix_floor` (the constant), which is correct

**Verify:**
- `DEVICE_IDS.helix_floor !== DEVICE_IDS.helix_lt` (Floor and LT must remain distinct devices)
- The test description string matches the literal value
- The literal value matches the `types.ts` constant

**What NOT to change:**
- Do NOT change `preset-builder.ts` — it already uses `DEVICE_IDS[device]` correctly
- Do NOT change `stomp-builder.ts` — same architecture
- Do NOT change `config.ts` firmware constants — `device_version` is not the cause of -8309
- Do NOT change any other DEVICE_IDS entries (helix_lt, pod_go, etc.)
  </action>
  <verify>
    <automated>cd "C:/Users/dsbog/HelixAI/.claude/worktrees/flamboyant-ptolemy" && npx vitest run</automated>
  </verify>
  <done>
- `DEVICE_IDS.helix_floor` equals the confirmed value from Plan 01 (not 2162691)
- `orchestration.test.ts` test description and literal assertion both reflect the new value
- All 170+ tests pass
- The `types.ts` comment cites the source file, user, and date for provenance
- `helix_floor !== helix_lt` assertion passes
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (170+)
2. `npx vitest run src/lib/helix/orchestration.test.ts` — Floor device ID test passes with new literal
3. Grep confirms no stale `2162691` references remain in types.ts or orchestration.test.ts:
   ```bash
   grep -n "2162691" src/lib/helix/types.ts src/lib/helix/orchestration.test.ts
   ```
   Expected: zero matches (unless 2162691 turns out to be the confirmed value, in which case the bug has a different cause)
4. The types.ts `helix_floor` comment includes: filename, user name, date, "Phase 59"
</verification>

<success_criteria>
- DEVICE_IDS.helix_floor matches the hardware-confirmed value from Paul Morgan's Helix Floor export
- All tests pass with the updated constant
- The dual-assertion pattern (constant reference + literal) is maintained for regression detection
- Provenance comment prevents future regressions by documenting the authoritative source
</success_criteria>

<output>
After completion, create `.planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-02-SUMMARY.md`
</output>
