---
phase: 69-audit-gap-closure
plan: 01
title: "Fix Stomp maxFx and Update Traceability"
type: execute
wave: 1
depends_on: []
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, PROMPT-03, STADPARAM-01, STADPARAM-02, STADPARAM-03, STADPARAM-04, KLAYER-01, KLAYER-02, KLAYER-03, KLAYER-04, PROMPT-01, PROMPT-02, PROMPT-04, PROMPT-05, PROMPT-06]
gap_closure: true
autonomous: true
files_to_read:
  - src/lib/planner.ts
  - src/lib/helix/device-family.ts
  - .planning/REQUIREMENTS.md
  - .planning/v5.0-MILESTONE-AUDIT.md
files_to_modify:
  - src/lib/planner.ts
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "planner.ts Stomp maxFx (isXL ? 5 : 2) matches DeviceCapabilities (STOMP_XL=5, STOMP=2)"
    - "REQUIREMENTS.md traceability table shows all 19 gap-closure requirements as Complete with [x] checkboxes"
    - "All existing tests pass after the maxFx fix"
  artifacts:
    - path: "src/lib/planner.ts"
      provides: "Corrected Stomp maxFx values matching DeviceCapabilities"
      contains: "isXL ? 5 : 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with all 19 requirements marked Complete"
  key_links:
    - from: "src/lib/planner.ts"
      to: "src/lib/helix/device-family.ts"
      via: "maxFx values match maxEffectsPerDsp in STOMP_CAPABILITIES (2) and STOMP_XL_CAPABILITIES (5)"
      pattern: "isXL \\? 5 : 2"
---

<objective>
Fix the one real code bug found by the v5.0 milestone audit (Stomp maxFx mismatch in planner.ts) and update the REQUIREMENTS.md traceability table to mark all 19 gap-closure requirements as Complete.

Purpose: Close DISC-01 from the milestone audit and bring the traceability table into sync with the actual codebase state. The integration checker confirmed all 19 requirements are WIRED — the traceability table just needs to reflect that.

Output: Fixed planner.ts, updated REQUIREMENTS.md
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v5.0-MILESTONE-AUDIT.md
@src/lib/planner.ts
@src/lib/helix/device-family.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stomp maxFx values in planner.ts</name>
  <files>src/lib/planner.ts</files>
  <action>
On line 64 of src/lib/planner.ts, change:

```typescript
const maxFx = isXL ? 6 : 4;
```

to:

```typescript
const maxFx = isXL ? 5 : 2;
```

This aligns the planner's DEVICE RESTRICTION message with the actual DeviceCapabilities values:
- STOMP_CAPABILITIES.maxEffectsPerDsp = 2 (line 137 of device-family.ts)
- STOMP_XL_CAPABILITIES.maxEffectsPerDsp = 5 (line 156 of device-family.ts)

The old values (6/4) were incorrect — they told Claude the wrong block budget, though chain-rules silently corrected at assembly time. With this fix, Claude receives accurate budget guidance upfront.

Do NOT change any other values on that line or adjacent lines (blocks and snaps are correct: blocks=isXL?9:6, snaps=isXL?4:3).

After the fix, verify the line reads:
```typescript
const maxFx = isXL ? 5 : 2;
```
  </action>
  <verify>
    <automated>cd "C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana" && npx tsc --noEmit && npx vitest run src/lib/planner.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
Line 64 of planner.ts reads `const maxFx = isXL ? 5 : 2;`. TypeScript compiles. All planner tests pass. The DEVICE RESTRICTION string sent to Claude now says "Keep effects to 5 maximum" for Stomp XL and "Keep effects to 2 maximum" for Stomp, matching DeviceCapabilities exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In .planning/REQUIREMENTS.md, update the traceability table and requirement checkboxes to mark all 19 gap-closure requirements as Complete.

**Checkbox updates (lines 19-46):**
Change all `- [ ]` to `- [x]` for these requirements:
- CAT-01 through CAT-05 (lines 19-23)
- PROMPT-01 through PROMPT-06 (lines 27-32)
- STADPARAM-01 through STADPARAM-04 (lines 36-39)
- KLAYER-01 through KLAYER-04 (lines 43-46)

**Traceability table updates (lines 92-110):**
For each of the 19 Pending entries, change the Status column from "Pending" to "Complete" and update the Phase column to reference the original implementation phase. The format should be:

| CAT-01 | Phase 62 | Complete |
| CAT-02 | Phase 62 | Complete |
| CAT-03 | Phase 62 | Complete |
| CAT-04 | Phase 62 | Complete |
| CAT-05 | Phase 62 | Complete |
| PROMPT-01 | Phase 65 | Complete |
| PROMPT-02 | Phase 65 | Complete |
| PROMPT-03 | Phase 65 | Complete (maxFx fixed in Phase 69) |
| PROMPT-04 | Phase 65 | Complete |
| PROMPT-05 | Phase 65 | Complete |
| PROMPT-06 | Phase 65 | Complete |
| STADPARAM-01 | Phase 63 | Complete |
| STADPARAM-02 | Phase 63 | Complete |
| STADPARAM-03 | Phase 63 | Complete |
| STADPARAM-04 | Phase 63 | Complete |
| KLAYER-01 | Phase 64 | Complete |
| KLAYER-02 | Phase 64 | Complete |
| KLAYER-03 | Phase 64 | Complete |
| KLAYER-04 | Phase 64 | Complete |

Also update the "Last updated" footer line to reflect this change.

Do NOT modify the FRONT-01 through FRONT-04 entries or the ROUTE-01 through ROUTE-04 entries (they are already correct).
  </action>
  <verify>
    <automated>cd "C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana" && grep -c "Pending" .planning/REQUIREMENTS.md && grep -c "\- \[x\]" .planning/REQUIREMENTS.md && grep -c "Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
REQUIREMENTS.md traceability table shows 0 "Pending" entries for the 19 gap-closure requirements. All 27 v5.0 requirements have [x] checkboxes. All traceability rows show "Complete" status. The Phase column for each requirement references its original implementation phase (62, 63, 64, or 65), with PROMPT-03 noting the maxFx fix in Phase 69.
  </done>
</task>

</tasks>

<verification>
1. `grep "isXL ? 5 : 2" src/lib/planner.ts` returns a match
2. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
3. `npx tsc --noEmit` exits 0
4. `npx vitest run` full suite passes with no regressions
</verification>

<success_criteria>
- planner.ts line 64 reads `const maxFx = isXL ? 5 : 2;`
- REQUIREMENTS.md has zero "Pending" entries in the traceability table
- All 27 v5.0 requirement checkboxes are [x]
- TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/69-audit-gap-closure/69-01-SUMMARY.md`
</output>
