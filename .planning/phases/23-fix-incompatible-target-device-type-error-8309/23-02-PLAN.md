---
phase: 23-fix-incompatible-target-device-type-error-8309
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/lib/helix/types.ts
  - src/lib/helix/config.ts
  - src/lib/helix/orchestration.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "A user who selects Helix Floor in HelixAI and imports the generated .hlx into HX Edit on a Helix Floor gets no -8309 error"
    - "All existing tests pass after the constant corrections"
    - "The firmware version stamp in generated files reflects v3.80 (current hardware baseline)"
  artifacts:
    - path: "src/lib/helix/types.ts"
      provides: "Corrected DEVICE_IDS.helix_floor constant"
      contains: "DEVICE_IDS.helix_floor = <correct value from Plan 01>"
    - path: "src/lib/helix/config.ts"
      provides: "Updated firmware version to v3.80"
      contains: "HLX_APP_VERSION: 58720256"
    - path: "src/lib/helix/orchestration.test.ts"
      provides: "Updated test assertion matching new Floor device ID"
      contains: "expect(hlx.data.device).toBe(<new value>)"
  key_links:
    - from: "src/lib/helix/types.ts DEVICE_IDS.helix_floor"
      to: "src/lib/helix/preset-builder.ts buildHlxFile()"
      via: "DEVICE_IDS[device] lookup"
      pattern: "DEVICE_IDS\\[device\\]"
    - from: "src/lib/helix/preset-builder.ts"
      to: "data.device in generated .hlx JSON"
      via: "written into HlxFile.data.device"
      pattern: "data.*device"
---

<objective>
Apply three targeted constant corrections to eliminate the -8309 incompatible target device type error for Helix Floor users.

Purpose: With the confirmed Helix Floor device ID in hand from Plan 01, this plan applies the minimal change: one integer in types.ts, two integers in config.ts, and one test assertion in orchestration.test.ts. No logic changes, no new functions, no API surface changes.

Output: Generated Helix Floor presets contain the correct data.device integer, causing HX Edit to accept them on real Helix Floor hardware.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-fix-incompatible-target-device-type-error-8309/23-RESEARCH.md
@.planning/phases/23-fix-incompatible-target-device-type-error-8309/23-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact code sections being modified. Read these first — no codebase exploration needed. -->

From src/lib/helix/types.ts (lines 171-175):
```typescript
export const DEVICE_IDS: Record<DeviceTarget, number> = {
  helix_lt: 2162692,
  helix_floor: 2162688, // ← REPLACE THIS VALUE with floor-device-id.txt HELIX_FLOOR_DEVICE_ID
  pod_go: 2162695,
} as const;
```

From src/lib/helix/config.ts (lines 6-13):
```typescript
export const FIRMWARE_CONFIG = {
  HLX_VERSION: 6,
  HLX_APP_VERSION: 57671680,  // ← REPLACE: v3.70 BCD → 58720256 (v3.80 BCD = 0x03800000)
  HLX_BUILD_SHA: "v3.70",     // ← REPLACE: "v3.70" → "v3.80"
} as const;
```

From src/lib/helix/orchestration.test.ts (lines 87-94):
```typescript
it("buildHlxFile with device='helix_floor' produces .hlx with device=2162688", () => {
  const spec = buildPresetSpec(cleanIntent());
  validatePresetSpec(spec);
  const hlx = buildHlxFile(spec, "helix_floor");

  expect(hlx.data.device).toBe(DEVICE_IDS.helix_floor);
  expect(hlx.data.device).toBe(2162688);  // ← REPLACE literal with new Floor ID
});
```

Also update the test description string "produces .hlx with device=2162688" → "produces .hlx with device=<new_id>".

Firmware version encoding (BCD):
- v3.70 = 0x03700000 = 57671680  (current)
- v3.80 = 0x03800000 = 58720256  (target)
- v3.82 = 0x03820000 = 58851328  (user's HX Edit version — NOT the target; v3.80 is sufficient)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read confirmed device ID and update types.ts and config.ts</name>
  <files>src/lib/helix/types.ts, src/lib/helix/config.ts</files>
  <behavior>
    - After change: DEVICE_IDS.helix_floor !== 2162688 (old wrong value)
    - After change: DEVICE_IDS.helix_floor === value from floor-device-id.txt
    - After change: FIRMWARE_CONFIG.HLX_APP_VERSION === 58720256
    - After change: FIRMWARE_CONFIG.HLX_BUILD_SHA === "v3.80"
    - helix_lt (2162692) and pod_go (2162695) are UNCHANGED
  </behavior>
  <action>
    Step 1 — Read the confirmed device ID from Plan 01's output:
    cat /c/Users/dsbog/helixai/.planning/phases/23-fix-incompatible-target-device-type-error-8309/floor-device-id.txt

    Extract the HELIX_FLOOR_DEVICE_ID value. If it is UNKNOWN, stop and report that Plan 01 must be completed first.

    If floor-device-id.txt has NOTE containing "confirmed-same" (meaning 2162688 was confirmed by real hardware), then:
    - Do NOT change DEVICE_IDS.helix_floor in types.ts
    - Only update config.ts firmware version
    - Print: "Device ID 2162688 confirmed correct by hardware — updating firmware version only. Bug cause needs further investigation."

    Otherwise (a new integer was found):

    Step 2 — Update src/lib/helix/types.ts:
    Replace the DEVICE_IDS block. Change only helix_floor:
    - helix_lt: 2162692  (unchanged)
    - helix_floor: <NEW_VALUE_FROM_FILE>  (corrected)
    - pod_go: 2162695  (unchanged)
    Add a comment on the helix_floor line: `// confirmed from real Helix Floor .hlx export — fixes -8309`

    Step 3 — Update src/lib/helix/config.ts:
    In FIRMWARE_CONFIG:
    - HLX_APP_VERSION: 58720256  (v3.80 BCD = 0x03800000)
    - HLX_BUILD_SHA: "v3.80"
    Update the JSDoc comment: "encodes firmware version (e.g., 58720256 = FW 3.80)"

    Do NOT change POD_GO_FIRMWARE_CONFIG — that is for Pod Go, not Helix.

    Step 4 — Verify TypeScript compiles:
    cd /c/Users/dsbog/helixai && npx tsc --noEmit 2>&1 | head -20
    Zero errors expected.
  </action>
  <verify>
    <automated>cd /c/Users/dsbog/helixai && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>types.ts has helix_floor set to the confirmed value; config.ts shows HLX_APP_VERSION=58720256 and HLX_BUILD_SHA="v3.80"; TypeScript compiles with zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update orchestration test assertion to match new Floor device ID</name>
  <files>src/lib/helix/orchestration.test.ts</files>
  <behavior>
    - Test "buildHlxFile with device='helix_floor'" passes after the constant update
    - The literal 2162688 in the test assertion is replaced with the new Floor device ID
    - The test description string reflects the new device ID
    - All other tests in orchestration.test.ts continue to pass (helix_lt tests, validation tests, snapshot tests)
  </behavior>
  <action>
    Read the confirmed device ID from floor-device-id.txt (same value used in Task 1).

    In src/lib/helix/orchestration.test.ts, find the test at lines 87-94:
    ```typescript
    it("buildHlxFile with device='helix_floor' produces .hlx with device=2162688", () => {
      ...
      expect(hlx.data.device).toBe(2162688);
    });
    ```

    Make two changes:
    1. Update the test description string: replace "device=2162688" with "device=<NEW_ID>"
    2. Update the literal assertion: replace `.toBe(2162688)` with `.toBe(<NEW_ID>)`

    The assertion `expect(hlx.data.device).toBe(DEVICE_IDS.helix_floor)` on the line above the literal does NOT need changing — it references the constant which is already updated.

    If the device ID was confirmed-same (2162688 stays unchanged): do NOT modify the test file.

    Run tests to verify:
    cd /c/Users/dsbog/helixai && npx vitest run src/lib/helix/orchestration.test.ts 2>&1
  </action>
  <verify>
    <automated>cd /c/Users/dsbog/helixai && npx vitest run src/lib/helix/orchestration.test.ts 2>&1</automated>
  </verify>
  <done>All tests in orchestration.test.ts pass. The helix_floor test description and literal assertion both reflect the new device ID. No other test regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
    Run the complete test suite:
    cd /c/Users/dsbog/helixai && npx vitest run 2>&1

    Expected: all tests pass. The only test that should change behavior is the helix_floor device ID test in orchestration.test.ts.

    If any test fails:
    - Read the failure output carefully
    - Identify whether it is caused by the device ID change or config.ts firmware version change
    - Fix the specific failing assertion (do not revert the constant)

    Common false positive: If other test files also hardcode 2162688 as an expected value (check with grep), those must also be updated to the new Floor ID.

    Check for any other hardcoded device ID references:
    grep -rn "2162688" /c/Users/dsbog/helixai/src/ 2>/dev/null

    Any remaining occurrences of 2162688 (other than comments) indicate a test or code that must also be updated.
  </action>
  <verify>
    <automated>cd /c/Users/dsbog/helixai && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>npx vitest run exits with zero failures. No occurrences of the old literal 2162688 remain in active test assertions (only in comments and this summary).</done>
</task>

</tasks>

<verification>
After all tasks:
1. grep "helix_floor" src/lib/helix/types.ts — shows the new confirmed integer, not 2162688
2. grep "HLX_APP_VERSION" src/lib/helix/config.ts — shows 58720256
3. grep "HLX_BUILD_SHA" src/lib/helix/config.ts — shows "v3.80"
4. npx vitest run — exits 0 (all tests pass)
5. npx tsc --noEmit — exits 0 (TypeScript compiles)
6. grep "2162688" src/ -r — no occurrences in assertions (may appear in comments only)
</verification>

<success_criteria>
- DEVICE_IDS.helix_floor in types.ts contains the confirmed correct integer from a real Helix Floor .hlx export
- FIRMWARE_CONFIG in config.ts reflects v3.80 (HLX_APP_VERSION=58720256, HLX_BUILD_SHA="v3.80")
- orchestration.test.ts helix_floor test description and literal assertion both reflect the new device ID
- All vitest tests pass
- TypeScript compiles with zero errors
- A user who selects Helix Floor and imports the generated .hlx into HX Edit on a Helix Floor no longer gets -8309
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-incompatible-target-device-type-error-8309/23-02-SUMMARY.md`
</output>
