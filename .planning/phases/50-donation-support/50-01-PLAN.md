---
phase: 50-donation-support
plan: 01
type: execute
wave: 1
depends_on: [48-01]
files_modified:
  - src/components/DonationCard.tsx
  - src/components/Footer.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [DONATE-01, DONATE-02, DONATE-03, DONATE-04]
---

<objective>
Add a tasteful post-download inline donation card and a persistent "Support" link in the footer. The donation card appears after the user's first preset download (once per session), is dismissible, and uses the app's design system (no brand colors). Footer's "Support" link re-shows the card.

Payment links:
- PayPal: paypal.me/dsbogard
- Venmo: venmo.com/Daniel-Bogard-1
- CashApp: cash.app/$ravbogard
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create DonationCard component</name>
  <files>src/components/DonationCard.tsx, src/app/globals.css</files>
  <action>
    1. **Create `src/components/DonationCard.tsx`** — A client component:
       - Inline card that renders in the conversation flow (not a modal or popup)
       - Design: matches `.hlx-preset-card` style — `var(--hlx-elevated)` background, `var(--hlx-border)` border, rounded corners
       - Header: small thank-you message (e.g., "Enjoying HelixTones?" or "Hope you dig the preset!")
       - Subtitle: brief ask (e.g., "If this saved you time, consider buying me a coffee")
       - Three buttons in a row:
         - PayPal → opens `https://paypal.me/dsbogard` in new tab
         - Venmo → opens `https://venmo.com/Daniel-Bogard-1` in new tab
         - CashApp → opens `https://cash.app/$ravbogard` in new tab
       - All buttons use `--hlx-*` CSS custom properties only — NO PayPal blue, Venmo teal, or CashApp green
       - Button style: outlined with `var(--hlx-border-warm)`, text in `var(--hlx-text-sub)`, hover fills with subtle `var(--hlx-amber)` tint
       - Each button shows the service name as text (e.g., "PayPal", "Venmo", "CashApp") — no brand logos needed
       - Dismiss button: small "×" or "Maybe later" text button that hides the card
       - Props: `onDismiss: () => void`, `visible: boolean`
       - Transition: fade in/out with CSS transition

    2. **Add `.hlx-donation-card` styles to `src/app/globals.css`** if needed (or use Tailwind inline).
  </action>
  <done>DonationCard component renders inline with correct payment links, dismissible, uses only --hlx-* design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate donation card into page + wire footer Support link</name>
  <files>src/app/page.tsx, src/components/Footer.tsx</files>
  <action>
    1. **Update `src/app/page.tsx`**:
       - Add state: `const [showDonation, setShowDonation] = useState(false)` and `const [donationDismissed, setDonationDismissed] = useState(false)`
       - After the first preset download (in the download handler), set `showDonation(true)` — but only if `!donationDismissed`
       - Render `<DonationCard visible={showDonation && !donationDismissed} onDismiss={() => { setShowDonation(false); setDonationDismissed(true); }} />` in the conversation flow, after the preset result card
       - Pass `onSupportClick={() => { setShowDonation(true); }}` to the `<Footer />` component — this re-shows the card even if previously dismissed (but does NOT reset dismissal state for the auto-trigger)

    2. **Update `src/components/Footer.tsx`**:
       - Wire the `onSupportClick` prop: render a "Support" text link before "A project of..." that calls the callback
       - Style: same `var(--hlx-text-muted)` with `hover:var(--hlx-amber)` transition
       - Separator: use middot between "Support" and "A project of..."
  </action>
  <done>Donation card appears after first download, dismissible, doesn't re-appear after dismissal. Footer "Support" link re-shows it. All success criteria met.</done>
</task>

</tasks>

<verification>
1. `npm run build` — build succeeds
2. Visual: after first download, donation card appears inline
3. Visual: dismiss button hides card, card doesn't re-appear on next download
4. Visual: PayPal/Venmo/CashApp buttons open correct URLs in new tab
5. Visual: Footer "Support" link re-shows donation card after dismissal
6. Visual: all donation UI uses --hlx-* colors only, no brand colors
</verification>

<success_criteria>
- Inline donation card appears after first download (not modal/popup)
- Dismissible, once-per-session auto-show
- PayPal, Venmo, CashApp buttons open correct URLs in new tabs
- Footer "Support" link always visible, re-shows card
- All UI uses --hlx-* CSS custom properties only
</success_criteria>

<output>
After completion, create `.planning/phases/50-donation-support/50-01-SUMMARY.md`
</output>
