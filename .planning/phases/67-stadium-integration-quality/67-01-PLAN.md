---
phase: 67-stadium-integration-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helix/catalogs/stadium-catalog.ts
  - src/lib/helix/catalogs/stadium-catalog.test.ts
  - src/lib/helix/device-family.ts
  - src/lib/helix/device-family.test.ts
  - src/lib/helix/chain-rules.ts
autonomous: true
requirements: [STADQ-01, STADQ-02]
must_haves:
  truths:
    - "STADIUM_EFFECT_NAMES includes all WAH_MODELS keys so Claude can generate wah pedals for Stadium"
    - "STADIUM_EFFECT_NAMES includes all VOLUME_MODELS keys so Claude can generate volume pedals for Stadium"
    - "getToneIntentSchema('stadium') accepts wah and volume model names without Zod rejection"
    - "STADIUM_CAPABILITIES.dualAmpSupported is false so Stadium never triggers the HD2-only dual-amp crash path"
  artifacts:
    - path: "src/lib/helix/catalogs/stadium-catalog.ts"
      provides: "WAH_MODELS + VOLUME_MODELS keys spread into STADIUM_EFFECT_NAMES"
      contains: "WAH_MODELS"
    - path: "src/lib/helix/device-family.ts"
      provides: "dualAmpSupported: false in STADIUM_CAPABILITIES"
      contains: "dualAmpSupported: false"
    - path: "src/lib/helix/catalogs/stadium-catalog.test.ts"
      provides: "Tests asserting WAH/VOLUME are IN STADIUM_EFFECT_NAMES (replaces old no-overlap tests)"
    - path: "src/lib/helix/device-family.test.ts"
      provides: "Test asserting dualAmpSupported === false for helix_stadium"
  key_links:
    - from: "src/lib/helix/catalogs/stadium-catalog.ts"
      to: "src/lib/helix/tone-intent.ts"
      via: "STADIUM_EFFECT_NAMES used as Zod enum source in getToneIntentSchema('stadium')"
      pattern: "STADIUM_EFFECT_NAMES"
    - from: "src/lib/helix/device-family.ts"
      to: "src/lib/helix/chain-rules.ts"
      via: "caps.dualAmpSupported drives isDualAmp gate at line 347"
      pattern: "dualAmpSupported"
---

<objective>
Fix two data-correctness bugs in the Stadium integration layer: (1) add WAH and VOLUME model names to STADIUM_EFFECT_NAMES so the Zod schema accepts them and Claude can generate Stadium presets with wah and volume pedals, and (2) set dualAmpSupported to false in STADIUM_CAPABILITIES to eliminate the crash path where chain-rules would attempt an HD2-only AMP_MODELS lookup for an Agoura amp name.

Purpose: Stadium preset generation must support wah/volume effects (STADQ-01) and must not crash on a dual-amp code path that cannot handle Agoura amp names (STADQ-02).
Output: Fixed stadium-catalog.ts, fixed device-family.ts, updated chain-rules.ts comment, updated tests.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-stadium-integration-quality/67-RESEARCH.md
@src/lib/helix/catalogs/stadium-catalog.ts
@src/lib/helix/catalogs/stadium-catalog.test.ts
@src/lib/helix/device-family.ts
@src/lib/helix/device-family.test.ts
@src/lib/helix/chain-rules.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/helix/models.ts:
```typescript
export const WAH_MODELS: Record<string, HelixModel> = { ... };
export const VOLUME_MODELS: Record<string, HelixModel> = { ... };
export const STADIUM_AMPS: Record<string, HelixModel> = { ... };
```

From src/lib/helix/catalogs/stadium-catalog.ts (current — to be modified):
```typescript
export const STADIUM_EFFECT_NAMES = [
  ...Object.keys(DISTORTION_MODELS),
  ...Object.keys(DELAY_MODELS),
  ...Object.keys(REVERB_MODELS),
  ...Object.keys(MODULATION_MODELS),
  ...Object.keys(DYNAMICS_MODELS),
  ...Object.keys(STADIUM_EQ_MODELS),
] as [string, ...string[]];
```

From src/lib/helix/device-family.ts (current — to be modified):
```typescript
const STADIUM_CAPABILITIES: DeviceCapabilities = {
  family: "stadium",
  dspCount: 1,
  maxBlocksPerDsp: 48,
  maxBlocksTotal: 48,
  maxSnapshots: STADIUM_CONFIG.STADIUM_MAX_SNAPSHOTS,
  dualAmpSupported: true,  // BUG: should be false
  pathCount: 4,
  variaxSupported: false,
  sendReturnCount: 2,
  // ...
};
```

From src/lib/helix/chain-rules.ts (line 347):
```typescript
const isDualAmp = !!(intent.secondAmpName && intent.secondCabName && caps.dualAmpSupported);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WAH/VOLUME to STADIUM_EFFECT_NAMES and update catalog tests</name>
  <files>src/lib/helix/catalogs/stadium-catalog.ts, src/lib/helix/catalogs/stadium-catalog.test.ts</files>
  <behavior>
    - Test: STADIUM_EFFECT_NAMES contains every key from WAH_MODELS
    - Test: STADIUM_EFFECT_NAMES contains every key from VOLUME_MODELS
    - Test: getToneIntentSchema("stadium").safeParse() accepts a wah model name as effectName (verify via tone-intent import)
    - Test: getToneIntentSchema("stadium").safeParse() accepts a volume model name as effectName
  </behavior>
  <action>
    1. In `src/lib/helix/catalogs/stadium-catalog.ts`:
       - Add `WAH_MODELS` and `VOLUME_MODELS` to the import from `"../models"`.
       - Spread `...Object.keys(WAH_MODELS)` and `...Object.keys(VOLUME_MODELS)` into the `STADIUM_EFFECT_NAMES` array, after the existing `STADIUM_EQ_MODELS` spread.
       - Update the JSDoc comment to note Stadium overrides the [62-01] exclusion for WAH/VOLUME (other families still exclude them).

    2. In `src/lib/helix/catalogs/stadium-catalog.test.ts`:
       - Replace the test `"has no overlap with WAH_MODELS keys"` with `"contains all WAH_MODELS keys"` — loop over `Object.keys(WAH_MODELS)` and `expect(STADIUM_EFFECT_NAMES).toContain(key)`.
       - Replace the test `"has no overlap with VOLUME_MODELS keys"` with `"contains all VOLUME_MODELS keys"` — loop over `Object.keys(VOLUME_MODELS)` and `expect(STADIUM_EFFECT_NAMES).toContain(key)`.
       - Add a new test: import `getToneIntentSchema` from `"../tone-intent"` and `STADIUM_AMP_NAMES, STADIUM_CAB_NAMES` from `"./stadium-catalog"`. For one WAH_MODELS key and one VOLUME_MODELS key, safeParse a complete ToneIntent object (using `STADIUM_AMP_NAMES[0]`, `STADIUM_CAB_NAMES[0]`, `guitarType: "humbucker"`, 3 snapshots, and one effect with the wah/volume name). Assert `result.success === true`.

    Note: This overrides decision [62-01] for Stadium ONLY. Helix, Stomp, and Pod Go catalogs remain unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/lib/helix/catalogs/stadium-catalog.test.ts --reporter=verbose</automated>
  </verify>
  <done>STADIUM_EFFECT_NAMES includes all WAH and VOLUME model names; tests pass confirming both catalog membership and schema acceptance</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix dualAmpSupported to false and update capability tests</name>
  <files>src/lib/helix/device-family.ts, src/lib/helix/device-family.test.ts, src/lib/helix/chain-rules.ts</files>
  <behavior>
    - Test: getCapabilities("helix_stadium").dualAmpSupported === false
    - Test: getCapabilities("helix_stadium_xl").dualAmpSupported === false
    - Existing helix_floor dualAmpSupported === true test still passes
    - Existing stomp/podgo dualAmpSupported === false tests still pass
  </behavior>
  <action>
    1. In `src/lib/helix/device-family.ts`:
       - Change `dualAmpSupported: true` to `dualAmpSupported: false` in the `STADIUM_CAPABILITIES` constant (line ~189).
       - Add inline comment: `// Stadium prompt uses includeSecondAmp: false; chain-rules dual-amp path uses HD2-only AMP_MODELS lookup`

    2. In `src/lib/helix/device-family.test.ts`:
       - Change the existing test `"dualAmpSupported === true"` in the `"helix_stadium capabilities"` describe block (line ~172) to assert `false` instead of `true`.
       - Add a test in the `"helix_stadium_xl capabilities"` describe block asserting `getCapabilities("helix_stadium_xl").dualAmpSupported === false`.

    3. In `src/lib/helix/chain-rules.ts`:
       - Update the comment at line ~352 from `"Dual-amp is only for Helix LT/Floor (isDualAmp guard excludes Stadium/Stomp/PodGo)"` to `"Dual-amp is capability-driven — only devices with dualAmpSupported: true (Helix Floor/LT/Rack) enter this path"`. No logic change.
  </action>
  <verify>
    <automated>npx vitest run src/lib/helix/device-family.test.ts --reporter=verbose</automated>
  </verify>
  <done>STADIUM_CAPABILITIES.dualAmpSupported is false; both helix_stadium and helix_stadium_xl return false; chain-rules comment is accurate; all existing capability tests pass</done>
</task>

</tasks>

<verification>
After both tasks, run the full test suite to verify no regressions:

```bash
npx vitest run --reporter=verbose
```

Specific checks:
- `stadium-catalog.test.ts` — all WAH/VOLUME containment tests pass
- `device-family.test.ts` — Stadium dualAmpSupported is false, all other devices unchanged
- No other test files should break (WAH/VOLUME addition is additive; dualAmpSupported change only affects Stadium)
</verification>

<success_criteria>
1. STADIUM_EFFECT_NAMES includes all WAH_MODELS and VOLUME_MODELS keys
2. getToneIntentSchema("stadium") accepts wah and volume model names
3. STADIUM_CAPABILITIES.dualAmpSupported === false
4. chain-rules.ts comment accurately describes the capability-driven isDualAmp gate
5. All tests pass including updated tests and existing regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/67-stadium-integration-quality/67-01-SUMMARY.md`
</output>
