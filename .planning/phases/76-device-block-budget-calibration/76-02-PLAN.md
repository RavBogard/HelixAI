---
phase: 76-device-block-budget-calibration
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - src/lib/families/helix/prompt.ts
  - src/lib/families/stadium/prompt.ts
  - src/lib/families/stomp/prompt.ts
  - src/lib/families/stomp/prompt.test.ts
autonomous: true
requirements: [BUDGET-02, BUDGET-04]

must_haves:
  truths:
    - "Helix prompt maxEffects is 8, not 6"
    - "Stadium prompt maxEffects is 8, not 6"
    - "Stomp prompt block count references match STOMP_MAX_BLOCKS (8)"
    - "No mismatch between prompt maxEffects and DeviceCapabilities maxEffectsPerDsp"
    - "All prompt text referencing '6 effects' or '6 block slots' is updated"
  artifacts:
    - path: "src/lib/families/helix/prompt.ts"
      provides: "maxEffects: 8, '8 is the maximum' text"
      contains: "maxEffects: 8"
    - path: "src/lib/families/stadium/prompt.ts"
      provides: "maxEffects: 8, '4-8 maximum', 'up to 8 effects'"
      contains: "maxEffects: 8"
    - path: "src/lib/families/stomp/prompt.ts"
      provides: "Updated block count references to match STOMP_MAX_BLOCKS=8"
      contains: "8 block slots"
    - path: "src/lib/families/stomp/prompt.test.ts"
      provides: "Updated test assertions for new STOMP_MAX_BLOCKS=8"
      contains: "8 block slots"
  key_links:
    - from: "src/lib/families/helix/prompt.ts"
      to: "src/lib/helix/device-family.ts"
      via: "maxEffects: 8 aligns with Helix maxEffectsPerDsp Infinity (prompt is the practical cap)"
      pattern: "maxEffects: 8"
    - from: "src/lib/families/stadium/prompt.ts"
      to: "src/lib/helix/device-family.ts"
      via: "maxEffects: 8 matches STADIUM_CAPABILITIES.maxEffectsPerDsp = 8"
      pattern: "maxEffects: 8"
    - from: "src/lib/families/stomp/prompt.ts"
      to: "src/lib/helix/config.ts"
      via: "STOMP_CONFIG.STOMP_MAX_BLOCKS used in prompt text (now 8)"
      pattern: "STOMP_CONFIG\\.STOMP_MAX_BLOCKS"
---

<objective>
Update all family prompt texts to match the corrected DeviceCapabilities values from Plan 01, ensuring no mismatch between what the AI is told to generate and what chain-rules allows through.

Purpose: After Plan 01 corrects the underlying capability values, the prompt text still tells the AI outdated limits (Helix: "6 is the maximum", Stadium: "4-6 maximum", "up to 6 effects"). This creates a mismatch where the AI generates fewer effects than the pipeline now supports.

Output: All prompt files updated with correct maxEffects values, stomp prompt test updated, no prompt/capability mismatches remain.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-device-block-budget-calibration/76-RESEARCH.md
@.planning/phases/76-device-block-budget-calibration/76-01-SUMMARY.md

@src/lib/families/helix/prompt.ts
@src/lib/families/stadium/prompt.ts
@src/lib/families/stomp/prompt.ts
@src/lib/families/stomp/prompt.test.ts
@src/lib/families/shared/tone-intent-fields.ts

<interfaces>
<!-- From Plan 01 (completed): corrected values now in effect -->

From src/lib/helix/config.ts (after Plan 01):
```typescript
STOMP_MAX_BLOCKS: 8,      // Was 6
STOMP_XL_MAX_BLOCKS: 8,   // Was 9
```

From src/lib/helix/device-family.ts (after Plan 01):
```typescript
STOMP_CAPABILITIES.maxEffectsPerDsp: 4      // Was 2
STOMP_XL_CAPABILITIES.maxEffectsPerDsp: 4   // Was 5
STADIUM_CAPABILITIES.maxEffectsPerDsp: 8    // Was 4
HELIX_CAPABILITIES.maxEffectsPerDsp: Infinity // Unchanged
```

From src/lib/families/shared/tone-intent-fields.ts:
```typescript
// toneIntentFieldsSection({ maxEffects: N, ... }) produces:
// "- **effects**: Array of up to N effects, each with:"
// This is the primary maxEffects text in all prompts.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update prompt maxEffects and text references for Helix, Stadium, and Stomp</name>
  <files>src/lib/families/helix/prompt.ts, src/lib/families/stadium/prompt.ts, src/lib/families/stomp/prompt.ts</files>
  <action>
    Make these exact text corrections:

    1. **src/lib/families/helix/prompt.ts**:
       - Line 79: `toneIntentFieldsSection({ maxEffects: 6, ...})` -> `maxEffects: 8`
       - Line 90: `"2-4 is typical, 6 is the maximum"` -> `"2-4 is typical, 8 is the maximum"`

    2. **src/lib/families/stadium/prompt.ts**:
       - Line 46: `toneIntentFieldsSection({ maxEffects: 6, ...})` -> `maxEffects: 8`
       - Line 57: `"Keep effects to 4-6 maximum"` -> `"Keep effects to 4-8 maximum"`
       - Line 91: `"up to 6 effects"` -> `"up to 8 effects"`

    3. **src/lib/families/stomp/prompt.ts**:
       - The stomp prompt already uses `maxEffects = 4` (line 70) which is correct -- NO CHANGE to maxEffects.
       - However, the prompt text at line 107 references `${maxBlocks} block slots` where `maxBlocks = STOMP_CONFIG.STOMP_MAX_BLOCKS`. Since Plan 01 changed STOMP_MAX_BLOCKS from 6 to 8, this text will automatically update. Verify the prompt text reads naturally with "8 block slots total" and update any text that no longer makes sense.
       - The planner.ts stompRestriction user message (line 62) now uses STOMP_CONFIG constants too (from Plan 01), so it will show "8 block slots total" -- verify this is correct.

    4. **Pod Go prompt** -- NO CHANGES needed. Pod Go maxEffects=4 and maxBlocksPerDsp=4 are already correct per research.

    After corrections, run the full test suite. If any prompt tests fail due to the new values, update the test assertions accordingly.
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run src/lib/families/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Helix prompt says "up to 8 effects" and "8 is the maximum". Stadium prompt says "up to 8 effects", "4-8 maximum", maxEffects=8. Stomp prompt block count references are consistent with STOMP_MAX_BLOCKS=8. No prompt/capability mismatches remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update stomp prompt test assertions and add prompt/capability alignment verification</name>
  <files>src/lib/families/stomp/prompt.test.ts</files>
  <action>
    1. **Update existing test** at line 33-35:
       The test `"unified prompt references STOMP_MAX_BLOCKS (6) block slots in genre sections"` currently asserts the prompt contains `${STOMP_CONFIG.STOMP_MAX_BLOCKS} block slots`. Since STOMP_MAX_BLOCKS changed from 6 to 8, the test should still pass because it uses the config constant (not a hardcoded "6"). However, update the test description from "(6)" to "(8)" to be accurate: `"unified prompt references STOMP_MAX_BLOCKS (8) block slots in genre sections"`.

    2. **Add new test** to verify prompt/capability alignment for all families:
       - Import getCapabilities from device-family.ts
       - Import buildPlannerPrompt from each family's prompt.ts (or test the ones already imported)
       - Test: "Stomp prompt maxEffects (4) does not exceed Stomp maxEffectsPerDsp (4)"
         Assert that the stomp prompt contains "up to 4 effects" (from toneIntentFieldsSection)
       - Test: "Helix prompt maxEffects (8) is finite despite Infinity maxEffectsPerDsp"
         This is a documentation/sanity test -- Helix has Infinity cap in chain-rules but the prompt provides a practical limit of 8

    3. Run the full test suite to confirm all 704+ tests pass.

    Commit with message: "fix(76-02): update prompt maxEffects to match corrected device capabilities"
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. Stomp prompt test description updated to reflect STOMP_MAX_BLOCKS=8. New alignment tests verify prompt maxEffects values are consistent with DeviceCapabilities. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass
2. `grep -n "maxEffects: 8" src/lib/families/helix/prompt.ts` -- confirms Helix correction
3. `grep -n "maxEffects: 8" src/lib/families/stadium/prompt.ts` -- confirms Stadium correction
4. `grep -n "8 is the maximum" src/lib/families/helix/prompt.ts` -- confirms text update
5. `grep -n "up to 8 effects" src/lib/families/stadium/prompt.ts` -- confirms text update
6. `grep -n "4-8 maximum" src/lib/families/stadium/prompt.ts` -- confirms text update
7. `grep -n "maxEffects: 4" src/lib/families/podgo/prompt.ts` -- confirms Pod Go unchanged
</verification>

<success_criteria>
- Helix prompt: maxEffects=8, "8 is the maximum"
- Stadium prompt: maxEffects=8, "4-8 maximum", "up to 8 effects"
- Stomp prompt: maxEffects=4 (unchanged), block count references use STOMP_MAX_BLOCKS=8
- Pod Go prompt: maxEffects=4 (unchanged)
- No mismatch between any prompt maxEffects and its family's DeviceCapabilities maxEffectsPerDsp
- All stomp prompt tests pass with updated assertions
- Full test suite passes (704+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/76-device-block-budget-calibration/76-02-SUMMARY.md`
</output>
