---
phase: 76-device-block-budget-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helix/config.ts
  - src/lib/helix/device-family.ts
  - src/lib/helix/device-family.test.ts
  - src/lib/helix/tone-intent.ts
  - src/lib/helix/chain-rules.ts
  - src/lib/helix/chain-rules.test.ts
  - src/lib/planner.ts
autonomous: true
requirements: [BUDGET-01, BUDGET-03, BUDGET-05]

must_haves:
  truths:
    - "Stadium DeviceCapabilities maxEffectsPerDsp is 8, not 4"
    - "Stomp DeviceCapabilities maxEffectsPerDsp is 4, not 2"
    - "Stomp XL DeviceCapabilities maxEffectsPerDsp is 4, not 5"
    - "STOMP_MAX_BLOCKS is 8 (FW 3.0+), STOMP_XL_MAX_BLOCKS is 8"
    - "Zod schema accepts up to 10 effects, not 6"
    - "Chain-rules logs console.warn when truncating effects"
    - "Planner.ts maxFx is 4 for both Stomp and Stomp XL"
  artifacts:
    - path: "src/lib/helix/config.ts"
      provides: "STOMP_MAX_BLOCKS=8, STOMP_XL_MAX_BLOCKS=8"
      contains: "STOMP_MAX_BLOCKS: 8"
    - path: "src/lib/helix/device-family.ts"
      provides: "Corrected maxEffectsPerDsp for Stomp(4), StompXL(4), Stadium(8)"
      contains: "maxEffectsPerDsp: 8"
    - path: "src/lib/helix/tone-intent.ts"
      provides: "Zod .max(10) instead of .max(6)"
      contains: ".max(10)"
    - path: "src/lib/helix/chain-rules.ts"
      provides: "console.warn on effect truncation"
      contains: "console.warn"
    - path: "src/lib/planner.ts"
      provides: "Unified maxFx=4 for both Stomp variants"
      contains: "const maxFx = 4"
  key_links:
    - from: "src/lib/helix/config.ts"
      to: "src/lib/helix/device-family.ts"
      via: "STOMP_CONFIG.STOMP_MAX_BLOCKS feeds maxBlocksPerDsp/maxBlocksTotal"
      pattern: "STOMP_CONFIG\\.STOMP_MAX_BLOCKS"
    - from: "src/lib/helix/device-family.ts"
      to: "src/lib/helix/chain-rules.ts"
      via: "caps.maxEffectsPerDsp controls truncation threshold"
      pattern: "caps\\.maxEffectsPerDsp"
---

<objective>
Correct all hardware block budget values in config, DeviceCapabilities, Zod schema, chain-rules truncation, and planner.ts to match real Line 6 hardware limits.

Purpose: The codebase has artificially conservative maxEffectsPerDsp values (Stadium=4, Stomp=2) and incorrect STOMP_MAX_BLOCKS (6 instead of 8 for FW 3.0+). These cause silent truncation of effects that the hardware can actually handle. Chain-rules truncation is also silent -- no log when effects are dropped.

Output: Corrected config constants, DeviceCapabilities, Zod max, chain-rules warning, planner maxFx, plus TDD tests proving all corrections.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-device-block-budget-calibration/76-RESEARCH.md

@src/lib/helix/config.ts
@src/lib/helix/device-family.ts
@src/lib/helix/device-family.test.ts
@src/lib/helix/tone-intent.ts
@src/lib/helix/chain-rules.ts
@src/lib/helix/chain-rules.test.ts
@src/lib/planner.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/helix/config.ts:
```typescript
export const STOMP_CONFIG = {
  STOMP_MAX_BLOCKS: 6,        // CHANGE TO: 8
  STOMP_XL_MAX_BLOCKS: 9,     // CHANGE TO: 8
  STOMP_MAX_SNAPSHOTS: 3,     // NO CHANGE
  STOMP_XL_MAX_SNAPSHOTS: 4,  // NO CHANGE
  // ...other fields unchanged
};
```

From src/lib/helix/device-family.ts:
```typescript
// STOMP_CAPABILITIES.maxEffectsPerDsp: 2 -> 4
// STOMP_XL_CAPABILITIES.maxEffectsPerDsp: 5 -> 4
// STADIUM_CAPABILITIES.maxEffectsPerDsp: 4 -> 8
// HELIX_CAPABILITIES.maxEffectsPerDsp: Infinity (NO CHANGE)
// POD_GO_CAPABILITIES.maxEffectsPerDsp: 4 (NO CHANGE)
```

From src/lib/helix/tone-intent.ts (line 42 in buildToneIntentSchema):
```typescript
effects: z.array(effectSchema).max(6),  // CHANGE TO: .max(10)
```

From src/lib/helix/chain-rules.ts (lines 387-391):
```typescript
// Current (silent truncation):
if (caps.maxEffectsPerDsp < Infinity && userEffects.length > caps.maxEffectsPerDsp) {
    userEffects.length = caps.maxEffectsPerDsp;
}
// ADD: console.warn BEFORE truncation
```

From src/lib/planner.ts (line 64):
```typescript
const maxFx = isXL ? 5 : 2;  // CHANGE TO: const maxFx = 4;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for corrected hardware budget values</name>
  <files>src/lib/helix/device-family.test.ts, src/lib/helix/chain-rules.test.ts</files>
  <behavior>
    - Test: Stomp maxEffectsPerDsp === 4 (currently 2, will fail)
    - Test: Stomp XL maxEffectsPerDsp === 4 (currently 5, will fail)
    - Test: Stadium maxEffectsPerDsp === 8 (currently 4, will fail)
    - Test: Stomp maxBlocksTotal === 8 (currently 6, will fail)
    - Test: Stomp XL maxBlocksTotal === 8 (currently 9, will fail)
    - Test: Helix maxEffectsPerDsp === Infinity (should pass already)
    - Test: Pod Go maxEffectsPerDsp === 4 (should pass already)
    - Test: chain-rules console.warn is called when effects exceed maxEffectsPerDsp (currently no warn, will fail)
    - Test: chain-rules does NOT warn when effects fit within budget (should pass)
    - Test: Stadium with 8 effects produces all 8 in output (currently truncates to 4, will fail)
    - Test: Zod schema accepts 8 effects without error (currently .max(6) rejects, will fail)
    - Test: Zod schema rejects 11 effects (validates upper bound still works)
  </behavior>
  <action>
    In device-family.test.ts:
    1. UPDATE the existing test "maxBlocksTotal === 6" for helix_stomp to assert === 8
    2. UPDATE the existing test "maxBlocksTotal === 9" for helix_stomp_xl to assert === 8
    3. ADD a new describe block "maxEffectsPerDsp correctness" with tests for ALL device families:
       - helix_floor: Infinity
       - helix_stomp: 4
       - helix_stomp_xl: 4
       - pod_go: 4
       - helix_stadium: 8

    In chain-rules.test.ts:
    4. ADD a describe block "effect budget truncation warning (BUDGET-05)" with tests:
       - "logs console.warn when effects exceed maxEffectsPerDsp for Stadium" — create a ToneIntent with 10 effects for Stadium (caps.maxEffectsPerDsp will be 8), spy on console.warn, call assembleSignalChain, assert console.warn was called with message containing "Effect budget exceeded"
       - "does NOT warn when effects fit within budget" — create a ToneIntent with 4 effects for Stadium, assert console.warn NOT called
       - "Stadium with 8 user effects produces all 8 in output" — create a ToneIntent with 8 effects, assert the output chain has 8 user effect blocks (plus amp, cab, mandatory blocks)

    5. ADD a test for Zod schema max in a new file or inline:
       - Import getToneIntentSchema from tone-intent.ts
       - Test: schema.parse with 8 effects succeeds
       - Test: schema.parse with 11 effects throws

    For the chain-rules tests, use real effect model names from the STADIUM_EFFECT_NAMES catalog (e.g., "Agoura Ganymede", "Agoura Adriatic Delay", "Agoura Minotaur", etc.) for Stadium tests. For Helix tests, use HELIX_EFFECT_NAMES names. Import effect names from the appropriate catalog files.

    Use vi.spyOn(console, 'warn') from vitest to spy on console.warn. Remember to restore after each test.

    These tests should FAIL on the current codebase (RED phase). Commit with message: "test(76-01): add failing tests for corrected hardware budget values"
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run src/lib/helix/device-family.test.ts src/lib/helix/chain-rules.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>New tests exist and FAIL on current codebase. Existing passing tests still pass. The failure messages confirm each test is asserting the correct new value against the current incorrect value.</done>
</task>

<task type="auto">
  <name>Task 2: Correct all hardware budget values and add chain-rules warning</name>
  <files>src/lib/helix/config.ts, src/lib/helix/device-family.ts, src/lib/helix/tone-intent.ts, src/lib/helix/chain-rules.ts, src/lib/planner.ts</files>
  <action>
    Make these exact value corrections:

    1. **src/lib/helix/config.ts** (STOMP_CONFIG):
       - Line 69: `STOMP_MAX_BLOCKS: 6` -> `STOMP_MAX_BLOCKS: 8` (update comment to: "FW 3.0+ increased from 6 to 8")
       - Line 71: `STOMP_XL_MAX_BLOCKS: 9` -> `STOMP_XL_MAX_BLOCKS: 8` (update comment to: "Same DSP chip as Stomp -- same 8-block limit")

    2. **src/lib/helix/device-family.ts** (capability constants):
       - STOMP_CAPABILITIES line 137: `maxEffectsPerDsp: 2` -> `maxEffectsPerDsp: 4` (add comment: "8 total - amp(1) - cab(1) - boost(1) - gate(0-1) = 4 user effects")
       - STOMP_XL_CAPABILITIES line 156: `maxEffectsPerDsp: 5` -> `maxEffectsPerDsp: 4` (add comment: "Same DSP/block limit as Stomp")
       - STADIUM_CAPABILITIES line 197: `maxEffectsPerDsp: 4` -> `maxEffectsPerDsp: 8` (add comment: "12 slots - amp(1) - cab(1) - EQ(1) - volume(1) = 8 user effects")

    3. **src/lib/helix/tone-intent.ts** (Zod schema):
       - Line 42: `effects: z.array(effectSchema).max(6)` -> `effects: z.array(effectSchema).max(10)` (add comment: "Raised from 6 to accommodate Stadium 8 effects with headroom")

    4. **src/lib/helix/chain-rules.ts** (truncation warning):
       - At lines 389-391, BEFORE the truncation, add:
       ```typescript
       const dropped = userEffects.length - caps.maxEffectsPerDsp;
       console.warn(
         `[chain-rules] Effect budget exceeded: ${userEffects.length} effects requested, ` +
         `max ${caps.maxEffectsPerDsp} for ${caps.family}. Dropping ${dropped} effect(s): ` +
         userEffects.slice(caps.maxEffectsPerDsp).map(e => e.model.name).join(', ')
       );
       ```

    5. **src/lib/planner.ts** (Stomp variant restriction):
       - Line 64: `const maxFx = isXL ? 5 : 2;` -> `const maxFx = 4;` (both Stomp and Stomp XL share same DSP, same 4 user-effect budget)
       - Line 62: Update `const blocks = isXL ? 9 : 6;` -> `const blocks = isXL ? STOMP_CONFIG.STOMP_XL_MAX_BLOCKS : STOMP_CONFIG.STOMP_MAX_BLOCKS;` (use config constants instead of hardcoded values). Import STOMP_CONFIG from config.ts if not already imported.

    After all corrections, run the full test suite. ALL tests from Task 1 should now PASS (GREEN phase).

    Commit with message: "fix(76-01): correct hardware budget values for all device families"
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>All 704+ tests pass. STOMP_MAX_BLOCKS=8, STOMP_XL_MAX_BLOCKS=8, Stomp maxEffectsPerDsp=4, StompXL maxEffectsPerDsp=4, Stadium maxEffectsPerDsp=8, Zod .max(10), chain-rules logs console.warn on truncation, planner.ts maxFx=4 for both Stomp variants.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (existing + new)
2. `grep -n "STOMP_MAX_BLOCKS: 8" src/lib/helix/config.ts` -- confirms value correction
3. `grep -n "maxEffectsPerDsp: 8" src/lib/helix/device-family.ts` -- confirms Stadium correction
4. `grep -n "maxEffectsPerDsp: 4" src/lib/helix/device-family.ts` -- confirms Stomp/StompXL corrections (should appear twice)
5. `grep -n "\.max(10)" src/lib/helix/tone-intent.ts` -- confirms Zod schema correction
6. `grep -n "console.warn" src/lib/helix/chain-rules.ts` -- confirms truncation warning added
7. `grep -n "const maxFx = 4" src/lib/planner.ts` -- confirms planner correction
</verification>

<success_criteria>
- STOMP_MAX_BLOCKS=8, STOMP_XL_MAX_BLOCKS=8 in config.ts
- Stomp maxEffectsPerDsp=4, Stomp XL maxEffectsPerDsp=4, Stadium maxEffectsPerDsp=8 in device-family.ts
- Helix maxEffectsPerDsp=Infinity unchanged, Pod Go maxEffectsPerDsp=4 unchanged
- Zod schema .max(10) in tone-intent.ts
- console.warn before truncation in chain-rules.ts with descriptive message
- planner.ts maxFx=4 for both Stomp variants
- All tests pass (704+ including new TDD tests)
</success_criteria>

<output>
After completion, create `.planning/phases/76-device-block-budget-calibration/76-01-SUMMARY.md`
</output>
