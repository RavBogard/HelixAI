---
phase: 39-hx-stomp-support
plan: 03
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [STOMP-07]

must_haves:
  truths:
    - "Device picker shows STOMP and STOMP XL as selectable options in both picker arrays"
    - "Clicking STOMP sets selectedDevice to helix_stomp and triggers generation"
    - "Clicking STOMP XL sets selectedDevice to helix_stomp_xl and triggers generation"
    - "Download filename uses _Stomp.hlx and _StompXL.hlx suffixes"
    - "Device badge shows STOMP or STOMP XL for the respective devices"
    - "Generate for other device chip handles 6 devices correctly"
    - "downloadStoredPreset handles Stomp devices with correct suffix"
    - "loadConversation setSelectedDevice type union includes helix_stomp and helix_stomp_xl"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Both device picker arrays updated, download suffix cases, badge cases, chip logic, type union extended"
      contains: "helix_stomp"
  key_links:
    - from: "src/app/page.tsx selectedDevice state"
      to: "src/app/api/generate/route.ts"
      via: "device field in POST body to /api/generate"
      pattern: "helix_stomp"
---

<objective>
Add HX Stomp and HX Stomp XL to the page.tsx UI. Update both device picker arrays (rig picker at ~line 1277 and chat picker at ~line 1368) from 4 to 6 devices, update the grid layout, add download suffix cases, update the device badge switch, update the continuation chip's otherDevice logic, update the selectedDevice type union, and update loadConversation's setSelectedDevice cast.

Purpose: This is the final user-facing step. After Plans 01 and 02 wire the backend, this plan makes Stomp visible and selectable in the UI so users can generate Stomp presets.

Output: Updated page.tsx. npm run build passes. Both device pickers show all 6 devices. Download produces correctly named files.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-hx-stomp-support/39-RESEARCH.md
@.planning/phases/39-hx-stomp-support/39-01-SUMMARY.md

<interfaces>
<!-- Key page.tsx locations to update. Line numbers are approximate — read the file to confirm. -->

Location 1 — selectedDevice useState type (line ~309):
```typescript
// CURRENT:
const [selectedDevice, setSelectedDevice] = useState<"helix_lt" | "helix_floor" | "pod_go" | "helix_stadium">("helix_lt");
// TARGET: add helix_stomp | helix_stomp_xl to the inline union
```

Location 2 — downloadPreset() device suffix switch (line ~763-768):
```typescript
// CURRENT:
const deviceSuffix =
  generatedPreset.device === "helix_lt" ? "_LT"
  : generatedPreset.device === "helix_floor" ? "_Floor"
  : generatedPreset.device === "pod_go" ? "_PodGo"
  : generatedPreset.device === "helix_stadium" ? "_Stadium"
  : "";
// TARGET: add Stomp cases before the final ""
```

Location 3 — downloadStoredPreset() device suffix switch (line ~800-805):
```typescript
// CURRENT:
const deviceSuffix =
  selectedDevice === "helix_lt" ? "_LT"
  : selectedDevice === "helix_floor" ? "_Floor"
  : selectedDevice === "pod_go" ? "_PodGo"
  : selectedDevice === "helix_stadium" ? "_Stadium"
  : "";
// TARGET: add Stomp cases
```

Location 4 — loadConversation() setSelectedDevice cast (line ~846):
```typescript
// CURRENT:
setSelectedDevice(data.device as "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium");
// TARGET: add helix_stomp | helix_stomp_xl to the cast
```

Location 5 — Rig device picker array (line ~1277-1283, grid-cols-4):
```typescript
// CURRENT: 4 devices in grid-cols-4
{ id: "helix_lt" as const, label: "LT", desc: "Helix LT" },
{ id: "helix_floor" as const, label: "FLOOR", desc: "Helix Floor" },
{ id: "pod_go" as const, label: "POD GO", desc: "Pod Go" },
{ id: "helix_stadium" as const, label: "STADIUM", desc: "Helix Stadium" },
// TARGET: add 2 more, change grid class
```

Location 6 — Chat device picker array (line ~1368-1374, grid-cols-4):
```typescript
// CURRENT: 4 devices in grid-cols-4
{ id: "helix_lt" as const, label: "LT", desc: "Helix LT" },
{ id: "helix_floor" as const, label: "FLOOR", desc: "Helix Floor" },
{ id: "pod_go" as const, label: "POD GO", desc: "Pod Go" },
{ id: "helix_stadium" as const, label: "STADIUM", desc: "Helix Stadium" },
// TARGET: add 2 more, change grid class
```

Location 7 — Device badge (line ~1431-1434):
```typescript
// CURRENT:
{generatedPreset.device === "helix_lt" ? "LT"
  : generatedPreset.device === "helix_floor" ? "FLOOR"
  : generatedPreset.device === "helix_stadium" ? "STADIUM"
  : "POD GO"}
// TARGET: add Stomp cases before the final "POD GO" fallback
```

Location 8 — "Generate for other device" chip (lines ~1545-1556):
```typescript
// CURRENT:
const otherDevice = selectedDevice === "pod_go" ? "helix_lt"
  : selectedDevice === "helix_stadium" ? "helix_lt"
  : selectedDevice === "helix_lt" ? "helix_floor"
  : "helix_lt"
// Label: selectedDevice === "pod_go" || selectedDevice === "helix_stadium" ? "Helix LT"
//        : selectedDevice === "helix_lt" ? "Helix Floor" : "Helix LT"
// TARGET: add Stomp → "helix_lt" cases; update label to show "Helix LT" for Stomp
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update selectedDevice type union, download suffix cases, and badge</name>
  <files>src/app/page.tsx</files>
  <action>
    Read page.tsx from line 300 to 850 to confirm exact current locations before editing.

    === Change 1: selectedDevice useState type union (line ~309) ===
    Add "helix_stomp" | "helix_stomp_xl" to the inline union type:
    ```typescript
    const [selectedDevice, setSelectedDevice] = useState<
      "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium" | "helix_stomp" | "helix_stomp_xl"
    >("helix_lt");
    ```

    === Change 2: downloadPreset() device suffix (line ~763-768) ===
    Add Stomp cases before the final empty string fallback:
    ```typescript
    const deviceSuffix =
      generatedPreset.device === "helix_lt" ? "_LT"
      : generatedPreset.device === "helix_floor" ? "_Floor"
      : generatedPreset.device === "pod_go" ? "_PodGo"
      : generatedPreset.device === "helix_stadium" ? "_Stadium"
      : generatedPreset.device === "helix_stomp" ? "_Stomp"
      : generatedPreset.device === "helix_stomp_xl" ? "_StompXL"
      : "";
    ```

    === Change 3: downloadStoredPreset() device suffix (line ~800-805) ===
    Same pattern — add Stomp cases:
    ```typescript
    const deviceSuffix =
      selectedDevice === "helix_lt" ? "_LT"
      : selectedDevice === "helix_floor" ? "_Floor"
      : selectedDevice === "pod_go" ? "_PodGo"
      : selectedDevice === "helix_stadium" ? "_Stadium"
      : selectedDevice === "helix_stomp" ? "_Stomp"
      : selectedDevice === "helix_stomp_xl" ? "_StompXL"
      : "";
    ```

    === Change 4: loadConversation() setSelectedDevice type cast (line ~846) ===
    Extend the as-cast to include Stomp variants:
    ```typescript
    setSelectedDevice(data.device as "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium" | "helix_stomp" | "helix_stomp_xl");
    ```

    === Change 5: Device badge (line ~1431-1434) ===
    Add Stomp cases before the final "POD GO" fallback:
    ```typescript
    {generatedPreset.device === "helix_lt" ? "LT"
      : generatedPreset.device === "helix_floor" ? "FLOOR"
      : generatedPreset.device === "helix_stadium" ? "STADIUM"
      : generatedPreset.device === "helix_stomp" ? "STOMP"
      : generatedPreset.device === "helix_stomp_xl" ? "STOMP XL"
      : "POD GO"}
    ```

    After making all 5 changes, run: npx tsc --noEmit 2>&1 | head -20
    Fix any TypeScript errors before continuing.
  </action>
  <verify>
    <automated>cd /c/Users/dsbog/HelixAI && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>selectedDevice type union includes helix_stomp and helix_stomp_xl; download functions produce _Stomp.hlx and _StompXL.hlx filenames; badge shows STOMP and STOMP XL; loadConversation cast accepts Stomp devices; TypeScript accepts all changes</done>
</task>

<task type="auto">
  <name>Task 2: Update both device picker arrays and the otherDevice chip logic</name>
  <files>src/app/page.tsx</files>
  <action>
    Read page.tsx from line 1265 to 1560 to confirm exact locations before editing.

    === Change 1: Rig device picker (line ~1277) ===

    Current grid class: "grid grid-cols-4 gap-2"
    Current array: 4 entries

    Update to use two-row layout with grid-cols-3:
    ```typescript
    <div className="grid grid-cols-3 gap-2">
      {([
        { id: "helix_lt" as const, label: "LT", desc: "Helix LT" },
        { id: "helix_floor" as const, label: "FLOOR", desc: "Helix Floor" },
        { id: "helix_stadium" as const, label: "STADIUM", desc: "Helix Stadium" },
        { id: "pod_go" as const, label: "POD GO", desc: "Pod Go" },
        { id: "helix_stomp" as const, label: "STOMP", desc: "HX Stomp" },
        { id: "helix_stomp_xl" as const, label: "STOMP XL", desc: "HX Stomp XL" },
      ]).map(({ id, label, desc }) => (
    ```

    Grid layout rationale: 3 columns × 2 rows = 6 devices.
    Row 1: LT | FLOOR | STADIUM
    Row 2: POD GO | STOMP | STOMP XL
    This groups the standard Helix LT-sized form factors on row 1 and compact/pedalboard devices on row 2.

    Keep all other parts of the map callback exactly the same (button styles, disabled logic, onClick, etc.).

    === Change 2: Chat device picker (line ~1368) ===

    Current: "grid grid-cols-4 gap-3 w-full max-w-sm"
    Current array: 4 entries

    Update:
    ```typescript
    <div className="grid grid-cols-3 gap-3 w-full max-w-sm">
      {([
        { id: "helix_lt" as const, label: "LT", desc: "Helix LT" },
        { id: "helix_floor" as const, label: "FLOOR", desc: "Helix Floor" },
        { id: "helix_stadium" as const, label: "STADIUM", desc: "Helix Stadium" },
        { id: "pod_go" as const, label: "POD GO", desc: "Pod Go" },
        { id: "helix_stomp" as const, label: "STOMP", desc: "HX Stomp" },
        { id: "helix_stomp_xl" as const, label: "STOMP XL", desc: "HX Stomp XL" },
      ]).map(({ id, label, desc }) => (
    ```

    Keep all other parts of the map callback exactly the same.

    === Change 3: "Generate for other device" chip (line ~1545-1556) ===

    Current logic:
    ```typescript
    const otherDevice = selectedDevice === "pod_go" ? "helix_lt"
      : selectedDevice === "helix_stadium" ? "helix_lt"
      : selectedDevice === "helix_lt" ? "helix_floor"
      : "helix_lt"
    ```

    Update to handle all 6 devices:
    ```typescript
    const otherDevice = selectedDevice === "helix_lt" ? "helix_floor"
      : selectedDevice === "helix_floor" ? "helix_lt"
      : "helix_lt"
    // Everything else (pod_go, stadium, stomp, stomp_xl) → helix_lt
    ```

    Current label logic:
    ```typescript
    Generate for {selectedDevice === "pod_go" || selectedDevice === "helix_stadium" ? "Helix LT"
      : selectedDevice === "helix_lt" ? "Helix Floor" : "Helix LT"}
    ```

    Update:
    ```typescript
    Generate for {selectedDevice === "helix_lt" ? "Helix Floor"
      : selectedDevice === "helix_floor" ? "Helix LT"
      : "Helix LT"}
    ```

    After all changes, run the full build and test suite:
    npm run build && npx vitest run --reporter=dot
  </action>
  <verify>
    <automated>cd /c/Users/dsbog/HelixAI && npm run build 2>&1 | tail -10 && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Both device pickers show 6 devices in grid-cols-3 layout; STOMP and STOMP XL entries present with correct ids and labels; otherDevice chip maps Stomp variants to helix_lt; npm run build exits 0; all tests pass</done>
</task>

</tasks>

<verification>
npm run build — zero TypeScript errors
npx vitest run — all tests pass (no regressions)
selectedDevice type union: "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium" | "helix_stomp" | "helix_stomp_xl"
Rig picker: 6 buttons in grid-cols-3, includes STOMP and STOMP XL
Chat picker: 6 buttons in grid-cols-3, includes STOMP and STOMP XL
Badge: shows "STOMP" for helix_stomp, "STOMP XL" for helix_stomp_xl
Download: _Stomp.hlx and _StompXL.hlx (confirmed in downloadPreset code)
Chip: "Generate for Helix LT" when Stomp is selected
</verification>

<success_criteria>
STOMP-07: Both device picker arrays updated with STOMP and STOMP XL
STOMP-07: Download produces HelixAI_[Name]_Stomp.hlx and HelixAI_[Name]_StompXL.hlx
STOMP-07: Badge shows STOMP / STOMP XL for respective devices
STOMP-07: "Generate for other device" chip correctly handles all 6 devices
STOMP-07: Resuming a Stomp conversation uses correct _Stomp/_StompXL suffix
STOMP-10: No regressions — existing 4 devices still work correctly in UI
</success_criteria>

<output>
After completion, create .planning/phases/39-hx-stomp-support/39-03-SUMMARY.md with:
- All locations updated in page.tsx (with approximate line numbers)
- Build result
- Test count (should be unchanged from Plan 02 — page.tsx changes don't affect tests)
- Note: Manual HX Edit import verification required for STOMP-09 (hardware validation)
</output>
