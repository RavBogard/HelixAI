---
phase: 05-frontend-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/globals.css
  - src/lib/providers.ts
  - src/app/api/providers/route.ts
autonomous: false
requirements:
  - UX-02
  - UX-04

must_haves:
  truths:
    - "No dead provider code remains in the codebase"
    - "CSS has no orphaned classes that nothing references"
    - "The app runs, chat works, generation produces a downloadable preset, and the UI looks correct"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Clean CSS with no orphaned provider toggle classes"
    - path: "src/lib/providers.ts"
      provides: "DELETED — no longer exists"
    - path: "src/app/api/providers/route.ts"
      provides: "DELETED — no longer exists"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: "hlx-* class references"
      pattern: "hlx-"
---

<objective>
Delete dead multi-provider code and clean up orphaned CSS, then visually verify the full app

Purpose: Plan 01 removed all multi-provider references from page.tsx. The /api/providers route, providers.ts library, and provider-specific CSS classes are now dead code. This plan removes them and verifies the complete frontend works end-to-end.

Output: Clean codebase with no dead provider code. Visual confirmation that the app works.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-polish/05-CONTEXT.md
@.planning/phases/05-frontend-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead provider code and clean up orphaned CSS</name>
  <files>src/lib/providers.ts, src/app/api/providers/route.ts, src/app/globals.css</files>
  <action>
**Delete files entirely:**
- `src/lib/providers.ts` — No longer imported by anything. The generate route imports from `@/lib/planner` and `@/lib/helix`, not providers.
- `src/app/api/providers/route.ts` — The GET /api/providers endpoint is no longer called by any frontend code.

**Clean up globals.css:**
Remove the following CSS class blocks that are no longer referenced by any component:

1. `.hlx-provider-toggle` block (lines 501-515) — was used by provider selector buttons, now removed from page.tsx
2. `.hlx-provider-toggle:hover:not(:disabled)` block (lines 517-521)
3. `.hlx-provider-toggle:disabled` block (lines 523-526)
4. `.hlx-provider-active` block (lines 528-535)

Keep ALL other CSS classes — they are actively used by the refactored page.tsx.

**Verify no other files import from providers.ts:**
Before deleting, confirm that `grep -r "providers" src/ --include="*.ts" --include="*.tsx"` shows no remaining imports of `@/lib/providers` or `../providers` (the page.tsx references were removed in Plan 01, and the route.ts is being deleted now).
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>
    - src/lib/providers.ts does not exist
    - src/app/api/providers/route.ts does not exist
    - globals.css contains no `.hlx-provider-toggle` or `.hlx-provider-active` classes
    - TypeScript compiles with zero errors (no broken imports)
    - All actively-used hlx-* CSS classes remain intact
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete frontend</name>
  <files>n/a</files>
  <action>
Start the dev server (`npm run dev`) and present the full app for human verification. This checkpoint covers the complete Phase 5 frontend refactor across both plans.

The UI should show:
1. Welcome screen with "Helix Preset Builder" subtitle and tone suggestions
2. Chat interview flow with Gemini streaming
3. When [READY_TO_GENERATE] detected: device selector (Helix LT / Helix Floor) + Generate Preset button
4. After generation: single preset card with name, summary, and Download .hlx button
5. Footer: "Powered by Gemini . Claude . Line 6 Helix"
6. Warm Analog Studio aesthetic throughout (dark surface, amber accents, tube glow, rack lines)
  </action>
  <verify>
    Human follows the verification steps below:
    1. Run `npm run dev` and open http://localhost:3000
    2. Verify welcome screen: tube glow logo, gradient hero text "What tone are you after?", subtitle says "Helix preset" (not "Helix LT preset"), five suggestion buttons with LED dots
    3. Click a suggestion (e.g., "SRV Texas blues crunch") and send it
    4. Verify chat streams correctly with amber left-border on AI messages
    5. Continue chatting until [READY_TO_GENERATE] triggers the generate screen
    6. Verify device selector shows "Helix LT" (active/highlighted by default) and "Helix Floor" toggle
    7. Click "Generate Preset" button (should have tube glow animation)
    8. Verify single preset card appears with preset name, markdown summary, and Download .hlx button
    9. Click Download .hlx — verify file downloads with valid JSON content
    10. Verify footer says "Powered by Gemini . Claude . Line 6 Helix" (no GPT)
    11. Verify "New Session" button in header works (resets to welcome screen)
    12. Check page title in browser tab: "HelixAI — Helix Preset Builder"
  </verify>
  <done>
    - Human confirms welcome screen renders correctly with updated branding
    - Human confirms chat interview flow works end-to-end
    - Human confirms device selector appears and defaults to Helix LT
    - Human confirms single preset card displays after generation
    - Human confirms .hlx file downloads successfully
    - Human confirms Warm Analog Studio aesthetic is intact (no visual regressions)
    - Human confirms footer text is updated (no GPT reference)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `src/lib/providers.ts` does not exist on disk
- `src/app/api/providers/route.ts` does not exist on disk
- globals.css has no `hlx-provider` references
- Human has verified the full UI flow visually
</verification>

<success_criteria>
- All dead multi-provider code removed from codebase
- No orphaned CSS classes remain
- TypeScript compiles cleanly with no broken imports
- Human has verified: welcome screen, chat flow, device selector, preset generation, download, footer, aesthetic — all working correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-polish/05-02-SUMMARY.md`
</output>
