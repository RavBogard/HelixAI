---
phase: 40-rebrand-helixai-to-helixtones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/gemini.ts
  - src/lib/planner.ts
  - src/components/auth/AuthButton.tsx
  - src/components/sidebar/ChatSidebar.tsx
  - package.json
  - supabase/schema.sql
autonomous: true
requirements: [REBRAND-01, REBRAND-02, REBRAND-03, REBRAND-04]

must_haves:
  truths:
    - "Browser tab title reads 'HelixTones — Helix Preset Builder'"
    - "The AI introduces itself as 'HelixTones' in chat responses, not 'HelixAI'"
    - "Downloaded preset files are named 'HelixTones_Preset_*.hlx' (or .pgp/.hsp)"
    - "The header wordmark and logo alt text show 'HelixTones' (or 'helixtones')"
    - "Sign-in and new-chat cross-component event wiring still works after rename"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Page title metadata"
      contains: "HelixTones — Helix Preset Builder"
    - path: "src/app/page.tsx"
      provides: "UI display strings, download filenames, event listeners"
      contains: "HelixTones"
    - path: "src/lib/gemini.ts"
      provides: "AI chat system prompt persona"
      contains: "You are HelixTones"
    - path: "src/lib/planner.ts"
      provides: "AI planner system prompt persona"
      contains: "HelixTones' Planner"
    - path: "src/components/auth/AuthButton.tsx"
      provides: "Dispatches helixtones:before-signin event"
      contains: "helixtones:before-signin"
    - path: "src/components/sidebar/ChatSidebar.tsx"
      provides: "Listens for helixtones:conversation-created and dispatches helixtones:new-chat"
      contains: "helixtones:"
  key_links:
    - from: "src/components/auth/AuthButton.tsx"
      to: "src/app/page.tsx"
      via: "window.dispatchEvent(new Event('helixtones:before-signin'))"
      pattern: "helixtones:before-signin"
    - from: "src/app/page.tsx"
      to: "src/components/sidebar/ChatSidebar.tsx"
      via: "window.dispatchEvent(new Event('helixtones:conversation-created'))"
      pattern: "helixtones:conversation-created"
    - from: "src/components/sidebar/ChatSidebar.tsx"
      to: "src/app/page.tsx"
      via: "window.dispatchEvent(new Event('helixtones:new-chat'))"
      pattern: "helixtones:new-chat"
---

<objective>
Rename all user-visible "HelixAI" strings and internal cross-file DOM event/storage contracts to "HelixTones"/"helixtones" across 7 source files.

Purpose: Rebrand the product from "HelixAI" to "HelixTones" — every string the user sees in the browser or that the AI speaks must reflect the new name. Internal event names are renamed to stay consistent and avoid silent breakage.
Output: Build-clean codebase where zero user-visible "HelixAI" strings remain and all cross-file event/storage contracts use the "helixtones:" prefix.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-rebrand-helixai-to-helixtones/40-RESEARCH.md

<interfaces>
<!-- Key strings being changed. Executor uses these as find-and-replace anchors. -->
<!-- DO NOT rename: CSS --hlx-* variables, hlx-* class names, Supabase env vars, TypeScript identifiers -->

Category A — User-visible display strings (src/app/layout.tsx):
  Line 24: title: "HelixAI — Helix Preset Builder"
           → title: "HelixTones — Helix Preset Builder"

Category A — User-visible display strings (src/app/page.tsx):
  Line 1019: alt="HelixAI"           → alt="HelixTones"   (header logo Image)
  Line 1030: >helix ai<              → >helixtones<        (wordmark span, lowercase)
  Line 1087: alt="HelixAI"           → alt="HelixTones"   (hero logo Image)
  Line 760:  "HelixAI_Preset"        → "HelixTones_Preset" (downloadPreset() base name)
  Line 806:  `HelixAI_Preset${...}`  → `HelixTones_Preset${...}` (downloadStoredPreset())
  Line 1426: "HelixAI Preset"        → "HelixTones Preset" (fallback preset card name)

Category B — AI persona names:
  src/lib/gemini.ts line 34:
    "You are HelixAI, an expert guitar tone consultant..."
    → "You are HelixTones, an expert guitar tone consultant..."

  src/lib/planner.ts line 27:
    "You are HelixAI's Planner."
    → "You are HelixTones' Planner."

Category C — Cross-file event/storage contracts (ALL 4 files, atomic rename):
  Event: 'helixai:before-signin'       → 'helixtones:before-signin'
    - src/app/page.tsx lines 467, 468 (addEventListener + removeEventListener)
    - src/components/auth/AuthButton.tsx line 58 (dispatchEvent)

  Event: 'helixai:new-chat'            → 'helixtones:new-chat'
    - src/app/page.tsx lines 489, 490 (addEventListener + removeEventListener)
    - src/components/sidebar/ChatSidebar.tsx line 67 (dispatchEvent)

  Event: 'helixai:conversation-created' → 'helixtones:conversation-created'
    - src/app/page.tsx lines 632, 637, 724, 728 (all 4 dispatchEvent calls)
    - src/components/sidebar/ChatSidebar.tsx lines 43, 44 (addEventListener + removeEventListener)

  Storage key: 'helixai_pre_oauth_state' → 'helixtones_pre_oauth_state'
    - src/app/page.tsx line 390 (sessionStorage.getItem)
    - src/app/page.tsx line 400 (sessionStorage.removeItem)
    - src/app/page.tsx line 456 (sessionStorage.setItem)

Category D — Package name:
  package.json line 2: "name": "helix-ai" → "name": "helixtones"

Category E — Comment only (optional, include for consistency):
  supabase/schema.sql line 1: "-- HelixAI v2.0 Database Schema"
                             → "-- HelixTones v2.0 Database Schema"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename user-visible strings and AI persona prompts</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/lib/gemini.ts,
    src/lib/planner.ts,
    package.json,
    supabase/schema.sql
  </files>
  <action>
Apply the following string replacements. Make NO other changes — do not rename CSS classes, variable names, function names, or Supabase env vars.

**src/app/layout.tsx** (line 24):
  Before: `title: "HelixAI — Helix Preset Builder"`
  After:  `title: "HelixTones — Helix Preset Builder"`
  (The description line is already brand-neutral — leave it unchanged.)

**src/app/page.tsx** — 6 changes, all string literals:
  1. Line 1019 alt text (header logo):
     Before: `alt="HelixAI"`
     After:  `alt="HelixTones"`

  2. Line 1030 wordmark span text (lowercase, inside JSX):
     Before: `>helix ai<`  (the text content of the span, not a JS string)
     After:  `>helixtones<`

  3. Line 1087 alt text (hero logo):
     Before: `alt="HelixAI"`
     After:  `alt="HelixTones"`

  4. Line 760 in downloadPreset() — base name fallback:
     Before: `const baseName = generatedPreset.spec?.name || "HelixAI_Preset";`
     After:  `const baseName = generatedPreset.spec?.name || "HelixTones_Preset";`

  5. Line 806 in downloadStoredPreset() — filename string:
     Before: `const filename = \`HelixAI_Preset${deviceSuffix}${ext}\`;`
     After:  `const filename = \`HelixTones_Preset${deviceSuffix}${ext}\`;`

  6. Line 1426 in preset card JSX — fallback preset name:
     Before: `{generatedPreset.spec?.name || "HelixAI Preset"}`
     After:  `{generatedPreset.spec?.name || "HelixTones Preset"}`

**src/lib/gemini.ts** (line 34):
  Before: `return \`You are HelixAI, an expert guitar tone consultant...`
  After:  `return \`You are HelixTones, an expert guitar tone consultant...`
  (Change only the persona name at the start of the string. Leave the rest of the prompt unchanged.)

**src/lib/planner.ts** (line 27):
  Before: `return \`You are HelixAI's Planner. You choose creative model selections for ${deviceName} presets.`
  After:  `return \`You are HelixTones' Planner. You choose creative model selections for ${deviceName} presets.`
  (Note the possessive changes from "HelixAI's" to "HelixTones'" — correct English possessive for a word ending in 's'.)

**package.json** (line 2):
  Before: `"name": "helix-ai"`
  After:  `"name": "helixtones"`

**supabase/schema.sql** (line 1, optional consistency fix):
  Before: `-- HelixAI v2.0 Database Schema`
  After:  `-- HelixTones v2.0 Database Schema`

DO NOT touch:
- Any `--hlx-*` CSS variable or `hlx-*` class name
- Any TypeScript identifier (function names, variable names, import paths)
- Supabase env vars (`NEXT_PUBLIC_SUPABASE_URL`, etc.)
- The DOM event strings (those are Task 2)
- `package-lock.json` (do not edit manually — it has no runtime impact on deployed Next.js apps)
  </action>
  <verify>
    <automated>
      cd C:/Users/dsbog/HelixAI && grep -rn "HelixAI\|helix-ai\|helix ai" src/app/layout.tsx src/lib/gemini.ts src/lib/planner.ts package.json supabase/schema.sql 2>/dev/null; echo "exit:$?"
    </automated>
    Expected: zero lines matching "HelixAI", "helix-ai", or "helix ai" in these files. The grep should produce no output (or only irrelevant context lines). Then run: npm run build (in C:/Users/dsbog/HelixAI) and confirm it exits 0 with no type errors.
  </verify>
  <done>
    layout.tsx title is "HelixTones — Helix Preset Builder". Both alt texts in page.tsx say "HelixTones". Wordmark span says "helixtones". Both download filename strings use "HelixTones_Preset". Preset card fallback says "HelixTones Preset". gemini.ts system prompt says "You are HelixTones". planner.ts says "You are HelixTones' Planner". package.json name is "helixtones". npm run build exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename cross-file DOM event and sessionStorage contracts atomically</name>
  <files>
    src/app/page.tsx,
    src/components/auth/AuthButton.tsx,
    src/components/sidebar/ChatSidebar.tsx
  </files>
  <action>
Rename all `helixai:*` DOM custom event names and the `helixai_pre_oauth_state` sessionStorage key. ALL four occurrences across ALL three files must be changed in the same edit pass — a partial rename breaks auth and sidebar silently.

**src/app/page.tsx** — 10 string literal changes:

  sessionStorage key (3 occurrences):
    Line 390: `sessionStorage.getItem('helixai_pre_oauth_state')`
              → `sessionStorage.getItem('helixtones_pre_oauth_state')`
    Line 400: `sessionStorage.removeItem('helixai_pre_oauth_state')`
              → `sessionStorage.removeItem('helixtones_pre_oauth_state')`
    Line 456: `sessionStorage.setItem('helixai_pre_oauth_state', ...)`
              → `sessionStorage.setItem('helixtones_pre_oauth_state', ...)`

  helixai:before-signin (2 occurrences — addEventListener + removeEventListener):
    Line 467: `window.addEventListener('helixai:before-signin', handler)`
              → `window.addEventListener('helixtones:before-signin', handler)`
    Line 468: `return () => window.removeEventListener('helixai:before-signin', handler)`
              → `return () => window.removeEventListener('helixtones:before-signin', handler)`

  helixai:new-chat (2 occurrences — addEventListener + removeEventListener):
    Line 489: `window.addEventListener('helixai:new-chat', handler)`
              → `window.addEventListener('helixtones:new-chat', handler)`
    Line 490: `return () => window.removeEventListener('helixai:new-chat', handler)`
              → `return () => window.removeEventListener('helixtones:new-chat', handler)`

  helixai:conversation-created (4 occurrences — all dispatchEvent calls):
    Lines 632, 637, 724, 728:
      `window.dispatchEvent(new Event('helixai:conversation-created'))`
      → `window.dispatchEvent(new Event('helixtones:conversation-created'))`

**src/components/auth/AuthButton.tsx** — 1 string literal change:
  Line 58: `window.dispatchEvent(new Event('helixai:before-signin'))`
           → `window.dispatchEvent(new Event('helixtones:before-signin'))`

**src/components/sidebar/ChatSidebar.tsx** — 3 string literal changes:
  Line 43: `window.addEventListener('helixai:conversation-created', handler)`
           → `window.addEventListener('helixtones:conversation-created', handler)`
  Line 44: `return () => window.removeEventListener('helixai:conversation-created', handler)`
           → `return () => window.removeEventListener('helixtones:conversation-created', handler)`
  Line 67: `window.dispatchEvent(new Event('helixai:new-chat'))`
           → `window.dispatchEvent(new Event('helixtones:new-chat'))`

Also add the one-time sessionStorage migration for live users at the top of the existing useEffect in page.tsx that calls sessionStorage.getItem('helixtones_pre_oauth_state') (the one at line ~389, inside the init useEffect). Immediately before the getItem call, add:

```typescript
// One-time migration: carry forward any pre_oauth_state saved under the old key
const legacyOauthState = sessionStorage.getItem('helixai_pre_oauth_state')
if (legacyOauthState) {
  sessionStorage.setItem('helixtones_pre_oauth_state', legacyOauthState)
  sessionStorage.removeItem('helixai_pre_oauth_state')
}
```

This migration costs zero runtime overhead for new users and prevents chat-state loss for any user who had a tab open during the deploy.

DO NOT rename any TypeScript variable names (e.g. `serializeChatState`, `handleSignIn`, `fetchConversations`). Only the string literals inside quotes are changed.
  </action>
  <verify>
    <automated>
      cd C:/Users/dsbog/HelixAI && grep -rn "helixai:" src/app/page.tsx src/components/auth/AuthButton.tsx src/components/sidebar/ChatSidebar.tsx 2>/dev/null; grep -rn "helixai_pre_oauth_state" src/app/page.tsx 2>/dev/null; echo "exit:$?"
    </automated>
    Expected: zero matches for "helixai:" and "helixai_pre_oauth_state" in any of the three files. The grep produces no output. Confirm helixtones: variants are present: grep -rn "helixtones:" src/app/page.tsx src/components/auth/AuthButton.tsx src/components/sidebar/ChatSidebar.tsx should show 10 matches. Then run: npm run build — must exit 0.
  </verify>
  <done>
    Zero "helixai:" strings remain in any .tsx or .ts source file. "helixtones:" appears in all three cross-file locations. The sessionStorage key is "helixtones_pre_oauth_state" in all three usages. Legacy migration block is present in the init useEffect. npm run build exits 0 with no type errors.
  </done>
</task>

</tasks>

<verification>
Run the full zero-remaining-hits grep after both tasks complete:

```bash
cd C:/Users/dsbog/HelixAI
grep -rn "HelixAI\|Helix AI\|helix-ai\|helix_ai\|helixai:" \
  src/ \
  --include="*.ts" --include="*.tsx"
```

Expected: zero output. Any remaining line is a missed occurrence and must be fixed before closing the phase.

Then verify the build is clean:
```bash
cd C:/Users/dsbog/HelixAI && npm run build
```

Expected: exits 0, no TypeScript errors, no lint errors.

DO NOT search for `hlx` — CSS design tokens use the `hlx-` prefix and must NOT be renamed.
</verification>

<success_criteria>
1. Browser tab title shows "HelixTones — Helix Preset Builder"
2. AI chat responses say "I'm HelixTones" (not "HelixAI") when the AI introduces itself
3. Downloaded preset files are named "HelixTones_Preset_LT.hlx" (etc.) — confirmed by inspecting the download filename
4. Logo alt text is "HelixTones" in both the header and hero positions
5. Header wordmark span reads "helixtones"
6. Sign in with Google still works — the before-signin chat serialization fires correctly (manual test: start a chat, click Sign In, confirm chat state is preserved after OAuth redirect)
7. New Chat sidebar button still clears the chat (manual test: send a message, open sidebar, click New Chat, confirm chat clears)
8. Conversation sidebar still refreshes after first AI response (manual test: sign in, start a chat, send a message, confirm sidebar updates with conversation title)
9. `grep -rn "HelixAI\|helixai:" src/ --include="*.ts" --include="*.tsx"` returns zero lines
10. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/40-rebrand-helixai-to-helixtones/40-01-SUMMARY.md` with:
- What was renamed (all categories A–E)
- The final grep command output (should be empty)
- Build status (exit code)
- Any occurrences intentionally left unchanged and why (CSS tokens, env vars, git remote)
</output>
