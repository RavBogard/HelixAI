---
phase: 73-per-device-craft-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - src/lib/families/stomp/prompt.ts
  - src/lib/families/stomp/prompt.test.ts
  - src/lib/families/podgo/prompt.ts
  - src/lib/families/podgo/prompt.test.ts
  - src/lib/families/helix/prompt.ts
  - src/lib/families/helix/prompt.test.ts
autonomous: true
requirements:
  - CRAFT-01
  - CRAFT-02
  - CRAFT-03
  - CRAFT-04

must_haves:
  truths:
    - "Stomp planner prompt encourages 3-4 effects for most genres, not 1-2"
    - "Stomp planner prompt says 'aim for 3-4 effects' instead of 'Maximum 2 effects' for metal"
    - "Pod Go planner prompt specifies exact 4-effect templates per genre"
    - "Pod Go planner prompt says 'fill all 4 slots' instead of '2 effects maximum' for metal"
    - "Helix planner prompt says '4-6 effects typical' instead of '2-4 is typical'"
    - "Helix planner prompt encourages dual-DSP advantage for richer chains"
    - "All existing prompt tests pass (cache identity, cross-family isolation, section presence)"
  artifacts:
    - path: "src/lib/families/stomp/prompt.ts"
      provides: "CRAFT-01 optimized Stomp effect discipline with higher minimum counts"
      contains: "3-4 effects"
    - path: "src/lib/families/podgo/prompt.ts"
      provides: "CRAFT-02 Pod Go exact 4-effect templates per genre"
      contains: "fill all 4 slots"
    - path: "src/lib/families/helix/prompt.ts"
      provides: "CRAFT-03 Helix dual-DSP encouragement with 4-6 typical effects"
      contains: "4-6"
    - path: "src/lib/families/stomp/prompt.test.ts"
      provides: "CRAFT-01 prompt content assertions"
      contains: "CRAFT-01"
    - path: "src/lib/families/podgo/prompt.test.ts"
      provides: "CRAFT-02 prompt content assertions"
      contains: "CRAFT-02"
    - path: "src/lib/families/helix/prompt.test.ts"
      provides: "CRAFT-03 prompt content assertions"
      contains: "CRAFT-03"
  key_links:
    - from: "src/lib/families/stomp/prompt.ts"
      to: "planner AI behavior"
      via: "Effect Discipline by Genre section"
      pattern: "3-4 effects"
    - from: "src/lib/families/helix/prompt.ts"
      to: "planner AI behavior"
      via: "Creative Guidelines + Effect Discipline sections"
      pattern: "4-6"
---

<objective>
Update all three device family planner prompts with device-specific craft guidance: Stomp encourages 3-4 effects (not 1-2), Pod Go specifies exact 4-effect templates per genre, Helix encourages 4-6 effects leveraging dual DSP. Add prompt tests asserting the new content.

Purpose: CRAFT-01 (Stomp tonal variety), CRAFT-02 (Pod Go genre-intelligent prompt side), CRAFT-03 (Helix dual-DSP advantage), CRAFT-04 (craft in prompts, complementing the code changes from Plan 01).

Output: Updated prompt.ts files for stomp, podgo, and helix families. Updated prompt.test.ts files with CRAFT-specific assertions.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-per-device-craft-optimization/73-RESEARCH.md
@.planning/phases/73-per-device-craft-optimization/73-01-SUMMARY.md

@src/lib/families/stomp/prompt.ts
@src/lib/families/stomp/prompt.test.ts
@src/lib/families/podgo/prompt.ts
@src/lib/families/podgo/prompt.test.ts
@src/lib/families/helix/prompt.ts
@src/lib/families/helix/prompt.test.ts
</context>

<interfaces>
<!-- Current prompt structure the executor must preserve. -->

From stomp/prompt.ts:
- `buildPlannerPrompt(_device: DeviceTarget, modelList: string): string` — MUST remain cache-identical for helix_stomp and helix_stomp_xl
- Uses `STOMP_CONFIG.STOMP_MAX_BLOCKS` (8) and `STOMP_CONFIG.STOMP_MAX_SNAPSHOTS` (3)
- `maxEffects = 4` local var passed to `toneIntentFieldsSection()`
- Existing sections to modify: "## Effect Discipline by Genre" (lines 108-128) and "## Genre-Based Priority When Over Budget" (lines 121-128)

From podgo/prompt.ts:
- `buildPlannerPrompt(device: DeviceTarget, modelList: string): string`
- Existing sections to modify: "## Effect Discipline by Genre" (lines 88-107)
- DEVICE RESTRICTION block at line 109 — preserve this

From helix/prompt.ts:
- `buildPlannerPrompt(device: DeviceTarget, modelList: string): string` — MUST remain cache-identical for helix_lt and helix_floor
- Existing sections to modify: "## Creative Guidelines" (lines 87-95) — change "2-4 is typical" to "4-6 is typical"
- Existing sections to modify: "## Effect Discipline by Genre" (lines 103-117) — raise minimum effect counts

From stomp/prompt.test.ts — existing tests that MUST still pass:
- `helix_stomp and helix_stomp_xl produce identical planner prompt text`
- `contains priority hierarchy language for genre-based over-budget`
- `unified prompt references STOMP_MAX_BLOCKS (8) block slots`
- `Stomp prompt maxEffects (4) does not exceed Stomp maxEffectsPerDsp (4)`

From podgo/prompt.test.ts — existing tests that MUST still pass:
- `specifies maxEffects of 4 in ToneIntent fields`
- `hard-enforces 4-effect limit in device restriction`
- `contains "no exceptions"`

From helix/prompt.test.ts — existing tests that MUST still pass:
- `helix_lt and helix_floor return identical prompt text`
- `contains effect discipline by genre section`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Stomp prompt with CRAFT-01 effect discipline</name>
  <files>src/lib/families/stomp/prompt.ts, src/lib/families/stomp/prompt.test.ts</files>
  <action>
  Replace the "## Effect Discipline by Genre" section in stomp/prompt.ts (lines 108-128) with updated guidance that encourages using all available effect slots. The key changes:

  **Section heading** — keep as: `## Effect Discipline by Genre (HX Stomp family — ${maxBlocks} block slots)`

  **Intro paragraph** — change to explain that after amp + cab + always-on boost = 3 mandatory blocks, up to 4 remaining slots are available for user effects. Encourage using them: "Use them — every slot should earn its place, but do NOT leave slots empty when the genre benefits from more effects."

  **Per-genre guidance** (replace current counts):
  - **Metal / hard rock**: "3-4 effects" (was "Maximum 2 effects"). Drive is mandatory; add delay (low mix), optional gate or wah. Priority: drive > delay > gate > wah
  - **Blues / classic rock / country**: "3-4 effects" (was "2-3 effects"). Delay AND reverb are both standard; add drive or compressor. Priority: delay > reverb > drive > compressor
  - **Jazz / fusion**: "2-3 effects" (was "1 effect maximum"). Reverb is essential; add compressor and optional chorus.
  - **Ambient / worship**: "4 effects (use all available slots)" (was "3-4 effects"). MUST include reverb AND delay. Add modulation and a second time-based effect. Priority: reverb > delay > mod > second delay/reverb
  - **Pop / funk**: "3-4 effects" (was "2 effects"). Chorus or phaser plus delay and reverb.

  **Add closing emphasis**: "IMPORTANT: A Stomp preset with only 1-2 effects is underusing the device. Aim for 3-4 effects for most genres, 4 for ambient/worship."

  **Keep the "## Genre-Based Priority When Over Budget" section** — only update metal to match: `drive > delay > gate` (was `drive > delay > mod`).

  **Do NOT change**: maxEffects variable (stays 4), cache identity (Stomp/Stomp XL identical), toneIntentFieldsSection call, any other section of the prompt.

  **Tests to add** in stomp/prompt.test.ts (new describe block "CRAFT-01: effect count optimization"):
  - `Stomp prompt encourages 3-4 effects for metal (not Maximum 2)`: assert prompt contains "3-4 effects" in metal line AND does NOT contain "Maximum 2 effects"
  - `Stomp prompt encourages 4 effects for ambient/worship`: assert prompt contains "4 effects" near "ambient" context
  - `Stomp prompt contains underuse warning`: assert prompt contains "underusing the device"
  </action>
  <verify>
    <automated>npx vitest run src/lib/families/stomp/prompt.test.ts</automated>
  </verify>
  <done>Stomp planner prompt encourages 3-4 effects for most genres (was 1-2), 4 for ambient. All existing prompt tests still pass. 3 new CRAFT-01 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Pod Go and Helix prompts with CRAFT-02/03 guidance</name>
  <files>src/lib/families/podgo/prompt.ts, src/lib/families/podgo/prompt.test.ts, src/lib/families/helix/prompt.ts, src/lib/families/helix/prompt.test.ts</files>
  <action>
  **Pod Go prompt (CRAFT-02):**

  Replace the "## Effect Discipline by Genre" section in podgo/prompt.ts (lines 88-107) with exact 4-effect templates. The key principle: Pod Go has exactly 4 user-effect slots — fill all 4 for every genre.

  **Section heading**: `## Effect Slot Planning by Genre (Pod Go — exactly 4 effect slots)`

  **Intro**: "Pod Go has exactly 4 user-effect slots. Choose ALL 4 for every genre — do not leave slots unused. Here are the ideal 4 effects per genre:"

  **Per-genre exact templates** (replace current ranges):
  - **Metal / hard rock**: drive + gate + delay (low mix) + [compressor OR wah]. Priority: drive > gate > delay
  - **Blues / classic rock**: drive + delay + reverb + [compressor OR tremolo]. Priority: delay > reverb > drive
  - **Country**: compressor + delay + reverb + [tremolo OR chorus]. Priority: delay > reverb > compressor
  - **Jazz / fusion**: compressor + reverb + [chorus OR EQ] + [delay OR second reverb]. Priority: reverb > compressor > modulation
  - **Ambient / worship**: delay + reverb + modulation + [second delay OR shimmer reverb]. Priority: reverb > delay > mod
  - **Pop / funk**: compressor + chorus/phaser + delay + reverb. Priority: delay > reverb > modulation > compressor
  - **Psychedelic**: wah + delay + reverb + modulation

  **Closing emphasis**: "When in doubt, fill all 4 slots. An unused slot is a wasted slot on Pod Go."

  **Keep the "## Genre-Based Slot Priority When Over Budget" section unchanged** — it still applies.
  **Keep the "DEVICE RESTRICTION" block at the end unchanged.**

  **Do NOT change**: toneIntentFieldsSection({ maxEffects: 4 ... }) call, "no exceptions" language, DEVICE RESTRICTION text.

  **Pod Go tests to add** in podgo/prompt.test.ts (new describe block "CRAFT-02: genre-intelligent slot planning"):
  - `Pod Go prompt specifies exact 4-effect templates per genre`: assert prompt contains "Choose ALL 4" and "exactly 4 effect slots"
  - `Pod Go prompt encourages filling all slots`: assert prompt contains "unused slot is a wasted slot"
  - `Pod Go prompt still maintains hard 4-effect limit language`: assert prompt still contains "hard 4 user-effect limit" (in DEVICE RESTRICTION)

  ---

  **Helix prompt (CRAFT-03):**

  In helix/prompt.ts, modify two sections:

  1. **"## Creative Guidelines" (line 91)**: Change `"Keep effects minimal: 2-4 is typical, 8 is the maximum"` to `"Leverage both DSPs: 4-6 effects is typical for Helix, 8 is the maximum"`. Keep the line about not adding effects for sake of filling slots.

  2. **"## Effect Discipline by Genre" section (lines 103-117)**: Raise all effect counts and add dual-DSP encouragement.
     - **Metal / hard rock**: "3-4 effects" (was "Maximum 3 effects"). Drive + gate on DSP0; delay (low mix) + optional modulation on DSP1. Mention dual DSP allows post-effects without compromising pre-amp chain.
     - **Blues / classic rock / country**: "4-5 effects" (was "2-3 effects"). Drive + compressor on DSP0; delay + reverb + optional tremolo/chorus on DSP1.
     - **Jazz / fusion**: "2-3 effects" (was "1-2 effects maximum"). Compressor on DSP0; reverb + optional chorus on DSP1.
     - **Ambient / worship**: "5-7 effects" (was "4-6 effects expected"). Drive or compressor on DSP0; modulation + delay + reverb + second delay or shimmer reverb on DSP1. Mention using extra DSP1 headroom for layered time-based effects.
     - **Pop / funk**: "3-5 effects" (was "2-3 effects"). Compressor + drive on DSP0; chorus/phaser + delay + reverb on DSP1.

  3. **Add a closing line to Effect Discipline section**: "IMPORTANT: Helix presets should typically have MORE effects than Stomp or Pod Go for the same genre. Use the dual-DSP advantage."

  **Do NOT change**: Dual-DSP Routing section, buildPlannerPrompt signature, cache identity (helix_lt = helix_floor), any other section.

  **Helix tests to add** in helix/prompt.test.ts (new describe block "CRAFT-03: dual-DSP richness"):
  - `Helix prompt encourages 4-6 effects typical (not 2-4)`: assert prompt contains "4-6" and does NOT contain the exact string "2-4 is typical"
  - `Helix prompt encourages dual-DSP advantage`: assert prompt contains "dual-DSP advantage" or "Leverage both DSPs"
  - `Helix prompt says presets should have more effects than constrained devices`: assert prompt contains "MORE effects than Stomp or Pod Go"
  </action>
  <verify>
    <automated>npx vitest run src/lib/families/podgo/prompt.test.ts src/lib/families/helix/prompt.test.ts</automated>
  </verify>
  <done>Pod Go prompt specifies exact 4-effect templates per genre with "fill all 4 slots" guidance. Helix prompt encourages 4-6 effects typical (not 2-4) with explicit dual-DSP richness language. All existing prompt tests pass. 6 new CRAFT-02/03 tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Run all prompt tests
npx vitest run src/lib/families/stomp/prompt.test.ts src/lib/families/podgo/prompt.test.ts src/lib/families/helix/prompt.test.ts

# Verify cache identity: Stomp = Stomp XL, Helix LT = Helix Floor
npx vitest run src/lib/families/stomp/prompt.test.ts -t "identical"
npx vitest run src/lib/families/helix/prompt.test.ts -t "identical"

# Full suite regression check
npx vitest run
```
</verification>

<success_criteria>
1. Stomp prompt says "3-4 effects" for metal (not "Maximum 2"), "4 effects" for ambient — CRAFT-01
2. Pod Go prompt says "Choose ALL 4" with exact per-genre templates — CRAFT-02
3. Helix prompt says "4-6 effects is typical" (not "2-4"), mentions dual-DSP advantage — CRAFT-03
4. All three prompts maintain cache identity (Stomp/XL identical, Helix LT/Floor identical)
5. No existing prompt tests broken
6. 9 new CRAFT tests pass (3 Stomp + 3 Pod Go + 3 Helix)
7. Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/73-per-device-craft-optimization/73-02-SUMMARY.md`
</output>
