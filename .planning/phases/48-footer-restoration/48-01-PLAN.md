---
phase: 48-footer-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [FOOTER-01]
---

<objective>
Extract the inline footer from page.tsx's welcome screen into a standalone Footer component, fixed to the viewport bottom, visible on all screens (welcome, chat, post-generation). Style: 11px mono, amber hover, consistent with existing design system.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer component and integrate into page</name>
  <files>src/components/Footer.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    1. **Create `src/components/Footer.tsx`** — A client component:
       - Fixed to viewport bottom: `fixed bottom-0 left-0 right-0 z-40`
       - Centered text: "A project of [Daniel Bogard](https://danielbogard.com) · Powered by Gemini · Claude · Line 6 Helix"
       - 11px JetBrains Mono (use `var(--font-mono)`)
       - Text color: `var(--hlx-text-muted)`, link color: `var(--hlx-text-sub)`, hover: `var(--hlx-amber)`
       - Subtle background: transparent or very subtle gradient from transparent to `var(--hlx-void)` for readability over content
       - Padding: small vertical padding (py-2 or py-3)
       - Export a `FOOTER_HEIGHT` constant (e.g., `"2rem"`) so page.tsx can add bottom padding to prevent content from hiding behind footer
       - Add an optional `onSupportClick` callback prop (for Phase 50 — the Support link). If provided, render a "Support" text button before the credit line. If not provided, don't render it. This keeps the component ready for Phase 50.

    2. **Update `src/app/page.tsx`**:
       - Import and render `<Footer />` at the end of the component, OUTSIDE the welcome/chat conditional — it should always be visible
       - Remove the existing inline footer (lines 1661-1672)
       - Add bottom padding to the main content container so the last content doesn't hide behind the fixed footer (use `pb-10` or similar)

    3. **Add `.hlx-footer` class to `src/app/globals.css`** (if needed for the subtle bottom gradient):
       ```css
       .hlx-footer {
         background: linear-gradient(transparent, var(--hlx-void) 40%);
         pointer-events: none; /* let background be click-through */
       }
       .hlx-footer > * {
         pointer-events: auto; /* but links are clickable */
       }
       ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Footer is a separate component, fixed to viewport bottom, visible on welcome screen, during chat, and after generation. Daniel Bogard link opens in new tab. Matches existing design system.</done>
</task>

</tasks>

<verification>
1. `npm run build` — build succeeds
2. Visual: footer visible at viewport bottom on welcome screen
3. Visual: footer stays at bottom during long chat conversations
4. Visual: footer visible after preset generation
5. Daniel Bogard link opens DanielBogard.com in new tab
</verification>

<success_criteria>
- Footer component is fixed to viewport bottom on all screens
- "Daniel Bogard" links to DanielBogard.com in new tab
- 11px mono styling with amber hover
- No content hidden behind footer (bottom padding applied)
</success_criteria>

<output>
After completion, create `.planning/phases/48-footer-restoration/48-01-SUMMARY.md`
</output>
