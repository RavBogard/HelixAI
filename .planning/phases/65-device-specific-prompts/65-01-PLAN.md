---
phase: 65-device-specific-prompts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/families/shared/gain-staging.ts
  - src/lib/families/shared/tone-intent-fields.ts
  - src/lib/families/shared/amp-cab-pairing.ts
  - src/lib/families/helix/prompt.ts
  - src/lib/families/stomp/prompt.ts
  - src/lib/families/podgo/prompt.ts
  - src/lib/families/stadium/prompt.ts
  - src/lib/prompt-router.ts
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06

must_haves:
  truths:
    - "The Helix family planner prompt contains explicit dual-DSP routing instructions (DSP0 → DSP1, split block positioning, join block) for every preset — the #1 priority fix"
    - "The Stomp chat prompt uses dream-then-trim flow and surfaces explicit trade-off questions when user tone exceeds the 6-block budget"
    - "The Pod Go chat prompt surfaces the 4-effect limit with empowering framing and hard-enforces it in the planner prompt"
    - "The Stadium chat prompt uses arena-grade personality with FOH/live-sound vocabulary and Agoura-native model names"
    - "The Helix chat prompt does NOT proactively suggest dual-amp — only offers it when user explicitly asks"
    - "Each per-family prompt file contains zero cross-family model names (grep for Agoura_ in non-Stadium files returns 0; grep for Placater/Derailed in Stadium file returns 0)"
    - "A prompt-router.ts dispatches to the correct family module based on DeviceTarget → DeviceFamily resolution"
  artifacts:
    - path: "src/lib/families/shared/gain-staging.ts"
      provides: "Shared gain-staging prompt section"
      exports: ["gainStagingSection"]
    - path: "src/lib/families/shared/tone-intent-fields.ts"
      provides: "Shared ToneIntent field description prompt section"
      exports: ["toneIntentFieldsSection"]
    - path: "src/lib/families/shared/amp-cab-pairing.ts"
      provides: "Shared amp-to-cab pairing table builder"
      exports: ["ampCabPairingSection"]
    - path: "src/lib/families/helix/prompt.ts"
      provides: "Helix family planner + chat prompts"
      exports: ["buildPlannerPrompt", "getSystemPrompt"]
    - path: "src/lib/families/stomp/prompt.ts"
      provides: "Stomp family planner + chat prompts"
      exports: ["buildPlannerPrompt", "getSystemPrompt"]
    - path: "src/lib/families/podgo/prompt.ts"
      provides: "Pod Go family planner + chat prompts"
      exports: ["buildPlannerPrompt", "getSystemPrompt"]
    - path: "src/lib/families/stadium/prompt.ts"
      provides: "Stadium family planner + chat prompts"
      exports: ["buildPlannerPrompt", "getSystemPrompt"]
    - path: "src/lib/prompt-router.ts"
      provides: "Central dispatcher: DeviceTarget → family prompt module"
      exports: ["getFamilyPlannerPrompt", "getFamilyChatPrompt"]
  key_links:
    - from: "src/lib/prompt-router.ts"
      to: "src/lib/families/{family}/prompt.ts"
      via: "exhaustive switch on resolveFamily(device)"
      pattern: "case .helix.*helixPlannerPrompt|case .stomp.*stompPlannerPrompt"
    - from: "src/lib/families/{family}/prompt.ts"
      to: "src/lib/families/shared/*.ts"
      via: "import and compose shared sections"
      pattern: "import.*from.*shared/(gain-staging|tone-intent-fields|amp-cab-pairing)"
    - from: "src/lib/families/helix/prompt.ts"
      to: "src/lib/helix/config.ts"
      via: "import STOMP_CONFIG for snapshot/block limit constants"
      pattern: "This link does NOT exist — Helix prompt uses no Stomp constants"
---

<objective>
Create per-family prompt modules (helix, stomp, podgo, stadium) with shared composable sections and a central prompt router. Each family gets its own `buildPlannerPrompt()` and `getSystemPrompt()` with device-appropriate conversation arcs, constraint surfacing, and model vocabulary.

Purpose: Eliminate the monolithic `buildPlannerPrompt()` and `getSystemPrompt()` functions by splitting them into isolated per-family modules. This makes cross-family model name leakage structurally impossible and gives each device family a prompt tuned to its constraints and demographic.

Output: 8 new TypeScript files — 3 shared sections, 4 per-family prompt modules, 1 prompt router.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/65-device-specific-prompts/65-CONTEXT.md
@.planning/phases/65-device-specific-prompts/65-RESEARCH.md
@src/lib/planner.ts
@src/lib/gemini.ts
@src/lib/helix/types.ts
@src/lib/helix/config.ts
@src/lib/helix/models.ts
@src/lib/helix/index.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/helix/types.ts:
```typescript
export type DeviceTarget = "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium" | "helix_stomp" | "helix_stomp_xl";

export function isHelix(device: DeviceTarget): boolean;
export function isPodGo(device: DeviceTarget): boolean;
export function isStadium(device: DeviceTarget): boolean;
export function isStomp(device: DeviceTarget): boolean;
```

From src/lib/helix/config.ts:
```typescript
export const STOMP_CONFIG = {
  STOMP_MAX_BLOCKS: 6,
  STOMP_XL_MAX_BLOCKS: 9,
  STOMP_MAX_SNAPSHOTS: 3,
  STOMP_XL_MAX_SNAPSHOTS: 4,
} as const;
```

From src/lib/planner.ts (existing monolithic function — source of prompt content to extract):
```typescript
export function buildPlannerPrompt(modelList: string, device?: DeviceTarget): string;
export async function callClaudePlanner(messages: ..., device: DeviceTarget, toneContext?: string): Promise<ToneIntent>;
```

From src/lib/gemini.ts (existing monolithic function — source of chat prompt content to extract):
```typescript
export function getSystemPrompt(): string;  // No device parameter — must be replaced
```

From src/lib/helix/index.ts:
```typescript
export function getModelListForPrompt(device: DeviceTarget): string;
```

NOTE: Phase 61 (not yet implemented) will provide `resolveFamily()`. For now, implement the prompt router with an inline resolveFamily implementation that maps DeviceTarget to DeviceFamily ("helix" | "stomp" | "podgo" | "stadium"). This will be replaced by Phase 61's canonical implementation later. The mapping is:
- helix_lt, helix_floor → "helix"
- helix_stomp, helix_stomp_xl → "stomp"
- pod_go → "podgo"
- helix_stadium → "stadium"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared composable prompt sections</name>
  <files>src/lib/families/shared/gain-staging.ts, src/lib/families/shared/tone-intent-fields.ts, src/lib/families/shared/amp-cab-pairing.ts</files>
  <action>
Create directory `src/lib/families/shared/` and three composable prompt section modules. Each exports a pure function returning a prompt text string.

**gain-staging.ts:**
Extract the gain-staging intelligence section from the existing `buildPlannerPrompt()` in `planner.ts`. This is the section about volume staging, amp gain interaction with drive pedals, and how different gain stages compound. Export as `gainStagingSection(): string`. Takes no parameters — same text for all families.

**tone-intent-fields.ts:**
Extract the ToneIntent field description section from existing `buildPlannerPrompt()`. Export as `toneIntentFieldsSection(opts: ToneIntentFieldsOptions): string` where the interface is:
```typescript
export interface ToneIntentFieldsOptions {
  maxEffects: number;      // 4 for PodGo, 6 for Stomp/Helix, variable
  snapshots: number;       // 3/4/8 per device
  includeSecondAmp: boolean; // true for Helix, false for others
  maxSnapNameLength?: number; // default 10
}
```
The function returns the ToneIntent field descriptions with device-varying values interpolated. Include the secondAmpName/secondCabName fields only when `includeSecondAmp` is true.

**amp-cab-pairing.ts:**
Create a generic amp-to-cab pairing table builder. Export as `ampCabPairingSection(pairings: AmpCabPairing[]): string` where:
```typescript
export interface AmpCabPairing {
  ampFamily: string;    // e.g., "Fender", "Marshall", "Agoura"
  amps: string[];       // amp model names in this family
  recommendedCabs: string[]; // cab names that pair well
}
```
Each family's prompt will call this with their own pairings data. The function formats them into a readable table/list for the prompt.

**Key rules:**
- All three modules are pure functions returning strings — no side effects, no imports from device-specific modules
- Use TypeScript template literals (existing project pattern)
- Export named functions (not default exports)
- Do NOT include device-specific model names in these shared files — they receive all varying content via parameters
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/families/shared/gain-staging.ts src/lib/families/shared/tone-intent-fields.ts src/lib/families/shared/amp-cab-pairing.ts 2>&1 | head -20</automated>
  </verify>
  <done>Three shared prompt section modules exist, export their named functions, compile without errors, and contain no device-specific model names (no "Placater", "Agoura_", "Derailed", etc.)</done>
</task>

<task type="auto">
  <name>Task 2: Create per-family prompt modules and prompt router</name>
  <files>src/lib/families/helix/prompt.ts, src/lib/families/stomp/prompt.ts, src/lib/families/podgo/prompt.ts, src/lib/families/stadium/prompt.ts, src/lib/prompt-router.ts</files>
  <action>
Create directories `src/lib/families/{helix,stomp,podgo,stadium}/` and one prompt.ts per family. Then create the central `src/lib/prompt-router.ts`.

**Each family's prompt.ts exports:**
```typescript
export function buildPlannerPrompt(device: DeviceTarget, modelList: string): string;
export function getSystemPrompt(device: DeviceTarget): string;
```

**Extract and split content from the existing monolithic functions:**
- Read the existing `buildPlannerPrompt()` in `src/lib/planner.ts` — this is the source of ALL planner prompt content
- Read the existing `getSystemPrompt()` in `src/lib/gemini.ts` — this is the source of ALL chat prompt content
- Split the branching logic (isPodGo, isStadium, isStomp, isHelix) into per-family files instead of inline guards

**Per-family planner prompt content (PROMPT-01):**

All families share:
- Import and compose `gainStagingSection()`, `toneIntentFieldsSection()`, `ampCabPairingSection()` from shared modules
- Role description: "You are HelixTones' Planner..."
- Valid Model Names section: `${modelList}` (injected at runtime — NEVER hardcode model names)
- ToneIntent fields section (from shared, with family-specific options)
- Gain-staging section (from shared)

**Helix family (PROMPT-06 — #1 PRIORITY):**
- `buildPlannerPrompt()`: Include explicit dual-DSP routing section (CRITICAL — this is the top priority fix per CONTEXT.md). Use numbered step-by-step block ordering:
  1. Standard single-amp: input → [pre-effects: comp, drive on DSP0/Path 1] → amp → cab → split block (routes to DSP1) → [post-effects: mod, delay, reverb on DSP1/Path 2] → join → output
  2. Dual-amp (ONLY when user explicitly requests two amps): input → [pre-effects max 2] → amp1+cab1 on Path 1A, amp2+cab2 on Path 1B via split, join merges → DSP1 post-effects → output
  3. Rules: pre-effects on DSP0, post-effects on DSP1, balance DSP load if needed (flexible guideline, not hard rule)
  4. Do NOT suggest dual-amp proactively — only use when user explicitly requests
- `buildPlannerPrompt()`: `toneIntentFieldsSection({ maxEffects: 6, snapshots: 8, includeSecondAmp: true })`
- `buildPlannerPrompt()`: HD2 amp-to-cab pairings via `ampCabPairingSection()` (extract from existing `cabAffinityByFamily` logic in `planner.ts`)
- `buildPlannerPrompt()`: Effect discipline by genre (existing content from planner.ts)
- `getSystemPrompt()`: Expert studio/gigging tone builder personality. Dream-then-trim flow. Does NOT proactively suggest dual-amp. Uses device-native HD2 model names alongside real-world references (e.g., "the Placater Dirty (Friedman BE-100 style)"). IMPORTANT: Preserve existing device instruction from gemini.ts: "The device is already selected via the UI. Do NOT ask the user which device they are using."
- Use IDENTICAL system prompt text for helix_lt and helix_floor (they are architecturally identical). Device name can appear in user message (which varies). This gives a single warm cache entry.

**Stomp family (PROMPT-03):**
- `buildPlannerPrompt()`: Import `STOMP_CONFIG` from `@/lib/helix/config` — use `STOMP_MAX_BLOCKS` / `STOMP_XL_MAX_BLOCKS` and `STOMP_MAX_SNAPSHOTS` / `STOMP_XL_MAX_SNAPSHOTS` based on exact `device` parameter. Do NOT hardcode numbers.
- `buildPlannerPrompt()`: `toneIntentFieldsSection({ maxEffects: device === "helix_stomp_xl" ? 6 : 4, snapshots: device === "helix_stomp_xl" ? 4 : 3, includeSecondAmp: false })`
- `buildPlannerPrompt()`: Genre-adjusted effect limits — Stomp metal gets "max 2 effects" (not 3) because of 6-slot limit; effect discipline tighter than Helix
- `buildPlannerPrompt()`: Genre-based priority hierarchy for over-budget: Metal = drive > delay > mod; Ambient = reverb > delay > mod > drive
- `getSystemPrompt()`: Dream-then-trim flow. Budget-conscious personality ("make 6 slots count"). Surfaces explicit trade-off questions when user tone exceeds slot budget: "That's 8 blocks but HX Stomp allows 6. Which matters more: the boost or the chorus?" Feels like a knowledgeable friend helping prioritize, not a system rejecting requests.

**Pod Go family (PROMPT-04):**
- `buildPlannerPrompt()`: Hard 4-effect limit — `toneIntentFieldsSection({ maxEffects: 4, snapshots: 4, includeSecondAmp: false })`
- `buildPlannerPrompt()`: Genre-adjusted: PodGo ambient = "delay + reverb mandatory, but only 2 remaining slots"
- `buildPlannerPrompt()`: No "stretch" configurations — 4 is the absolute max
- `getSystemPrompt()`: Upfront transparency about constraints. Empowering framing: "Pod Go gives you 4 effect slots — let's make every one count." Not limiting, empowering. Slot priority surfaced as positive framing.

**Stadium family (PROMPT-05):**
- `buildPlannerPrompt()`: `toneIntentFieldsSection({ maxEffects: 6, snapshots: 8, includeSecondAmp: false })` — Stadium currently single-path
- `buildPlannerPrompt()`: Agoura-native vocabulary — model list comes from `modelList` parameter (runtime injected, NEVER hardcoded). Add `// TODO(Phase62): populate Agoura amp→cab table` placeholder for the cab pairing table.
- `buildPlannerPrompt()`: Stadium-specific features: 7-band Parametric EQ, dual-DSP routing potential, expanded effect set
- `getSystemPrompt()`: Arena-grade personality. FOH (Front of House) mixing, stage volume, monitor mix, live sound engineering vocabulary. Different demographic (pro touring) from home/gigging Helix user. References Agoura model names using Agoura_ prefix format.

**prompt-router.ts:**
Implement the central dispatcher. Since Phase 61 (`resolveFamily()`) is not yet shipped, include a local `resolveFamily()` implementation inline with the mapping:
- helix_lt, helix_floor → "helix"
- helix_stomp, helix_stomp_xl → "stomp"
- pod_go → "podgo"
- helix_stadium → "stadium"

Export:
```typescript
export type DeviceFamily = "helix" | "stomp" | "podgo" | "stadium";
export function getFamilyPlannerPrompt(device: DeviceTarget, modelList: string): string;
export function getFamilyChatPrompt(device: DeviceTarget): string;
```

Both functions use an exhaustive switch on `resolveFamily(device)` dispatching to the family's imported functions. TypeScript compiler must reject any code that doesn't cover all four families. Add a `// TODO(Phase61): replace with canonical resolveFamily() from @/lib/helix/family-router` comment.

**Cache-safe prompt structure (CRITICAL):**
In each family's `buildPlannerPrompt()`, structure the prompt so static sections (role, gain-staging, ToneIntent fields) appear FIRST, and device-restriction text goes at the END. This preserves the existing Anthropic cache pattern where the system prompt prefix stays stable and only the suffix varies per-device within a family.

**Cross-family isolation (SUCCESS CRITERION):**
- Helix prompt.ts must NOT contain any Agoura_ amp names
- Stomp prompt.ts must NOT contain any Agoura_ amp names
- PodGo prompt.ts must NOT contain any Agoura_ amp names
- Stadium prompt.ts must NOT contain any HD2 amp names (Placater, Derailed, Litigator, etc.)
- All model names come from the `modelList` parameter injected at runtime — NEVER hardcode model names in prompt files
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/families/helix/prompt.ts src/lib/families/stomp/prompt.ts src/lib/families/podgo/prompt.ts src/lib/families/stadium/prompt.ts src/lib/prompt-router.ts 2>&1 | head -20</automated>
  </verify>
  <done>Four per-family prompt modules and one prompt router exist. Each family exports buildPlannerPrompt() and getSystemPrompt(). The router exports getFamilyPlannerPrompt() and getFamilyChatPrompt(). TypeScript compiles without errors. Helix planner prompt contains detailed dual-DSP routing instructions with numbered steps. No cross-family model names are hardcoded in any file.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — all 8 new files compile without errors
2. `grep -r "Agoura_" src/lib/families/helix/ src/lib/families/stomp/ src/lib/families/podgo/` returns 0 results (no Agoura in non-Stadium family prompts)
3. `grep -r "Placater\|Derailed\|Litigator\|Revv" src/lib/families/stadium/` returns 0 results (no HD2 names in Stadium prompt)
4. `grep -r "Agoura_\|Placater" src/lib/families/shared/` returns 0 results (no model names in shared sections)
5. Helix `buildPlannerPrompt()` output contains "DSP0", "DSP1", "split block", "join", numbered routing steps
6. Stomp `getSystemPrompt()` output contains trade-off question language
7. PodGo `getSystemPrompt()` output contains "4 effect slots" empowering framing
8. Stadium `getSystemPrompt()` output contains FOH/arena/live-sound vocabulary
</verification>

<success_criteria>
- 8 new TypeScript files created and compiling
- Each per-family prompt module exports both `buildPlannerPrompt()` and `getSystemPrompt()`
- Prompt router dispatches via exhaustive switch with all four families covered
- Zero cross-family model names in any prompt file (verified by grep)
- Helix planner prompt has explicit dual-DSP routing with numbered block-ordering steps
- Stomp/PodGo prompts use device-correct block/snapshot limits from STOMP_CONFIG constants (not hardcoded numbers)
- Shared sections are parameterized and contain no device-specific content
</success_criteria>

<output>
After completion, create `.planning/phases/65-device-specific-prompts/65-01-SUMMARY.md`
</output>
