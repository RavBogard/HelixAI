---
phase: 65-device-specific-prompts
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - src/app/api/chat/route.ts
  - src/app/api/generate/route.ts
  - src/lib/planner.ts
  - src/lib/gemini.ts
  - src/lib/usage-logger.ts
  - src/lib/families/helix/prompt.test.ts
  - src/lib/families/stomp/prompt.test.ts
  - src/lib/families/podgo/prompt.test.ts
  - src/lib/families/stadium/prompt.test.ts
  - src/lib/prompt-router.test.ts
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06

must_haves:
  truths:
    - "The chat API route passes DeviceTarget to getFamilyChatPrompt() — no more parameterless getSystemPrompt() call"
    - "The generate API route uses getFamilyPlannerPrompt() from the prompt router — no more monolithic buildPlannerPrompt() call"
    - "The old monolithic buildPlannerPrompt() in planner.ts is deleted — not deprecated, deleted"
    - "The old monolithic getSystemPrompt() in gemini.ts is deleted — not deprecated, deleted"
    - "Per-family prompt tests verify cross-family model name isolation (grep-equivalent assertions)"
    - "Per-family prompt tests verify required device-specific sections exist (dual-DSP for Helix, trade-off questions for Stomp, 4-slot framing for PodGo, FOH vocabulary for Stadium)"
    - "PlannerUsageRecord includes a device field for post-ship cache economics analysis"
  artifacts:
    - path: "src/app/api/chat/route.ts"
      provides: "Chat route using per-family chat prompts"
      contains: "getFamilyChatPrompt"
    - path: "src/app/api/generate/route.ts"
      provides: "Generate route using per-family planner prompts"
      contains: "getFamilyPlannerPrompt"
    - path: "src/lib/planner.ts"
      provides: "Planner module with buildPlannerPrompt deleted, callClaudePlanner updated"
      exports: ["callClaudePlanner"]
    - path: "src/lib/gemini.ts"
      provides: "Gemini module with getSystemPrompt deleted"
      exports: ["getModelId", "isPremiumKey", "createGeminiClient"]
    - path: "src/lib/usage-logger.ts"
      provides: "Usage logger with device field in PlannerUsageRecord"
      contains: "device.*DeviceTarget"
    - path: "src/lib/families/helix/prompt.test.ts"
      provides: "Helix prompt isolation and content tests"
    - path: "src/lib/families/stomp/prompt.test.ts"
      provides: "Stomp prompt isolation and content tests"
    - path: "src/lib/families/podgo/prompt.test.ts"
      provides: "Pod Go prompt isolation and content tests"
    - path: "src/lib/families/stadium/prompt.test.ts"
      provides: "Stadium prompt isolation and content tests"
    - path: "src/lib/prompt-router.test.ts"
      provides: "Prompt router dispatch tests"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/prompt-router.ts"
      via: "import getFamilyChatPrompt, pass device from request body"
      pattern: "getFamilyChatPrompt\\(.*device"
    - from: "src/app/api/generate/route.ts"
      to: "src/lib/prompt-router.ts"
      via: "import getFamilyPlannerPrompt, pass deviceTarget and modelList"
      pattern: "getFamilyPlannerPrompt\\(deviceTarget"
    - from: "src/lib/planner.ts"
      to: "src/lib/prompt-router.ts"
      via: "callClaudePlanner() calls getFamilyPlannerPrompt() for system prompt"
      pattern: "getFamilyPlannerPrompt"
    - from: "src/lib/planner.ts"
      to: "src/lib/usage-logger.ts"
      via: "logUsage() now includes device field"
      pattern: "device.*deviceTarget"
---

<objective>
Wire the API routes to use the per-family prompt modules from Plan 65-01, delete the old monolithic prompt functions, add device tracking to usage logging, and create comprehensive per-family prompt tests.

Purpose: Complete the prompt isolation refactor by updating all consumers. After this plan, every chat and generation request flows through the per-family prompt router. The old monolithic functions are gone (not deprecated — deleted), preventing silent fallback. Per-family tests enforce cross-family isolation as a regression guard.

Output: Updated API routes, cleaned-up planner/gemini modules, usage logger with device field, and 5 test files covering all families and the router.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/65-device-specific-prompts/65-CONTEXT.md
@.planning/phases/65-device-specific-prompts/65-RESEARCH.md
@.planning/phases/65-device-specific-prompts/65-01-SUMMARY.md
@src/app/api/chat/route.ts
@src/app/api/generate/route.ts
@src/lib/planner.ts
@src/lib/gemini.ts
@src/lib/usage-logger.ts
@src/lib/planner.test.ts
@src/lib/prompt-router.ts
@src/lib/families/helix/prompt.ts
@src/lib/families/stomp/prompt.ts
@src/lib/families/podgo/prompt.ts
@src/lib/families/stadium/prompt.ts

<interfaces>
<!-- Key types and contracts from Plan 65-01 output that this plan consumes -->

From src/lib/prompt-router.ts (created in 65-01):
```typescript
export type DeviceFamily = "helix" | "stomp" | "podgo" | "stadium";
export function getFamilyPlannerPrompt(device: DeviceTarget, modelList: string): string;
export function getFamilyChatPrompt(device: DeviceTarget): string;
```

From src/lib/planner.ts (existing — to be modified):
```typescript
export function buildPlannerPrompt(modelList: string, device?: DeviceTarget): string;  // DELETE
export async function callClaudePlanner(messages: ..., device: DeviceTarget, toneContext?: string): Promise<ToneIntent>;  // KEEP, update
```

From src/lib/gemini.ts (existing — to be modified):
```typescript
export function getSystemPrompt(): string;  // DELETE
export function getModelId(premium: boolean): string;  // KEEP
export function isPremiumKey(key: string | undefined | null): boolean;  // KEEP
export function createGeminiClient(): ...;  // KEEP
```

From src/lib/usage-logger.ts (existing — to be modified):
```typescript
export interface PlannerUsageRecord {
  timestamp: string;
  endpoint: "generate" | "chat";
  model: string;
  input_tokens: number;
  output_tokens: number;
  cache_creation_input_tokens: number | null;
  cache_read_input_tokens: number | null;
  total_tokens: number;
  cost_usd: number;
  cache_hit: boolean;
  // ADD: device: DeviceTarget;
}
```

From src/app/api/chat/route.ts (existing — to be modified):
```typescript
// Line 2: import { createGeminiClient, getSystemPrompt, getModelId, isPremiumKey } from "@/lib/gemini";
// Line 74: systemInstruction: getSystemPrompt(),  // ← must become getFamilyChatPrompt(device)
```

From src/app/api/generate/route.ts (existing — to be modified):
```typescript
// Line 2: import { callClaudePlanner } from "@/lib/planner";
// Line 30-42: deviceTarget resolution from request body
// Line 82: const toneIntent = await callClaudePlanner(messages, deviceTarget, toneContext);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire API routes, update planner/gemini, add device to usage logger</name>
  <files>src/app/api/chat/route.ts, src/app/api/generate/route.ts, src/lib/planner.ts, src/lib/gemini.ts, src/lib/usage-logger.ts</files>
  <behavior>
    - After changes, `getSystemPrompt` is no longer exported from gemini.ts
    - After changes, `buildPlannerPrompt` is no longer exported from planner.ts
    - `callClaudePlanner` still works and now uses `getFamilyPlannerPrompt()` for the system prompt
    - Chat route calls `getFamilyChatPrompt(device)` where device comes from the request body
    - PlannerUsageRecord includes a `device` field of type DeviceTarget
    - logUsage calls in planner.ts pass the device field
  </behavior>
  <action>
**1. Update `src/lib/planner.ts`:**
- DELETE the `buildPlannerPrompt()` function entirely (the whole function body — ~170 lines). Do not deprecate — delete.
- In `callClaudePlanner()`, replace the call to `buildPlannerPrompt(modelList, device)` with `getFamilyPlannerPrompt(device, modelList)` imported from `@/lib/prompt-router`.
- Remove the now-unused imports: `isPodGo`, `isStadium`, `isStomp` from `@/lib/helix`, and `AMP_MODELS` if no longer used.
- Keep `getModelListForPrompt` import (still needed for `modelList` param).
- Update the `logUsage()` call to include `device: device` in the record.
- Preserve the existing `cache_control: { type: "ephemeral" as const, ttl: "1h" }` pattern in the Claude API call — do NOT change the cache structure.

**2. Update `src/lib/gemini.ts`:**
- DELETE the `getSystemPrompt()` function entirely. Do not deprecate — delete.
- Keep all other exports: `getModelId()`, `isPremiumKey()`, `createGeminiClient()`, `MODEL_STANDARD`, `MODEL_PREMIUM`.

**3. Update `src/app/api/chat/route.ts`:**
- Remove `getSystemPrompt` from the `@/lib/gemini` import.
- Add import: `import { getFamilyChatPrompt } from "@/lib/prompt-router";`
- The route already has `device` available from the request body (check the existing code — it should be parsed from `req.json()`). If the route does not currently have device available, extract `device` from the request body with a fallback: `const device: DeviceTarget = body.device ?? "helix_lt";`
- Replace `systemInstruction: getSystemPrompt()` with `systemInstruction: getFamilyChatPrompt(device)`.

**4. Update `src/app/api/generate/route.ts`:**
- The generate route already resolves `deviceTarget` from the request body (lines 30-42). No change needed for device resolution.
- `callClaudePlanner(messages, deviceTarget, toneContext)` already passes deviceTarget — the internal change in planner.ts handles the rest. No changes needed to the generate route's planner call.
- However, verify that the import of `callClaudePlanner` from `@/lib/planner` still works after the planner.ts changes. It should — only `buildPlannerPrompt` was deleted.

**5. Update `src/lib/usage-logger.ts`:**
- Add `device: string;` field to the `PlannerUsageRecord` interface (use `string` rather than importing DeviceTarget to keep the logger dependency-free — the field is for analysis, not type enforcement).
- Update the `logUsage()` function to include the device field in the JSON output.

**Key anti-patterns to avoid:**
- Do NOT leave `buildPlannerPrompt` or `getSystemPrompt` as deprecated stubs — DELETE them to prevent silent fallback
- Do NOT change the cache_control structure in callClaudePlanner — only the source of the system prompt string changes
- Do NOT modify the deviceTarget resolution logic in generate/route.ts — it already works correctly
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/api/chat/route.ts src/app/api/generate/route.ts src/lib/planner.ts src/lib/gemini.ts src/lib/usage-logger.ts 2>&1 | head -30</automated>
  </verify>
  <done>API routes use prompt-router functions. Old monolithic buildPlannerPrompt() and getSystemPrompt() are deleted. TypeScript compiles without errors. PlannerUsageRecord has a device field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create per-family prompt tests and prompt router tests</name>
  <files>src/lib/families/helix/prompt.test.ts, src/lib/families/stomp/prompt.test.ts, src/lib/families/podgo/prompt.test.ts, src/lib/families/stadium/prompt.test.ts, src/lib/prompt-router.test.ts</files>
  <behavior>
    - Helix prompt test: planner prompt contains "DSP0", "DSP1", "split block", does NOT contain "Agoura_", dual-amp only when explicitly requested
    - Stomp prompt test: chat prompt contains trade-off language, planner prompt uses STOMP_CONFIG limits (not hardcoded), does NOT contain "Agoura_"
    - PodGo prompt test: chat prompt contains "4 effect slots" empowering framing, planner prompt hard-enforces 4-effect limit, does NOT contain "Agoura_"
    - Stadium prompt test: chat prompt contains FOH/arena vocabulary, does NOT contain "Placater" or "Derailed" or other HD2 amp names
    - Router test: all six DeviceTarget values dispatch to correct family, returns non-empty strings
  </behavior>
  <action>
Create 5 test files using vitest (existing project test framework). Follow the pattern from the existing `src/lib/planner.test.ts`.

**src/lib/families/helix/prompt.test.ts:**
```typescript
import { describe, it, expect } from "vitest";
import { buildPlannerPrompt, getSystemPrompt } from "./prompt";
```
Tests:
- `buildPlannerPrompt("helix_lt", sampleModelList)` contains "DSP0" and "DSP1" and "split block" and "join" (dual-DSP routing)
- Contains numbered routing steps (e.g., "1.", "2." in the DSP section)
- Contains "ONLY when user explicitly requests" (dual-amp not proactive)
- Does NOT contain "Agoura_" (cross-family isolation)
- `getSystemPrompt("helix_lt")` does NOT contain "Agoura_"
- `getSystemPrompt("helix_lt")` contains "device is already selected" (preserved from existing gemini.ts)
- `buildPlannerPrompt("helix_lt", ...)` and `buildPlannerPrompt("helix_floor", ...)` return identical text (same cache entry)

**src/lib/families/stomp/prompt.test.ts:**
- `getSystemPrompt("helix_stomp")` contains trade-off question language (e.g., "which matters more" or "prioritize")
- `buildPlannerPrompt("helix_stomp", ...)` does NOT contain "Agoura_"
- `buildPlannerPrompt("helix_stomp", ...)` references 3 snapshots; `buildPlannerPrompt("helix_stomp_xl", ...)` references 4 snapshots (different limits)
- `buildPlannerPrompt("helix_stomp", ...)` contains priority hierarchy language (drive > delay > mod for metal)

**src/lib/families/podgo/prompt.test.ts:**
- `getSystemPrompt("pod_go")` contains "4 effect slots" or "4 slots" (empowering framing)
- `buildPlannerPrompt("pod_go", ...)` does NOT contain "Agoura_"
- `buildPlannerPrompt("pod_go", ...)` specifies maxEffects of 4 (visible in ToneIntent fields section)
- Does NOT contain "stretch" or language implying the 4-slot limit is negotiable

**src/lib/families/stadium/prompt.test.ts:**
- `getSystemPrompt("helix_stadium")` contains at least one of: "FOH", "Front of House", "stage volume", "monitor", "arena", "live sound"
- `getSystemPrompt("helix_stadium")` does NOT contain "Placater" or "Derailed" or "Litigator" (HD2 names)
- `buildPlannerPrompt("helix_stadium", ...)` does NOT contain "Placater" or "Derailed" (cross-family isolation)
- `buildPlannerPrompt("helix_stadium", ...)` contains "TODO(Phase62)" (placeholder for Agoura cab pairing table)

**src/lib/prompt-router.test.ts:**
- `getFamilyPlannerPrompt("helix_lt", modelList)` returns non-empty string
- `getFamilyPlannerPrompt("helix_floor", modelList)` returns non-empty string
- `getFamilyPlannerPrompt("helix_stomp", modelList)` returns non-empty string
- `getFamilyPlannerPrompt("helix_stomp_xl", modelList)` returns non-empty string
- `getFamilyPlannerPrompt("pod_go", modelList)` returns non-empty string
- `getFamilyPlannerPrompt("helix_stadium", modelList)` returns non-empty string
- `getFamilyChatPrompt("helix_lt")` returns non-empty string
- Same for all 6 devices — all return non-empty strings from `getFamilyChatPrompt()`
- Different families return different prompt text (helix prompt !== stomp prompt)

For model list test data, use a minimal string like `"## AMPS\n- TestAmp1\n- TestAmp2\n## CABS\n- TestCab1"` — these tests verify prompt structure, not actual model content.
  </action>
  <verify>
    <automated>npx vitest run src/lib/families/ src/lib/prompt-router.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All per-family prompt tests and router tests pass. Cross-family isolation is verified by assertions. Device-specific content (dual-DSP for Helix, trade-offs for Stomp, 4-slot for PodGo, FOH for Stadium) is verified by assertions. Router dispatches all 6 DeviceTarget values correctly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — full project compiles without errors after all changes
2. `npx vitest run src/lib/families/ src/lib/prompt-router.test.ts` — all new tests pass
3. `npx vitest run src/lib/planner.test.ts` — existing planner tests still pass (or are updated if they tested deleted functions)
4. `grep -r "buildPlannerPrompt" src/lib/planner.ts` — returns 0 results (function deleted)
5. `grep -r "getSystemPrompt" src/lib/gemini.ts` — returns 0 results (function deleted)
6. `grep "getFamilyChatPrompt" src/app/api/chat/route.ts` — returns result (new import wired)
7. `grep "device" src/lib/usage-logger.ts` — PlannerUsageRecord has device field
</verification>

<success_criteria>
- Chat route calls `getFamilyChatPrompt(device)` instead of `getSystemPrompt()`
- Generate route's `callClaudePlanner()` internally uses `getFamilyPlannerPrompt()` instead of `buildPlannerPrompt()`
- Old `buildPlannerPrompt()` in planner.ts is gone — not exported, not present
- Old `getSystemPrompt()` in gemini.ts is gone — not exported, not present
- PlannerUsageRecord has a `device` field for cache economics analysis
- All 5 new test files pass with all assertions green
- Existing planner.test.ts still passes (or is updated to reflect the deleted function)
- Full project TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/65-device-specific-prompts/65-02-SUMMARY.md`
</output>
