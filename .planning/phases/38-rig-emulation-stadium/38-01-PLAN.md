# Phase 38 Plan — Rig Emulation for Stadium

**Phase:** 38
**Plan:** 01
**Requirements:** STAD-08

## Objective

Verify and test that rig emulation (pedal photo + text description) works with `device: "helix_stadium"`. The implementation was partially completed in Phase 32 (`/api/map` route) and is already working through the existing `mapRigToSubstitutions` architecture. This phase adds STAD-08 test coverage to `rig-mapping.test.ts`.

## Status Before This Plan

- `/api/map` route: already accepts `"helix_stadium"` (Phase 32)
- `mapRigToSubstitutions(rigIntent, "helix_stadium")`: already works — no Stadium-specific code path needed; existing `getModelIdForDevice` returns model IDs as-is for non-Pod-Go devices
- `isModelAvailableForDevice("...", "helix_stadium")`: already handles Stadium (Phase 33/34)
- No new source code changes needed — test coverage only

## Changes Required

### src/lib/rig-mapping.test.ts

Add a new describe block `"mapRigToSubstitutions — helix_stadium (STAD-08)"` with tests:

1. `helix_stadium does not throw for a known pedal` — `mapRigToSubstitutions(rig, "helix_stadium")` returns without throwing
2. `helix_stadium returns standard HD2_ IDs (no Mono/Stereo suffix)` — verifies non-Pod-Go path
3. `helix_stadium returns confidence 'direct' for known pedal` — same direct-tier as helix_lt
4. `helix_stadium does not return Agoura amps for effect substitutions` — effect entries use HD2_ IDs from DISTORTION_MODELS etc., not Agoura_ amps (amps only come from generate path, not rig mapping)
5. `mapRigToSubstitutions on helix_lt, helix_floor, pod_go unaffected` — regression tests
