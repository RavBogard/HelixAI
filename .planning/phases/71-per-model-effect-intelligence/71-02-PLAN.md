---
phase: 71-per-model-effect-intelligence
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - src/lib/families/helix/prompt.ts
  - src/lib/families/stomp/prompt.ts
  - src/lib/families/stadium/prompt.ts
  - src/lib/families/podgo/prompt.ts
  - src/lib/families/helix/prompt.test.ts
  - src/lib/families/stomp/prompt.test.ts
  - src/lib/families/stadium/prompt.test.ts
  - src/lib/families/podgo/prompt.test.ts
autonomous: true
requirements: [INTEL-01, INTEL-02, INTEL-03, INTEL-04]

must_haves:
  truths:
    - "All 4 family planner prompts contain the genre-effect-model section"
    - "The section appears AFTER Effect Discipline and BEFORE device-specific content"
    - "The section is identical across all 4 families (shared module, single source of truth)"
    - "helix_lt and helix_floor still produce identical prompt text (single cache entry)"
    - "helix_stomp and helix_stomp_xl still produce identical prompt text (single cache entry)"
  artifacts:
    - path: "src/lib/families/helix/prompt.ts"
      provides: "Helix prompt with genreEffectModelSection() call"
      contains: "genreEffectModelSection"
    - path: "src/lib/families/stomp/prompt.ts"
      provides: "Stomp prompt with genreEffectModelSection() call"
      contains: "genreEffectModelSection"
    - path: "src/lib/families/stadium/prompt.ts"
      provides: "Stadium prompt with genreEffectModelSection() call"
      contains: "genreEffectModelSection"
    - path: "src/lib/families/podgo/prompt.ts"
      provides: "Pod Go prompt with genreEffectModelSection() call"
      contains: "genreEffectModelSection"
    - path: "src/lib/families/helix/prompt.test.ts"
      provides: "Test verifying genre-effect section in Helix prompt"
      contains: "Genre-Informed Effect Model Selection"
    - path: "src/lib/families/stadium/prompt.test.ts"
      provides: "Test verifying genre-effect section in Stadium prompt"
      contains: "Genre-Informed Effect Model Selection"
  key_links:
    - from: "src/lib/families/helix/prompt.ts"
      to: "src/lib/families/shared/effect-model-intelligence.ts"
      via: "import { genreEffectModelSection }"
      pattern: "genreEffectModelSection"
    - from: "src/lib/families/stadium/prompt.ts"
      to: "src/lib/families/shared/effect-model-intelligence.ts"
      via: "import { genreEffectModelSection }"
      pattern: "genreEffectModelSection"
    - from: "src/lib/families/stomp/prompt.ts"
      to: "src/lib/families/shared/effect-model-intelligence.ts"
      via: "import { genreEffectModelSection }"
      pattern: "genreEffectModelSection"
    - from: "src/lib/families/podgo/prompt.ts"
      to: "src/lib/families/shared/effect-model-intelligence.ts"
      via: "import { genreEffectModelSection }"
      pattern: "genreEffectModelSection"
---

<objective>
Wire genreEffectModelSection() into all 4 family planner prompts and add alignment tests confirming the section is present and positioned correctly.

Purpose: Completes INTEL-04 (static system prompt placement) and ensures INTEL-01/02/03 genre guidance reaches the AI across all device families. The section is in the cached system prompt, not the dynamic user message.
Output: All 4 prompts contain genre-effect-model guidance, verified by per-family prompt tests.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-per-model-effect-intelligence/71-RESEARCH.md
@.planning/phases/71-per-model-effect-intelligence/71-01-SUMMARY.md

@src/lib/families/helix/prompt.ts
@src/lib/families/stomp/prompt.ts
@src/lib/families/stadium/prompt.ts
@src/lib/families/podgo/prompt.ts
@src/lib/families/helix/prompt.test.ts
@src/lib/families/stomp/prompt.test.ts
@src/lib/families/stadium/prompt.test.ts
@src/lib/families/podgo/prompt.test.ts
@src/lib/families/shared/effect-model-intelligence.ts

<interfaces>
<!-- Created by 71-01 — the shared module to import -->

From src/lib/families/shared/effect-model-intelligence.ts:
```typescript
export function genreEffectModelSection(): string;
// Returns prompt text starting with "## Genre-Informed Effect Model Selection"
// Contains 3 tables: Delay Models by Genre, Reverb Models by Genre, Wah Models by Genre
```

<!-- Existing import patterns in each prompt file -->

From src/lib/families/helix/prompt.ts (lines 8-10):
```typescript
import { gainStagingSection } from "../shared/gain-staging";
import { toneIntentFieldsSection } from "../shared/tone-intent-fields";
import { ampCabPairingSection } from "../shared/amp-cab-pairing";
```

From src/lib/families/stadium/prompt.ts (lines 8-9):
```typescript
import { gainStagingSection } from "../shared/gain-staging";
import { toneIntentFieldsSection } from "../shared/tone-intent-fields";
// NOTE: Stadium does NOT import ampCabPairingSection — it builds its own table
```

<!-- Insertion points in each prompt's buildPlannerPrompt() -->

Helix prompt.ts — insert after line 98 (ampCabPairingSection):
```
${ampCabPairingSection(HELIX_AMP_CAB_PAIRINGS)}

${genreEffectModelSection()}    // <-- NEW

## Effect Discipline by Genre
```

Stomp prompt.ts — insert after line 103 (ampCabPairingSection):
```
${ampCabPairingSection(STOMP_AMP_CAB_PAIRINGS)}

${genreEffectModelSection()}    // <-- NEW

## Effect Discipline by Genre
```

Stadium prompt.ts — insert after line 70 (buildAmpCabPairingTable block):
```
Choosing the recommended cab improves FOH translation...

${genreEffectModelSection()}    // <-- NEW

## Stadium-Specific Features
```

Pod Go prompt.ts — insert after line 83 (ampCabPairingSection):
```
${ampCabPairingSection(PODGO_AMP_CAB_PAIRINGS)}

${genreEffectModelSection()}    // <-- NEW

## Effect Discipline by Genre
```

<!-- Existing test patterns -->

From src/lib/families/helix/prompt.test.ts:
```typescript
it("contains gain-staging intelligence section", () => {
  expect(prompt).toContain("## Gain-Staging Intelligence");
});
it("contains amp-to-cab pairing section", () => {
  expect(prompt).toContain("## Amp-to-Cab Pairing");
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add genreEffectModelSection() to all 4 family prompts with alignment tests</name>
  <files>
    src/lib/families/helix/prompt.ts,
    src/lib/families/stomp/prompt.ts,
    src/lib/families/stadium/prompt.ts,
    src/lib/families/podgo/prompt.ts,
    src/lib/families/helix/prompt.test.ts,
    src/lib/families/stomp/prompt.test.ts,
    src/lib/families/stadium/prompt.test.ts,
    src/lib/families/podgo/prompt.test.ts
  </files>
  <behavior>
    - Test 1 (helix): buildPlannerPrompt contains "## Genre-Informed Effect Model Selection"
    - Test 2 (helix): genre section appears BEFORE "Based on the conversation" (cache position check)
    - Test 3 (helix): helix_lt and helix_floor still produce identical prompt text
    - Test 4 (stomp): buildPlannerPrompt contains "## Genre-Informed Effect Model Selection"
    - Test 5 (stomp): helix_stomp and helix_stomp_xl still produce identical prompt text
    - Test 6 (stadium): buildPlannerPrompt contains "## Genre-Informed Effect Model Selection"
    - Test 7 (podgo): buildPlannerPrompt contains "## Genre-Informed Effect Model Selection"
    - Test 8 (cross-family): All 4 families contain "Transistor Tape" in their prompt (delay table present)
    - Test 9 (cross-family): All 4 families contain "'63 Spring" in their prompt (reverb table present)
    - Test 10 (cross-family): All 4 families contain "Fassel" in their prompt (wah table present)
  </behavior>
  <action>
    **Step 1 (RED):** Add tests to each family's prompt.test.ts:

    In `src/lib/families/helix/prompt.test.ts`, add:
    ```typescript
    it("contains genre-informed effect model selection section", () => {
      expect(prompt).toContain("## Genre-Informed Effect Model Selection");
    });

    it("genre-effect section appears before 'Based on the conversation'", () => {
      const sectionIdx = prompt.indexOf("## Genre-Informed Effect Model Selection");
      const endIdx = prompt.indexOf("Based on the conversation");
      expect(sectionIdx).toBeGreaterThan(-1);
      expect(sectionIdx).toBeLessThan(endIdx);
    });

    // Verify cache identity is preserved (existing test may already check this, but add explicit genre section check)
    ```

    In `src/lib/families/stomp/prompt.test.ts`, add:
    ```typescript
    it("contains genre-informed effect model selection section", () => {
      expect(prompt).toContain("## Genre-Informed Effect Model Selection");
    });
    ```

    In `src/lib/families/stadium/prompt.test.ts`, add:
    ```typescript
    it("contains genre-informed effect model selection section", () => {
      expect(prompt).toContain("## Genre-Informed Effect Model Selection");
    });
    ```

    In `src/lib/families/podgo/prompt.test.ts`, add:
    ```typescript
    it("contains genre-informed effect model selection section", () => {
      expect(prompt).toContain("## Genre-Informed Effect Model Selection");
    });
    ```

    Add cross-family content tests in the most appropriate test file (helix/prompt.test.ts or a new describe block):
    ```typescript
    it("contains delay genre table with Transistor Tape", () => {
      expect(prompt).toContain("Transistor Tape");
    });
    it("contains reverb genre table with '63 Spring", () => {
      expect(prompt).toContain("'63 Spring");
    });
    it("contains wah genre table with Fassel", () => {
      expect(prompt).toContain("Fassel");
    });
    ```

    Run tests — the new tests must fail because the prompts don't contain the section yet.

    **Step 2 (GREEN):** Wire genreEffectModelSection() into all 4 family prompts.

    **helix/prompt.ts:**
    1. Add import: `import { genreEffectModelSection } from "../shared/effect-model-intelligence";`
    2. In `buildPlannerPrompt()`, insert `${genreEffectModelSection()}` on a new line AFTER `${ampCabPairingSection(HELIX_AMP_CAB_PAIRINGS)}` and BEFORE `## Effect Discipline by Genre`. Add a blank line before and after for readability.

    **stomp/prompt.ts:**
    1. Add import: `import { genreEffectModelSection } from "../shared/effect-model-intelligence";`
    2. In `buildPlannerPrompt()`, insert `${genreEffectModelSection()}` AFTER `${ampCabPairingSection(STOMP_AMP_CAB_PAIRINGS)}` and BEFORE `## Effect Discipline by Genre`.

    **stadium/prompt.ts:**
    1. Add import: `import { genreEffectModelSection } from "../shared/effect-model-intelligence";`
    2. In `buildPlannerPrompt()`, insert `${genreEffectModelSection()}` AFTER the amp-cab pairing block (after `Choosing the recommended cab improves...`) and BEFORE `## Stadium-Specific Features`.

    **podgo/prompt.ts:**
    1. Add import: `import { genreEffectModelSection } from "../shared/effect-model-intelligence";`
    2. In `buildPlannerPrompt()`, insert `${genreEffectModelSection()}` AFTER `${ampCabPairingSection(PODGO_AMP_CAB_PAIRINGS)}` and BEFORE `## Effect Discipline by Genre`.

    **Step 3:** Run all tests. The new prompt tests must pass. All existing prompt tests (cache identity, cross-family isolation, content checks) must still pass. Run the full suite to confirm no regressions.

    **CRITICAL:** Do NOT add genreEffectModelSection() to the chat prompts (getSystemPrompt). It belongs ONLY in the planner prompts (buildPlannerPrompt) because the planner makes model selections. The chat prompt is the interview personality.
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run src/lib/families/helix/prompt.test.ts src/lib/families/stomp/prompt.test.ts src/lib/families/stadium/prompt.test.ts src/lib/families/podgo/prompt.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All 4 family planner prompts contain "## Genre-Informed Effect Model Selection"
    - Genre-effect section appears in the static system prompt (BEFORE "Based on the conversation")
    - Helix LT/Floor produce identical prompts (cache identity preserved)
    - Stomp/Stomp XL produce identical prompts (cache identity preserved)
    - All prompt tests pass, all 734+ tests pass
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && npx vitest run --reporter=verbose 2>&1 | tail -5
```
Expected: 734 + ~10 new tests = 744+ tests passing.

Additionally verify genre-effect content is in the static prompt (not dynamic user message):
```bash
cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && grep -l "genreEffectModelSection" src/lib/families/*/prompt.ts
```
Expected: 4 files (helix, stomp, stadium, podgo).

Verify no accidental insertion into chat prompts:
```bash
cd C:/Users/dsbog/HelixAI/.claude/worktrees/condescending-khorana && grep -n "genreEffectModelSection" src/lib/families/*/prompt.ts
```
Expected: Each file should show the import line and exactly one usage in `buildPlannerPrompt()`, NOT in `getSystemPrompt()`.
</verification>

<success_criteria>
1. All 4 family planner prompts contain the genre-effect-model section (INTEL-04)
2. Section contains delay (INTEL-01), reverb (INTEL-02), and wah (INTEL-03) genre tables
3. Section is in the static cached prompt, not the dynamic user message
4. helix_lt/helix_floor cache identity preserved (identical prompt text)
5. helix_stomp/helix_stomp_xl cache identity preserved (identical prompt text)
6. ~10 new tests pass, all prior tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/71-per-model-effect-intelligence/71-02-SUMMARY.md`
</output>
