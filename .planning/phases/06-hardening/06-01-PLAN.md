---
phase: 06-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helix/config.ts
  - src/lib/helix/preset-builder.ts
  - src/lib/helix/index.ts
  - package.json
autonomous: true
requirements:
  - SC-3
  - SC-4

must_haves:
  truths:
    - "Firmware version values are read from a config module, not hardcoded in preset-builder.ts"
    - "Changing the firmware version requires editing exactly one config file, not touching generation code"
    - "The openai package is not in package.json dependencies"
    - "No source file in src/ imports or references the openai package"
  artifacts:
    - path: "src/lib/helix/config.ts"
      provides: "Centralized firmware version config"
      exports: ["FIRMWARE_CONFIG"]
    - path: "src/lib/helix/preset-builder.ts"
      provides: "Preset builder importing firmware values from config"
      contains: "import.*config"
    - path: "package.json"
      provides: "Clean dependency list without openai"
  key_links:
    - from: "src/lib/helix/preset-builder.ts"
      to: "src/lib/helix/config.ts"
      via: "import FIRMWARE_CONFIG"
      pattern: "FIRMWARE_CONFIG\\."
---

<objective>
Parameterize firmware version constants and remove the openai dependency.

Purpose: Two independent hardening tasks that eliminate hardcoded firmware values (so firmware updates don't require code changes in generation logic) and clean the last vestiges of the abandoned multi-provider architecture (openai package).

Output: A new `config.ts` module with firmware constants, updated `preset-builder.ts` importing from config, and a clean `package.json` without openai.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/helix/preset-builder.ts
@src/lib/helix/index.ts
@package.json
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/helix/preset-builder.ts (lines 5-7, the constants to extract):
```typescript
const HLX_VERSION = 6;
const HLX_APP_VERSION = 57671680; // FW 3.70+
const HLX_BUILD_SHA = "v3.70";
```

From src/lib/helix/index.ts (barrel exports — add config re-export):
```typescript
export { buildHlxFile, summarizePreset } from "./preset-builder";
// ... other exports
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract firmware constants to config module and update preset-builder</name>
  <files>src/lib/helix/config.ts, src/lib/helix/preset-builder.ts, src/lib/helix/index.ts</files>
  <action>
1. Create `src/lib/helix/config.ts` with a single exported `FIRMWARE_CONFIG` object containing all three firmware-related constants currently hardcoded in preset-builder.ts:
   - `HLX_VERSION` (currently `6`) — the schema version
   - `HLX_APP_VERSION` (currently `57671680`) — the firmware application version integer (FW 3.70+)
   - `HLX_BUILD_SHA` (currently `"v3.70"`) — the firmware build string

   Use the project's UPPER_SNAKE_CASE constant convention. Add a JSDoc comment explaining the values and noting that updating firmware only requires changing this file. Example structure:
   ```typescript
   /**
    * Helix firmware version configuration.
    * To support a new firmware version, update these values — no other code changes needed.
    * Values correspond to HX Edit .hlx file format fields.
    */
   export const FIRMWARE_CONFIG = {
     /** Schema version number in .hlx JSON */
     HLX_VERSION: 6,
     /** Application version integer — encodes firmware version (e.g., 57671680 = FW 3.70) */
     HLX_APP_VERSION: 57671680,
     /** Build SHA string shown in .hlx meta (e.g., "v3.70") */
     HLX_BUILD_SHA: "v3.70",
   } as const;
   ```

2. Update `src/lib/helix/preset-builder.ts`:
   - Remove the three hardcoded constants (`HLX_VERSION`, `HLX_APP_VERSION`, `HLX_BUILD_SHA`) on lines 5-7
   - Add import: `import { FIRMWARE_CONFIG } from "./config";`
   - Replace all references to the old constants with `FIRMWARE_CONFIG.HLX_VERSION`, `FIRMWARE_CONFIG.HLX_APP_VERSION`, `FIRMWARE_CONFIG.HLX_BUILD_SHA` (there are 4 usages: lines 13, 15, 18, 21)

3. Update `src/lib/helix/index.ts`:
   - Add re-export: `export { FIRMWARE_CONFIG } from "./config";`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - config.ts exists with FIRMWARE_CONFIG exported as const
    - preset-builder.ts imports from config.ts, contains zero hardcoded firmware values
    - index.ts re-exports FIRMWARE_CONFIG
    - All 50 existing tests pass (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove openai package from dependencies</name>
  <files>package.json</files>
  <action>
1. Run `npm uninstall openai` to remove the openai package from package.json and package-lock.json in one step.
2. Verify no source files in `src/` import or reference `openai` (grep to confirm — previous phases already removed all code references, but verify).
3. Run `npm run build` to confirm the Next.js build succeeds without the openai package.

Note: The `.claude/settings.local.json` file references openai in an allowlist entry — this is Claude Code configuration, not application code. Do NOT modify it.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); if (pkg.dependencies?.openai || pkg.devDependencies?.openai) { console.error('FAIL: openai still in package.json'); process.exit(1); } else { console.log('PASS: openai removed from package.json'); }"</automated>
  </verify>
  <done>
    - openai is not in package.json dependencies or devDependencies
    - package-lock.json is updated (openai removed)
    - `npm run build` succeeds
    - No source file in src/ imports openai
  </done>
</task>

</tasks>

<verification>
1. All existing tests pass: `npx vitest run`
2. Build succeeds: `npm run build`
3. Firmware config is centralized: `grep -r "FIRMWARE_CONFIG" src/lib/helix/` shows config.ts (definition), preset-builder.ts (import), index.ts (re-export)
4. No openai in dependencies: `node -e "const p=require('./package.json'); console.log('openai' in (p.dependencies||{}) ? 'FAIL' : 'PASS')"`
5. No hardcoded firmware values in preset-builder: `grep -c "HLX_VERSION\|HLX_APP_VERSION\|HLX_BUILD_SHA" src/lib/helix/preset-builder.ts` returns 0 (only FIRMWARE_CONFIG references)
</verification>

<success_criteria>
- FIRMWARE_CONFIG is the single source of truth for firmware version values
- preset-builder.ts contains zero hardcoded firmware constants
- openai package fully removed from project dependencies
- All 50 existing tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening/06-01-SUMMARY.md`
</output>
