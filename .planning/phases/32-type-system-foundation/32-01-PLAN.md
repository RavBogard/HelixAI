---
phase: 32-type-system-foundation
plan: 01
wave: 1
type: standard
autonomous: true
requirements: [STAD-02, NFR-04]
files_modified:
  - src/lib/helix/types.ts
  - src/lib/helix/config.ts
  - src/lib/helix/models.ts
  - src/lib/helix/chain-rules.ts
  - src/lib/helix/validate.ts
  - src/lib/helix/snapshot-engine.ts
  - src/lib/helix/param-engine.ts
  - src/lib/rig-mapping.ts
  - src/app/api/generate/route.ts
---

<objective>
Add "helix_stadium" to the DeviceTarget union type and thread it through every exhaustiveness check in the codebase. Add DEVICE_IDS.helix_stadium = 2490368, Stadium constants to config.ts, and isStadium() helper. Resolve all TypeScript exhaustiveness errors that result from adding the new union member. The build must pass with zero type errors.
Purpose: TypeScript exhaustiveness errors surface every unimplemented Stadium handler — these become the integration checklist for Phases 33-38.
Output: DeviceTarget union updated, all exhaustiveness errors resolved with Stadium stubs or skeletons, npm run build passes.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-device-id-research-floor-fix/stadium-device-id.md
@src/lib/helix/types.ts
@src/lib/helix/config.ts
@src/lib/helix/models.ts
@src/lib/helix/chain-rules.ts
@src/lib/helix/validate.ts
@src/lib/helix/snapshot-engine.ts
@src/lib/helix/param-engine.ts
</context>

<tasks>

<task type="auto">
  <name>Add helix_stadium to DeviceTarget union and DEVICE_IDS</name>
  <files>src/lib/helix/types.ts</files>
  <action>
    1. Change DeviceTarget from:
       export type DeviceTarget = "helix_lt" | "helix_floor" | "pod_go";
       to:
       export type DeviceTarget = "helix_lt" | "helix_floor" | "pod_go" | "helix_stadium";

    2. Add helix_stadium to DEVICE_IDS Record with value 2490368:
       helix_stadium: 2490368,  // Source: FluidSolo Stadium_Metal_Rhythm.hsp, meta.device_id, 2026-03-04

    3. Add isStadium() helper function after isHelix() and isPodGo():
       /** Returns true if the device target is a Helix Stadium */
       export function isStadium(device: DeviceTarget): boolean {
         return device === "helix_stadium";
       }

    4. IMPORTANT: The DEVICE_IDS Record type must use Record<DeviceTarget, number> - adding helix_stadium to the type will require adding it to the const too (already done in step 2).
  </action>
  <verify>TypeScript accepts the changes; DEVICE_IDS.helix_stadium === 2490368</verify>
  <done>DeviceTarget union includes "helix_stadium"; DEVICE_IDS has helix_stadium = 2490368 with source comment</done>
</task>

<task type="auto">
  <name>Add Stadium firmware constants to config.ts</name>
  <files>src/lib/helix/config.ts</files>
  <action>
    Add a new STADIUM_CONFIG constant block after POD_GO_FIRMWARE_CONFIG:

    /**
     * Helix Stadium file format constants.
     * Source: Direct inspection of real .hsp file (FluidSolo Stadium_Metal_Rhythm.hsp, 2026-03-04)
     */
    export const STADIUM_CONFIG = {
      /** Magic header prepended to all .hsp files — 8 ASCII bytes before JSON content */
      STADIUM_MAGIC_HEADER: "rpshnosj",
      /** Maximum user-assignable effect blocks per path */
      STADIUM_MAX_BLOCKS_PER_PATH: 12,
      /** Maximum snapshot slots per preset */
      STADIUM_MAX_SNAPSHOTS: 8,
      /** Maximum signal paths (1A, 1B, 2A, 2B) */
      STADIUM_MAX_PATHS: 4,
      /** Device version integer from real .hsp inspection */
      STADIUM_DEVICE_VERSION: 301990022,
    } as const;
  </action>
  <verify>STADIUM_CONFIG exports correctly; STADIUM_MAGIC_HEADER is "rpshnosj"</verify>
  <done>STADIUM_CONFIG constant block added to config.ts</done>
</task>

<task type="auto">
  <name>Update models.ts to handle helix_stadium in getModelsForDevice / getModelListForPrompt</name>
  <files>src/lib/helix/models.ts</files>
  <action>
    Find the getModelsForDevice function (or equivalent — may be getModelListForPrompt or getModelIdForDevice).
    Read the full relevant section of models.ts to understand the current device-based filtering pattern.

    For any switch(device) or if/else chain that covers DeviceTarget values:
    - Add a "helix_stadium" case that returns [] or a stub (Phase 33 will fill the real catalog)
    - The stub must make the TypeScript compiler happy — it cannot be a missing case

    For getModelListForPrompt(device: DeviceTarget): if it has a switch on device or if isPodGo(device),
    add an isStadium(device) branch that returns [] for now (stub — Phase 33 populates).

    For getModelIdForDevice and isModelAvailableForDevice: add Stadium handling that returns the
    base HD2 model ID (same as Helix LT) for now — Phase 33 will properly override this.

    Important: Read the full models.ts to understand exact function signatures before editing.
  </action>
  <verify>npm run build shows no TypeScript errors in models.ts related to DeviceTarget exhaustiveness</verify>
  <done>models.ts handles helix_stadium without TypeScript errors</done>
</task>

<task type="auto">
  <name>Update chain-rules.ts to handle helix_stadium</name>
  <files>src/lib/helix/chain-rules.ts</files>
  <action>
    Read chain-rules.ts fully to understand device-specific branches.

    The file uses isPodGo() to detect Pod Go and applies different limits/models.
    Add isStadium() handling:

    1. Import isStadium from types.ts (add to existing import)
    2. Import STADIUM_CONFIG from config.ts
    3. In assembleSignalChain(intent, device):
       - Stadium uses single-path (Path 1A only, dsp:0, path:0)
       - Stadium block limit: STADIUM_CONFIG.STADIUM_MAX_BLOCKS_PER_PATH (not MAX_BLOCKS_PER_DSP)
       - For now, Stadium uses the same mandatory block model IDs as Helix LT (Phase 34 will update to Stadium-specific IDs)
       - Add a stub/stub-comment: "// TODO Phase 34: use Stadium-specific mandatory block model IDs"

    The key requirement: TypeScript must NOT have an unhandled DeviceTarget case.
    Stadium path for v3.0: single path (like Helix LT single-DSP path), Path 1A.
  </action>
  <verify>chain-rules.ts compiles; assembleSignalChain accepts "helix_stadium" without TypeScript error</verify>
  <done>chain-rules.ts handles helix_stadium device target</done>
</task>

<task type="auto">
  <name>Update validate.ts to handle helix_stadium</name>
  <files>src/lib/helix/validate.ts</files>
  <action>
    Read validate.ts fully to understand current device-specific validation.

    Current: const podGo = device ? isPodGo(device) : false;

    Add Stadium handling:
    1. Import isStadium from types.ts
    2. const stadium = device ? isStadium(device) : false;
    3. For Stadium, use the same VALID_IDS set as Helix LT for now (Phase 34 will add Stadium-specific model IDs to VALID_IDS)
    4. Add a comment: "// TODO Phase 34: expand VALID_IDS with Stadium-specific Agoura_* and P35_* model IDs"

    The key fix: any switch(device) in validate.ts must not have an unhandled case.
    If validate.ts uses isPodGo() not switch(device), just ensure isStadium() doesn't throw.
  </action>
  <verify>validate.ts compiles; validatePresetSpec("helix_stadium") does not throw TypeScript compile error</verify>
  <done>validate.ts handles helix_stadium device target</done>
</task>

<task type="auto">
  <name>Update snapshot-engine.ts to handle helix_stadium</name>
  <files>src/lib/helix/snapshot-engine.ts</files>
  <action>
    Read snapshot-engine.ts to find any DeviceTarget-dependent code.

    If there's snapshot count logic (4 for Helix LT/Floor, 4 for Pod Go), add Stadium case:
    - Stadium supports 8 snapshots but v3.0 generates 4 (same as Helix) for consistency
    - Add: if (isStadium(device)) return 4; // v3.0 uses 4; Stadium supports 8 (STADIUM_MAX_SNAPSHOTS)

    Handle any other device-specific branches in snapshot-engine.ts for Stadium.

    Import isStadium if needed.
  </action>
  <verify>snapshot-engine.ts compiles with helix_stadium in DeviceTarget</verify>
  <done>snapshot-engine.ts handles helix_stadium device target</done>
</task>

<task type="auto">
  <name>Update param-engine.ts to handle helix_stadium</name>
  <files>src/lib/helix/param-engine.ts</files>
  <action>
    Read param-engine.ts to find any DeviceTarget-dependent code.

    If there's device-specific parameter resolution (Pod Go has different param ranges),
    add Stadium case:
    - Stadium uses same parameter ranges as Helix LT for now
    - Stadium is NOT Pod Go, so any isPodGo() check correctly excludes it

    Handle any explicit switch(device) patterns for Stadium.
    Import isStadium if needed.
  </action>
  <verify>param-engine.ts compiles with helix_stadium in DeviceTarget</verify>
  <done>param-engine.ts handles helix_stadium device target</done>
</task>

<task type="auto">
  <name>Update src/lib/rig-mapping.ts to handle helix_stadium</name>
  <files>src/lib/rig-mapping.ts</files>
  <action>
    Read src/lib/rig-mapping.ts to find DeviceTarget usage.

    The mapRigToSubstitutions(rigIntent, device) function likely has device-specific branches.
    Add Stadium handling:
    - Stadium stub: return same mapping as Helix LT for now (Phase 38 will refine)
    - Ensure no TypeScript exhaustiveness errors

    Import isStadium if needed.
  </action>
  <verify>rig-mapping.ts compiles with helix_stadium in DeviceTarget</verify>
  <done>rig-mapping.ts handles helix_stadium device target</done>
</task>

<task type="auto">
  <name>Update /api/generate route to handle helix_stadium</name>
  <files>src/app/api/generate/route.ts</files>
  <action>
    Read src/app/api/generate/route.ts to find DeviceTarget switch/if patterns.

    The generate route:
    1. Parses device from request body
    2. Validates device is in DeviceTarget
    3. Calls buildHlxFile or buildPgpFile based on device
    4. Returns appropriate file extension

    Add Stadium handling:
    - Accept "helix_stadium" as valid device string
    - For now (Phase 36 will wire buildHspFile): throw an error or return stub:
      if (isStadium(deviceTarget)) {
        return NextResponse.json({ error: "Stadium builder not yet implemented — Phase 35" }, { status: 501 });
      }
    - Set fileExtension to ".hsp" for Stadium
    - Storage filename: "latest.hsp" for Stadium

    IMPORTANT: Read the full route.ts before editing to understand exact patterns.
  </action>
  <verify>route.ts compiles; "helix_stadium" does not cause TypeScript errors in device string validation</verify>
  <done>/api/generate route accepts helix_stadium without TypeScript errors</done>
</task>

<task type="auto">
  <name>Check all other files for DeviceTarget exhaustiveness and update page.tsx device type</name>
  <files>src/app/page.tsx, src/app/api/map/route.ts, src/app/api/chat/route.ts</files>
  <action>
    Run: grep -r "DeviceTarget\|helix_lt\|helix_floor\|pod_go" src/ --include="*.ts" --include="*.tsx" -l

    For each file that uses DeviceTarget:
    1. Check for switch statements or if/else chains on device values
    2. Add "helix_stadium" case where needed (stub is fine — real implementation later)

    For page.tsx:
    - Find the device state type (likely string or DeviceTarget)
    - The device arrays will be updated in Phase 37 — for now just ensure TypeScript doesn't error

    For /api/map route:
    - Add "helix_stadium" to valid device list (stub — real implementation Phase 38)

    Fix ALL TypeScript exhaustiveness errors. The build must pass cleanly.
  </action>
  <verify>grep finds no TypeScript errors related to unhandled DeviceTarget cases</verify>
  <done>All files with DeviceTarget usage handle helix_stadium</done>
</task>

<task type="auto">
  <name>Run full test suite and build verification</name>
  <action>
    1. Run: cd /c/Users/dsbog/HelixAI && npx vitest run 2>&1 | tail -20
    2. Run: cd /c/Users/dsbog/HelixAI && npm run build 2>&1 | tail -30

    All 108 existing tests must pass. Build must succeed with zero TypeScript errors.

    If tests fail, investigate and fix. The only expected changes are:
    - DeviceTarget union now has 4 values (was 3)
    - DEVICE_IDS now has helix_stadium
    - isStadium() exists
    - Stadium stubs in device-specific branches
  </action>
  <verify>108+ tests passing; npm run build exits 0</verify>
  <done>All tests pass; build succeeds</done>
</task>

</tasks>

<verification>
npx vitest run — all 108 existing tests pass
npm run build — zero TypeScript errors
DEVICE_IDS.helix_stadium === 2490368
DeviceTarget includes "helix_stadium"
STADIUM_CONFIG.STADIUM_MAGIC_HEADER === "rpshnosj"
STADIUM_CONFIG.STADIUM_MAX_BLOCKS_PER_PATH === 12
STADIUM_CONFIG.STADIUM_MAX_SNAPSHOTS === 8
</verification>

<must_haves>
- DeviceTarget union includes "helix_stadium" — verified by TypeScript compilation
- DEVICE_IDS.helix_stadium === 2490368 with source comment
- STADIUM_CONFIG exported from config.ts with all required constants
- npm run build passes with zero TypeScript errors (all exhaustiveness handled)
- All 108 existing tests still pass
</must_haves>

<output>
After completion, create .planning/phases/32-type-system-foundation/32-01-SUMMARY.md with:
- What was changed (DeviceTarget, DEVICE_IDS, config constants, exhaustiveness fixes)
- Files modified list
- Test/build results
- Any deferred items (TODOs left for Phase 33-38)
</output>
