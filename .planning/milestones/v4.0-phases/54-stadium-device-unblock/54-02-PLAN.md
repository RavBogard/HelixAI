---
phase: 54-stadium-device-unblock
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified: []
autonomous: false
requirements:
  - STAD-08

must_haves:
  truths:
    - "Selecting STADIUM in the device picker generates a valid .hsp file — no 400 error"
    - "Five to ten generation requests with varied tone goals all produce .hsp files that open in HX Edit without errors"
    - "Amp parameters in generated Stadium presets are musically sensible — not clipping, not silent, distinguishable between clean and high-gain"
    - "Snapshot names and block enabled states match what HX Edit shows in the snapshot panel"
  artifacts: []
  key_links: []
---

<objective>
Verify Stadium generation end-to-end with HX Edit hardware verification.

Purpose: TypeScript compilation and unit tests cannot confirm that generated .hsp files load correctly on real hardware. HX Edit import verification is the only reliable acceptance gate for Stadium presets. This plan runs 5-10 varied generation requests and verifies each one loads correctly in HX Edit with musically sensible parameters, correct snapshot states, and no import errors.

Output: Human-verified confirmation that Stadium generation produces correct, loadable, musically sensible presets.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-stadium-device-unblock/54-RESEARCH.md
@.planning/phases/54-stadium-device-unblock/54-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stadium .hsp generation and HX Edit hardware verification</name>
  <files>none</files>
  <action>
Present the user with the complete hardware verification protocol. All automated code changes were completed in Plan 01 — this checkpoint confirms the generated .hsp files work with real hardware/software.
  </action>
  <verify>User confirms all four success criteria pass</verify>
  <done>
    - 5-10 varied tone goals tested
    - All generated .hsp files load in HX Edit without errors
    - Amp parameters are musically sensible across clean, crunch, and high-gain
    - Snapshot names and block states match HX Edit's snapshot panel
  </done>
  <what-built>
Complete Stadium device unblock:
- Fixed STADIUM_DEVICE_VERSION to 301990015 (Plan 01)
- Removed access field from all slot params in stadium-builder.ts (Plan 01)
- Fixed amp/cab to fixed grid positions b05/b06 (Plan 01)
- Removed API 400 guard for helix_stadium (Plan 01)
- Added STADIUM to both device pickers in page.tsx (Plan 01)
- Removed "temporarily unavailable" messages (Plan 01)
  </what-built>
  <how-to-verify>
**Setup:**
1. Start the dev server: `npm run dev`
2. Open http://localhost:3000 in your browser
3. Have HX Edit open and ready to import .hsp files

**Test Battery (run 5-10 of these, minimum one from each category):**

**Clean tones (at least 1):**
- "Warm Fender-style clean with subtle chorus and spring reverb"
- "Sparkling jazz clean with smooth compression"

**Crunch tones (at least 1):**
- "Mid-gain Marshall blues crunch with slapback delay"
- "Classic rock rhythm crunch, Vox-inspired"

**High-gain tones (at least 1):**
- "Aggressive modern metal with tight low-end and scooped mids"
- "80s thrash metal with crisp palm mutes"

**Ambient tones (at least 1):**
- "Atmospheric ambient clean with lush reverb and modulated delay"
- "Post-rock swells with shimmer reverb"

**For EACH test:**
1. Select "STADIUM" in the device picker
2. Complete the chat interview with the tone goal
3. Generate the preset
4. Download the .hsp file
5. Import into HX Edit — verify NO import errors or warnings
6. Check the signal chain visualization — amp and cab should appear at correct positions
7. Switch between snapshots — verify snapshot names are correct and block enabled states change as expected
8. Verify amp parameters look musically sensible (Drive/Master/Bass/Mid/Treble are not all at 0 or 1.0)

**Success Criteria Check:**
- [ ] SC1: No 400 error when selecting STADIUM
- [ ] SC2: All 5-10 .hsp files open in HX Edit without errors
- [ ] SC3: Amp params are musically sensible (clean amps have low Drive, high-gain amps have higher Drive)
- [ ] SC4: Snapshot names and block states visible and correct in HX Edit snapshot panel
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any HX Edit import failures or parameter issues</resume-signal>
</task>

</tasks>

<verification>
1. User confirms 5-10 .hsp files loaded in HX Edit without errors
2. User confirms amp parameters are musically sensible across tone categories
3. User confirms snapshot names and block states are correct
4. User confirms no 400 error when selecting STADIUM
</verification>

<success_criteria>
- Stadium device selection works in the UI
- Generated .hsp files load in HX Edit without import errors
- Amp parameters are musically sensible and distinguishable across tone categories
- Snapshots display correctly with expected names and block states
- Human approval of all four roadmap success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/54-stadium-device-unblock/54-02-SUMMARY.md`
</output>
