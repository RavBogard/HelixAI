---
phase: 53-stadium-builder-rebuild
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - src/lib/helix/stadium-builder.test.ts
autonomous: false
requirements: [STAD-07]

must_haves:
  truths:
    - "Generated .hsp JSON structure matches Agoura_Bassman.hsp reference at every level: meta, preset.flow block structure, param encoding, block keys, type fields, cab param count, cursor field"
    - "The generated .hsp file opens in HX Edit without import errors — confirmed by human verification on real hardware/software"
  artifacts:
    - path: "src/lib/helix/stadium-builder.test.ts"
      provides: "Field-by-field structural comparison test against real .hsp reference patterns"
  key_links:
    - from: "src/lib/helix/stadium-builder.ts"
      to: "HX Edit import"
      via: "Generated .hsp file loaded into HX Edit application"
      pattern: "buildHspFile"
---

<objective>
Validate that the fixed Stadium builder produces .hsp files structurally identical to real Line 6 presets via automated comparison test and manual HX Edit import verification.

Purpose: TypeScript compilation and unit tests are necessary but not sufficient — the hard acceptance gate for Phase 53 is that a generated .hsp file actually loads in HX Edit without errors. This plan adds a field-by-field structural comparison test and a human verification checkpoint.

Output: Extended test coverage with structural comparison assertions, plus confirmed HX Edit import.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/53-stadium-builder-rebuild/53-RESEARCH.md
@.planning/phases/53-stadium-builder-rebuild/53-01-SUMMARY.md
@src/lib/helix/stadium-builder.ts
@src/lib/helix/stadium-builder.test.ts

<interfaces>
<!-- Reference .hsp structure from research — the ground truth for field comparison -->

Agoura_Bassman.hsp structure (verified from 53-RESEARCH.md):
```
meta: { device_id, device_version, info, name }
preset: {
  clip: { end, filename, path, start },
  cursor: { flow, path, position },
  flow: [
    { // flow 0 — active path
      "@enabled": { value: true },
      b00: { // input
        "@enabled": { value: true },
        endpoint: "b13",
        favorite: 0,
        harness: { "@enabled": { value: true } },
        path: 0,
        position: 0,
        slot: [{ "@enabled": { value: true }, model: "P35_InputInst1", params: { ... }, version: 0 }],
        type: "input"
      },
      b05: { // amp — key matches position
        "@enabled": { snapshots: [...], value: true },
        favorite: 0,
        harness: { "@enabled": { value: true }, params: { EvtIdx: { value: -1 }, bypass: { value: false }, upper: { value: true } } },
        linkedblock: { block: "b06", flow: 0 },
        path: 0,
        position: 5,
        slot: [{ "@enabled": { value: true }, model: "Agoura_AmpUSTweedman", params: { Bass: { value: 0.64 }, ... }, version: 0 }],
        type: "amp"
      },
      b06: { // cab — linked to amp, 10 params
        linkedblock: { block: "b05", flow: 0 },
        position: 6,
        slot: [{ model: "HD2_CabMicIr_...", params: { Angle: {value}, Delay: {value}, Distance: {value}, HighCut: {value}, IrData: {value}, Level: {value}, LowCut: {value}, Mic: {value}, Pan: {value}, Position: {value} }, version: 0 }],
        type: "cab"
      },
      b13: { // output
        position: 13,
        type: "output"
      }
    },
    { // flow 1 — empty path (InputNone + OutputMatrix)
      "@enabled": { value: true },
      b00: { type: "input", slot: [{ model: "P35_InputNone", ... }] },
      b13: { type: "output" }
    }
  ],
  params: { activeexpsw, activesnapshot, inst1Z, tempo },
  snapshots: [ { color, expsw, name, source, tempo, valid }, ... ],  // 8 entries
  sources: { "16843008": { fs_color, fs_label, fs_topidx }, ... },  // 24 entries
  xyctrl: { rbtime, rubberband, x, y }
}
```

Key structural invariants to verify:
- No `access` field anywhere in entire JSON
- Block keys are `bNN` where NN matches `position` field value
- Amp at b05/position 5, cab at b06/position 6
- All non-structural blocks have type "fx"
- Cab has exactly 10 params
- cursor field present at preset level
- flow array has exactly 2 entries (active + empty)
- snapshots array has exactly 8 entries
- sources has 24 entries (12 per flow)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structural comparison test against real .hsp reference patterns</name>
  <files>src/lib/helix/stadium-builder.test.ts</files>
  <behavior>
    - Test: Generated .hsp has exactly 2 flow entries (flow 0 active, flow 1 empty with InputNone)
    - Test: Flow 1 has only b00 (InputNone) and b13 (OutputMatrix) — no other blocks
    - Test: snapshots array has exactly 8 entries, valid snapshots have color field, invalid ones do not
    - Test: sources object has exactly 24 entries (12 flow-0 starting at 16843008, 12 flow-1 starting at 16843264)
    - Test: Amp block has linkedblock pointing to cab key; cab block has linkedblock pointing to amp key
    - Test: Every block key in flow 0 matches the pattern `bNN` and the block's position field equals NN
    - Test: harness params for amp block include EvtIdx, bypass, upper (no access field)
    - Test: harness params for cab block include EvtIdx, bypass, dual, upper (no access field)
    - Test: Overall JSON structure matches reference (meta has device_id/device_version/info/name; preset has clip/cursor/flow/params/snapshots/sources/xyctrl)
  </behavior>
  <action>
Add a new `describe("structural comparison with real .hsp reference")` block to `stadium-builder.test.ts`. Use the same test fixture from Plan 01 Task 1.

Write these structural validation tests:

1. **Two flows:** `json.preset.flow.length === 2`. Flow 0 has `@enabled: { value: true }` plus blocks. Flow 1 has only `@enabled`, `b00` (InputNone model), and `b13` (OutputMatrix model).

2. **Block key-position invariant:** For every key in flow 0 that starts with "b", parse the number from the key and assert it equals the block's `position` field. This is the critical invariant from the research — key `bNN` implies `position: NN`.

3. **Amp-cab linking:** The amp block at b05 has `linkedblock: { block: "b06", flow: 0 }`. The cab block at b06 has `linkedblock: { block: "b05", flow: 0 }`.

4. **Snapshots structure:** Array length is 8. For each snapshot in the fixture (e.g., 4 valid), assert it has `color`, `expsw`, `name`, `source`, `tempo`, `valid: true`. For remaining slots, assert `valid: false`, no `color` field, `expsw: -1`.

5. **Sources count:** `Object.keys(json.preset.sources).length === 24`. First key starts at "16843008" (flow 0). Key "16843264" exists (flow 1 start).

6. **Harness structure:** Amp harness has `@enabled: { value: true }` and params `{ EvtIdx: { value: -1 }, bypass: { value: false }, upper: { value: true } }`. Cab harness has `{ EvtIdx: { value: -1 }, bypass: { value: false }, dual: { value: true }, upper: { value: true } }`. No param in any harness contains an `access` key.

7. **Top-level preset keys:** `Object.keys(json.preset).sort()` contains at minimum: `clip`, `cursor`, `flow`, `params`, `snapshots`, `sources`, `xyctrl`.

8. **Meta structure:** `json.meta` has `device_id` (number), `device_version` (number), `info` (string), `name` (string, <= 32 chars).

All tests should pass against the already-fixed code from Plan 01.
  </action>
  <verify>
    <automated>npx vitest run src/lib/helix/stadium-builder.test.ts && npx vitest run</automated>
  </verify>
  <done>All structural comparison tests pass. Full suite green. Every key structural invariant from the Agoura_Bassman.hsp reference is verified by an automated test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HX Edit import verification</name>
  <what-built>
The Stadium builder has been fully rewritten to fix all 5 confirmed format bugs:
1. Parameter encoding uses `{ value: X }` format (no `access` field)
2. Block keys use slot-grid allocation (b05=amp, b06=cab, b13=output)
3. All effect blocks emit `type: "fx"`
4. Cab blocks emit all 10 parameters
5. Preset includes `cursor` field

All automated tests pass including structural comparison against real .hsp reference patterns.
  </what-built>
  <how-to-verify>
1. Generate a Stadium preset by running the generation pipeline (or invoke `buildHspFile()` directly with a test PresetSpec — see test file for fixture)
2. Save the output as a `.hsp` file
3. Open HX Edit (connected to Stadium or in offline mode)
4. Import the generated `.hsp` file via File > Import Preset
5. Verify the preset loads without any error dialogs
6. Check that the signal chain is visible (amp, cab, effects in correct order)
7. Check that snapshot names appear in the snapshot panel
8. If import fails, note the exact error message — this indicates a remaining format issue

**Alternative if HX Edit unavailable:** Compare the generated JSON field-by-field against `C:/Users/dsbog/Downloads/NH_STADIUM_AURA_REFLECTIONS/Agoura_Bassman.hsp` (skip the 8-byte magic header to get to JSON). Verify every structural element matches: param format, block keys, type fields, cab param count, cursor presence.
  </how-to-verify>
  <resume-signal>Type "approved" if HX Edit import succeeds, or describe the import error for further debugging.</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/helix/stadium-builder.test.ts` — all tests pass (unit + structural comparison)
2. `npx vitest run` — full test suite green
3. HX Edit import verification — human-confirmed successful load
</verification>

<success_criteria>
- STAD-07: Generated .hsp file loads in HX Edit without errors — confirmed by human verification, not just by TypeScript compilation
- Structural comparison tests cover all key invariants from real .hsp reference files
- Phase 53 is complete when HX Edit import is approved
</success_criteria>

<output>
After completion, create `.planning/phases/53-stadium-builder-rebuild/53-02-SUMMARY.md`
</output>
