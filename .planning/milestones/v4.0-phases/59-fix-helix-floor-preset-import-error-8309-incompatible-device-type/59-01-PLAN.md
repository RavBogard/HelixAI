---
phase: 59-fix-helix-floor-preset-import-error-8309-incompatible-device-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - FLOOR-01

must_haves:
  truths:
    - "The correct Helix Floor device ID is confirmed by a real hardware export from Paul Morgan or another Helix Floor user"
    - "The confirmed value is documented with provenance (source file, user, date)"
  artifacts:
    - path: ".planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-01-SUMMARY.md"
      provides: "Confirmed Helix Floor device ID with provenance"
      contains: "data.device"
  key_links: []
---

<objective>
Get the correct Helix Floor device ID from a real hardware export before changing any code.

Purpose: The Helix Floor device ID has been changed TWICE before (Phase 23 and Phase 31), and users STILL get error 8309. The current value `2162691` (0x210003) has ZERO corroborating reference files in the 39-file collection. Before making a third attempt, we MUST get a verified value directly from the current reporter (Paul Morgan) who has confirmed Helix Floor hardware.

Output: A documented, confirmed device ID integer with full provenance.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Get confirmed Helix Floor device ID from Paul Morgan</name>
  <action>
Ask Paul Morgan (or any confirmed Helix Floor user) to perform these steps:

1. Connect Helix Floor to computer via USB
2. Open HX Edit (any firmware version)
3. Export any preset: File > Export Preset (or drag a preset from the list to desktop)
4. Open the exported `.hlx` file in any text editor (it is plain JSON)
5. Find `"device":` near the top of the file (inside the `"data"` object)
6. Report the integer number after `"device":`

Alternative (command line):
```
python3 -c "import json; d=json.load(open('YOUR_FILE.hlx')); print('data.device:', d['data']['device'])"
```

**Expected values and what they mean:**
- `2162689` (0x210001) — confirms research hypothesis; 15 reference files support this value
- `2162691` (0x210003) — current code is correct; bug has a different cause (escalate investigation)
- Any other value — document and use the new value

**CRITICAL:** Do NOT proceed to Plan 02 without this confirmed value. This is the THIRD time this bug is being fixed. The Phase 23 fix was obtained from a single user (John Rattinger) whose value `2162691` may have been from a Helix Rack, not a Helix Floor.

**Bonus (if possible):** Also ask Tal Solomon Vardy to export a preset from their device. If two users independently report the same device ID, confidence is very high.
  </action>
  <how-to-verify>
Paul Morgan (or another Helix Floor user) provides the `data.device` integer from a real `.hlx` file exported from their Helix Floor hardware via HX Edit.
  </how-to-verify>
  <resume-signal>Provide the confirmed device ID integer (e.g., "2162689" or "2162691") along with the source (which user, which file). Then type "confirmed" to proceed to Plan 02.</resume-signal>
</task>

</tasks>

<verification>
- The confirmed device ID is an integer in the range 2162688-2162699 (Helix family device IDs)
- The source user has confirmed Helix Floor hardware (not Helix Rack, LT, or other variant)
- The value is documented before any code changes begin
</verification>

<success_criteria>
A confirmed Helix Floor device ID integer is obtained from a real hardware export, with provenance documented (user name, file name, date). Plan 02 can proceed with this value.
</success_criteria>

<output>
After completion, create `.planning/phases/59-fix-helix-floor-preset-import-error-8309-incompatible-device-type/59-01-SUMMARY.md`
</output>
